# B5 exception chaining pack README

Run:

```bash
bash scripts/run_exception_chaining_goal_gate.sh
```

This is a runtime/ABI slice for exception chaining and traceback frames.
Compiled source-level syntax lowering remains separate.
