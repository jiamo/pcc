# Coroutine/scheduler roots package README

Run:

```bash
bash scripts/run_coroutine_scheduler_roots_gate.sh
```

This package closes the shared GC root/update model needed by Backend #2/#3/#4
for stackless Python coroutine/task execution.
