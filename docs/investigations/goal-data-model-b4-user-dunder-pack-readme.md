# B4 user dunder pack README

Run:

```bash
bash scripts/run_user_dunder_goal_gate.sh
```

This is a runtime dispatch slice.  It does not yet claim the source-level
compiled no-libpython acceptance tests for all dunder syntax sites.
