{
  "domain": "comprehension_iter_format",
  "idioms_tested": 20,
  "persisted_to": "/private/tmp/claude-501/-Users-jiamo-my-pcc/244ce9fe-a80c-4b8c-bff2-7f5caa8ac5c3/scratchpad/gap_findings/sweep2/comprehension_iter_format.json",
  "confirmed_gaps": [
    {
      "idiom": "import itertools\nfor x in itertools.chain(a, b):\n    print(x)",
      "kind": "MISMATCH",
      "observed": "Compile rc=0, but at runtime: AttributeError: chain. IR lowers `itertools.chain(a,b)` to py_obj_getattr(<\"itertools\" string-literal placeholder @.pystr.obj.1>, @.pyattr.chain) then py_obj_call; getattr on the module-name string has no 'chain' attribute -> raises. Contrast: itertools.product(...) emits a clean direct call `user_itertools_product(...)` and works; itertools.islice/count/repeat (module-level functions) work; itertools.chain.from_iterable(nested) also works. ONLY calling the module-level instance `chain` via its __call__ fails.",
      "expected": "1\\n2\\n3\\n4\\n5",
      "root_cause": "pcc/py_frontend/codegen/native_modules.py::_maybe_emit_native_module_alias_call (around line 2766) handles only kind=='function' and kind=='class'; for kind=='module_global' it hits `if kind != \"class\": return None` and returns None, so the call falls through to generic dynamic dispatch which does py_obj_getattr on the module-name string placeholder -> AttributeError. In pcc/py_stdlib/itertools.py `chain = _Chain()` is a module-level INSTANCE, classified kind=='module_global' (DynType) by pipeline.py:4671 (vs `product`/`islice`/`count`/`repeat` which are `def`s -> kind=='function'). The defining module's init `_pcc_py_module_top_itertools()` DOES build the _Chain instance and store it into the extern global @.modvar.itertools.chain (verified IR lines 11027-11031), and _emit_native_module_export_value already loads kind=='module_global' from that global. The fix wires the call site to load the module_global value and py_obj_call it.",
      "owning_files": ["pcc/py_frontend/codegen/native_modules.py"],
      "proposed_edits": [
        {
          "file": "pcc/py_frontend/codegen/native_modules.py",
          "old_string": "        if kind != \"class\":\n            return None\n        if not isinstance(attr.obj, Name):\n            return None\n        class_info = self._ensure_native_module_alias_class_export(",
          "new_string": "        if kind == \"module_global\":\n            # ``mod.attr(...)`` where ``attr`` is a module-level VALUE binding\n            # (e.g. ``chain = _Chain()`` in pcc/py_stdlib/itertools.py).\n            # _emit_native_module_export_value loads it from the defining\n            # module's ``.modvar.<mod>.<attr>`` extern global; we then call it\n            # through py_obj_call (which dispatches __call__ on the instance).\n            # Without this branch the call fell through to a generic\n            # py_obj_getattr on the module-name string placeholder and raised\n            # AttributeError (e.g. ``itertools.chain(a, b)``).\n            callable_obj = self._emit_native_module_export_value(\n                module_name,\n                attr.name,\n                info,\n            )\n            kwdict_unpack = self._split_starstar_kwargs_unpack(expr.args)\n            arg_exprs = expr.args\n            kwargs_expr = None\n            if kwdict_unpack is not None:\n                arg_exprs, kwargs_expr = kwdict_unpack\n            args_owned = not self._is_starred_unpack(arg_exprs)\n            args_tuple = self._emit_dynamic_call_args_tuple(arg_exprs)\n            kwargs_obj = self._emit_dynamic_call_kwargs_object(\n                expr.kwargs,\n                kwargs_expr,\n                expr.span,\n            )\n            result = self.builder.call(\n                self.runtime[\"py_obj_call\"],\n                [callable_obj, args_tuple, kwargs_obj],\n                name=self._fresh(f\"modglobal.call.{attr.name}\"),\n            )\n            if args_owned:\n                self._gc_release(args_tuple)\n            if expr.kwargs:\n                self._gc_release(kwargs_obj)\n            self._gc_release(callable_obj)\n            self._emit_post_call_err_check(expr.span)\n            return result\n        if kind != \"class\":\n            return None\n        if not isinstance(attr.obj, Name):\n            return None\n        class_info = self._ensure_native_module_alias_class_export(",
          "note": "Adds the kind=='module_global' callable branch to _maybe_emit_native_module_alias_call. old_string is unique (only one `if kind != \"class\"` immediately precedes `_ensure_native_module_alias_class_export`). All helpers used (_emit_native_module_export_value, _split_starstar_kwargs_unpack, _is_starred_unpack, _emit_dynamic_call_args_tuple, _emit_dynamic_call_kwargs_object, _gc_release, _emit_post_call_err_check, _fresh) already exist on this mixin/class. Mirrors the gc/args/kwargs/err-check pattern of method_call_expression_lowering.py::_emit_callable_attribute_call. Verified high-confidence: the extern global is populated at module init (IR), and product/islice/count/repeat/chain.from_iterable already work, isolating the gap to instance-via-__call__."
        }
      ],
      "needs_new_runtime_fn": false,
      "both_tier": false,
      "confidence": "high"
    },
    {
      "idiom": "for v in filter(None, [0, 1, 2, 0, 3, None, 4]):\n    print(v)\n# and: list(filter(None, xs))",
      "kind": "MISMATCH",
      "observed": "Compile rc=0, but at runtime: NameError: name 'filter' is not defined. Both `for v in filter(None, xs)` and `list(filter(None, xs))` fail. IR shows `@.name_error.1 = \"name 'filter' is not defined\"` and a `py_obj_call(<filter as name>, ...)` -> the `filter` builtin has no value/iterator-object form in the no-libpython runtime; it is only handled inline by the `next(filter(...))` consumer (iterator_builtin_lowering.py::_maybe_emit_next_filter_builtin, which DOES handle the None predicate via py_obj_truthy).",
      "expected": "1\\n2\\n3\\n4   (for-loop form); [1, 2, 3, 4]   (list form)",
      "root_cause": "`filter(None, iterable)` (None predicate == keep-truthy) is not lowered by the list()/comprehension consumers. pcc/py_frontend/codegen/list_builtin_lowering.py::_maybe_emit_list_from_map_filter requires call.args[0] to be a Name (a function reference) and returns None when args[0] is NoneLit, so `list(filter(None, xs))` falls through and `filter` resolves as an unbound name -> NameError. The for-loop form `for v in filter(None, xs)` is a SEPARATE consumer (generic expr eval of the for-source) that likewise has no `filter` handler and hits the same NameError. The runtime has no general `filter` object; only next(filter(...)) is inlined. The bounded apply-ready fix below covers the list()/comprehension consumer by handling the None predicate with each element's own truthiness (same as the working next(filter(None,...)) path). NOTE: the for-loop consumer needs a parallel for-normalization handler (not included here) — same root cause, different dispatch site (for_normalization_lowering.py, alongside the existing enumerate/zip normalizers).",
      "owning_files": ["pcc/py_frontend/codegen/list_builtin_lowering.py", "pcc/py_frontend/codegen/for_normalization_lowering.py"],
      "proposed_edits": [
        {
          "file": "pcc/py_frontend/codegen/list_builtin_lowering.py",
          "old_string": "from ..py_ast import (\n    Call,\n    ClassType,\n    DictType,\n    DynType,\n    ListExpr,\n    ListType,\n    Name,\n    TupleExpr,\n    TupleType,\n)",
          "new_string": "from ..py_ast import (\n    Call,\n    ClassType,\n    DictType,\n    DynType,\n    ListExpr,\n    ListType,\n    Name,\n    NoneLit,\n    TupleExpr,\n    TupleType,\n)",
          "note": "Edit A of 2: import NoneLit so the filter(None, ...) guard can detect the None predicate. NoneLit is defined in pcc/py_frontend/py_ast.py."
        },
        {
          "file": "pcc/py_frontend/codegen/list_builtin_lowering.py",
          "old_string": "        if (\n            not isinstance(call.func, Name)\n            or call.func.ident not in (\"map\", \"filter\")\n            or call.kwargs\n            or len(call.args) != 2\n            or not isinstance(call.args[0], Name)\n        ):\n            return None\n        mode = call.func.ident\n        func_name = call.args[0].ident\n        fn = self.functions.get(func_name)\n        builtin_map_str = mode == \"map\" and func_name == \"str\"\n        if fn is None and not builtin_map_str:\n            return None\n        ast_fd = self._find_user_funcdef(func_name) if fn is not None else None",
          "new_string": "        if (\n            not isinstance(call.func, Name)\n            or call.func.ident not in (\"map\", \"filter\")\n            or call.kwargs\n            or len(call.args) != 2\n        ):\n            return None\n        mode = call.func.ident\n        # ``filter(None, src)`` — the None predicate means \"keep truthy\n        # elements\"; there is no callable to resolve, so skip the function\n        # lookup and use each element's own truthiness (mirrors the\n        # next(filter(None, ...)) path in iterator_builtin_lowering.py).\n        pred_is_none = mode == \"filter\" and isinstance(call.args[0], NoneLit)\n        if not pred_is_none and not isinstance(call.args[0], Name):\n            return None\n        func_name = call.args[0].ident if not pred_is_none else \"\"\n        fn = self.functions.get(func_name) if not pred_is_none else None\n        builtin_map_str = mode == \"map\" and func_name == \"str\"\n        if not pred_is_none and fn is None and not builtin_map_str:\n            return None\n        ast_fd = self._find_user_funcdef(func_name) if fn is not None else None",
          "note": "Edit B of 2: relax the guard to accept filter(None, src) and compute pred_is_none. When pred_is_none, fn=None, builtin_map_str=False, item_slot stays None (the existing `if fn is not None:` setup at line ~369 is skipped), and ast_fd=None. Paired with Edit C below which routes the body to element truthiness. old_string is the verbatim guard+setup block; unique in the file."
        },
        {
          "file": "pcc/py_frontend/codegen/list_builtin_lowering.py",
          "old_string": "        if fn is not None:\n            self.builder.store(elem, item_slot)\n            arg_expr = Name(\n                span=call.span,\n                ty=DynType(name=\"dyn\"),\n                ident=temp_name,\n            )\n            result = self._emit_direct_user_function_call(\n                display_name=func_name,\n                fn=fn,\n                ast_func_def=ast_fd,\n                args=(arg_expr,),\n                kwargs=(),\n            )\n            result_ty = ast_fd.return_ty or DynType(name=\"dyn\")\n        else:\n            result = self.builder.call(\n                self.runtime[\"py_obj_str\"],\n                [elem],\n                name=self._fresh(\"map.str\"),\n            )\n            result_ty = DynType(name=\"dyn\")",
          "new_string": "        if pred_is_none:\n            # filter(None, src): the element is its own predicate result; the\n            # filter keep-logic below uses _truthy(elem) -> py_obj_truthy.\n            result = elem\n            result_ty = DynType(name=\"dyn\")\n        elif fn is not None:\n            self.builder.store(elem, item_slot)\n            arg_expr = Name(\n                span=call.span,\n                ty=DynType(name=\"dyn\"),\n                ident=temp_name,\n            )\n            result = self._emit_direct_user_function_call(\n                display_name=func_name,\n                fn=fn,\n                ast_func_def=ast_fd,\n                args=(arg_expr,),\n                kwargs=(),\n            )\n            result_ty = ast_fd.return_ty or DynType(name=\"dyn\")\n        else:\n            result = self.builder.call(\n                self.runtime[\"py_obj_str\"],\n                [elem],\n                name=self._fresh(\"map.str\"),\n            )\n            result_ty = DynType(name=\"dyn\")",
          "note": "Edit C of 2 (applies after Edit B): adds the pred_is_none leading branch so filter(None,...) sets result=elem; the existing filter else-branch (mode != 'map') then computes keep = self._truthy(result, result_ty), which for a DynType pcc object routes through py_obj_truthy (coercion_lowering.py:242-263) — exactly matching the working next(filter(None,...)) semantics (None/0/empty are falsy). old_string is the verbatim result-computation block; unique in the file. Confidence medium because this fixes only the list()/comprehension consumer; the for-loop consumer (the probe's original form) still NameErrors and needs a separate for-normalization handler."
        }
      ],
      "needs_new_runtime_fn": false,
      "both_tier": false,
      "confidence": "medium"
    }
  ]
}
