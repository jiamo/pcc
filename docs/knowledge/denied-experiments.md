# Denied experiments and measured dead ends

GENERATED by `scripts/distill_investigations.py` from
`docs/investigations/*.md`. Do not edit by hand.

Every line below is a change somebody already wrote, measured and
disproved, or a hypothesis an experiment ruled out. Re-deriving one
costs a full rebuild/measure cycle and produces a change known not to
work. **Read this page before proposing a fix**; if your idea is here,
either cite new evidence that overturns the verdict or do not make the
change. The linked investigation holds the measurement.

621 verdicts across 128 investigations.

## [Investigation: backend-0 finalizer re-enters the tracked-object table lock](../investigations/backend0-finalizer-table-lock-reentrancy.md)

- No.2 Release the table lock around arbitrary user code [DENIED]
- No.3 Skip tracking while finalizers run [DENIED]
- DENIED — The current candidate array stores raw side-table node pointers. Releasing
- DENIED — Dropping track/untrack events weakens object-graph semantics and can hide live

## [Investigation: BOC speedup proofs collapse under xdist CPU contention](../investigations/boc-speedup-proofs-xdist-cpu-contention.md)

- [DENIED — incomplete]
- No.2 Lower the speedup floors [DENIED]
- DENIED — incomplete — The marker correctly put both speedup nodes on `gw0`, and the two focused nodes
- DENIED — The observed results are correct-but-contended and narrowly below the existing
- DENIED — Both failures occurred only in the broad concurrent suite and retained their

## [Investigation: types dotted-name rsplit reintroduced libpython fallback](../investigations/bootstrap-types-rsplit-libpython-fallback.md)

- Fix the bootstrap. The current bootstrap/fallback gates regressed: the tight stage1 closure

## [Investigation: LowIR fallback and borrowed return ownership broke full bootstrap](../investigations/bootstrap-user-function-low-ir-fallback-2026-06-01.md)

- The full strict self bootstrap regressed after a long period of success:
- [DENIED BY USER]
- No.3 Treat user-function call results as borrowed in callers [DENIED]
- No.5 Dataclass copy / `replace(...)` field incref bug [DENIED]
- DENIED BY USER — The user explicitly rejected disabling tuple auto `py_gc_track` in
- DENIED — Generated pcc user-function calls use the normal owned-result convention:
- DENIED — This was a plausible locator because the crash occurred near
- `replace(expr, ..., ty=ty)`, but source inspection denied it. The runtime field
- The denied approaches are important: tuple auto-tracking was not the root

## [Class method borrowed parameters were not registered as GC roots](../investigations/class-method-borrowed-parameter-roots.md)

- No.2 Teach precise stack maps to exempt pin/unpin regions [DENIED]
- DENIED — This would preserve the frontend inconsistency and require path-sensitive

## [Investigation: contextual per-module fallback gate for Python self-host](../investigations/contextual-per-module-fallback-gate.md)

- No.1 Keep raw per-module as the only hard gate [DENIED]
- No.2 Drop per-module gates entirely [DENIED]
- DENIED — This uses the wrong semantic model for mixins. It measures missing host
- DENIED — This preserves correctness for pcc1 but loses the canary for accidental

## [Investigation: imported valueclass return annotations lose aggregate ABI](../investigations/cross-module-imported-valueclass-return-abi.md)

- No.2 Return a boxed object or duplicate the record in the kernel module [DENIED]
- DENIED — This restores object projection or duplicates semantic records, contradicts
- the value-model contract, and repeats the workaround denied by the predecessor

## [Investigation: cross-module valueclass methods lose aggregate ABI](../investigations/cross-module-valueclass-aggregate-abi-export.md)

- No.2 Move every value record into its consumer module [DENIED].
- DENIED — This duplicates compiler records, hides the generic ABI defect, and directly

## [Investigation: free-function call signatures retain valueclass shells](../investigations/cross-module-valueclass-free-function-call-signature.md)

- No.2 Box handles, split their fields at every call, or duplicate records [DENIED].
- DENIED — The physical ABI and class definition are already correct. Object boxing,
- export defect and repeat the predecessors' denied interface workarounds.

## [Investigation: dict-splat operands bypass rooted unwind cleanup](../investigations/dict-splat-stackmap-rooted-unwind.md)

- [DENIED] Relax or merge inconsistent precise root states at `err.exit`.
- [DENIED] Disable splat error checks or special-case the regression program.
- [DENIED] Release only on the normal path. Nested operand and runtime-call

## [Investigation: eager fallback fixture prevents bounded phase validation](../investigations/fallback-baseline-eager-fixture-prevents-sharding.md)

- No.2 Widen the unchanged aggregate command timeout [DENIED].
- DENIED — The repository requires sharding after a suite cannot fit its watchdog. Tests

## [Investigation: marshal raw per-module fallback ratchet exceeds baseline](../investigations/fallback-baseline-marshal-raw-ratchet-2026-07-10.md)

- No.1 Recapture the marshal baseline at 341 [DENIED]
- No.2 Pin the gate to CPython 3.13 [DENIED]
- DENIED — This would hide a real ratchet regression and violates the M0 task boundary.
- DENIED — Direct 3.13 and 3.14 probes both emitted the same 341 calls.

## [Investigation: shared field walker lacks a standalone static export](../investigations/field-walk-static-export-fallback-regression.md)

- No.2 Increase the two consumer ceilings [DENIED].
- DENIED — A missing registration with a successful causal substitution is a repairable

## [Investigation: five-GC matrix timeout contract drift](../investigations/five-gc-matrix-timeout-contract-drift.md)

- This investigation denied both easy stories: the first 900-second timeout was

## [Investigation: freestanding backend-0 GC slot actions](../investigations/freestanding-gc-backend0-slot-actions.md)

- action ownership change itself is not the cause.

## [Investigation: move raw GC control exports into strict freestanding pcc-Python](../investigations/freestanding-gc-control-production-ownership.md)

- No.2 Mark all of `py_obj_gc.py` freestanding immediately [DENIED]
- No.3 Duplicate the exports in both modules and let archive order select one [DENIED]
- DENIED — The remaining object contains cycle collection, managed list construction,
- DENIED — Duplicate definitions make ownership link-order-dependent and can silently

## [Investigation: freestanding incremental/concurrent GC scheduler](../investigations/freestanding-gc-incremental-concurrent-scheduler.md)

- delegate to the strict scheduler. Preserve the denied auto-sweep boundary:
- The strict auto-step path contains no sweep call, preserving the prior denied

## [Investigation: move production GC pointer indexes from C to freestanding pcc-Python](../investigations/freestanding-gc-index-table-production-ownership.md)

- No.2 Reuse the managed Python dictionary implementation [DENIED]
- No.3 Retain the C table as a permanent GC-kernel exception [DENIED]
- DENIED — The dictionary allocates and traces managed Python objects. Using it beneath
- DENIED — The table is an algorithmic data structure expressible through the owned raw

## [Investigation: move production GC state storage into a freestanding object](../investigations/freestanding-gc-state-production-ownership.md)

- No.2 Mark all of `py_substrate.py` freestanding [DENIED]
- No.3 Leave state in the managed substrate until the full algorithm moves [DENIED]
- DENIED — The module owns high-level exception tables, singleton/class construction,
- DENIED — The state declarations are a closed, behavior-preserving, zero-dependency

## [Investigation: freestanding GC telemetry counter ABI drift](../investigations/freestanding-gc-telemetry-counter-abi-drift.md)

- No.2 Preserve the old duplicate numeric mapping for compatibility [DENIED]
- DENIED — The public ABI is the enum in `pcc/py_runtime/include/py_runtime.h`, and the C

## [Investigation: move default-GC track/untrack into strict freestanding pcc-Python](../investigations/freestanding-gc-track-untrack-production-ownership.md)

- No.2 Duplicate the unlink algorithm in both modules [DENIED]
- No.3 Remove the threaded backend-4 suppression branch [DENIED]
- DENIED — Two implementations could drift in prev/next/head/count updates and violate
- DENIED — That branch is established runtime behavior. Deleting it would make the split

## [Investigation: production pcc-Python runtime still archives C objects](../investigations/freestanding-runtime-final-no-c-closure.md)

- siblings) fail with "ON-mode non-bridge py_cpy_* calls regressed: 10 >
- Expanding the allowed C-symbol set or renaming the object again is DENIED:

## [Investigation: a live exception's referents (message) are reclaimed by the tracing collect (#1/#2/#3/#4)](../investigations/gc-5backend-exception-referent-roots-no-libpython.md)

- No.2 `py_gc_track` the exception in `py_exc_alloc` [DENIED — exc is already indexed by `pcc_gc_alloc` on backends 1-4; tracking is redundant, and it's a runtime fix for a frontend bug; reverted]
- No.3 fix a propagation/catch over-decref [DENIED — there is no over-decref; `pcc_gc_note_object_freeing(exc)` fires only inside gc.collect, never in propagation]
- DENIED — After wipe+rebuild, the repro still returned `<null>` on #1/#2/#3 (and changed

## [Investigation: valueclass pointer payload roots under backend #4 relocation](../investigations/gc-5backend-valueclass-pointer-payload-roots-no-libpython.md)

- causes endless linear probing" mechanism is therefore denied for the current

## [Investigation: native gc callbacks should avoid libpython fallback](../investigations/gc-api-callbacks.md)

- CONFIRMED|DENIED|DENIED BY USER — pending

## [Investigation: Backend 1 auto step should sweep candidates and discharge debt](../investigations/gc-backend1-auto-step-sweep-debt.md)

- No.1 Let backend #1/#2 automatic steps sweep existing candidates [DENIED]
- DENIED — This made the pcc-Python churn probe abort during the loop, after printing only
- The denied alternative, sweeping candidates during automatic steps, reduced the

## [Investigation: Backend 1 explicit collect must sweep with live roots](../investigations/gc-backend1-explicit-collect-sweep.md)

- The backend #1 production gate regressed again with a different signature:

## [Investigation: Backend 1 needs codegen roots for owned object locals](../investigations/gc-backend1-owned-local-frame-roots.md)

- `docs/investigations/gc-backend1-auto-step-sweep-debt.md` denied sweeping

## [Investigation: Backend #1 reclaims a resurrected-then-dropped object one explicit collect late](../investigations/gc-backend1-resurrected-object-reclaim-lags-one-collect.md)

- root set produces zero gray objects [DENIED]
- DENIED — `pcc_gc_begin_mark_cycle` was never reached on the failing collect, so its
- No.2 landed in both mirrors; No.1 is DENIED and its whitening-pass detail is

## [Investigation: backend 1 threaded explicit collection invalidates a live list](../investigations/gc-backend1-threaded-explicit-collect-invalid-list.md)

- report, so it is denied as the deciding oracle for this race.
- reject this dedicated state. Unlike the denied `refcount == 0` rule, this does

## [Investigation: Backend #2 CMS worker is unstable under focused and full GC gates](../investigations/gc-backend2-cms-worker-instability.md)

- The threaded runtime had regressed into a contradictory state: the backend2

## [Investigation: Backend 3 threaded minor arena ownership](../investigations/gc-backend3-threaded-minor-arena.md)

- parked at GC safepoints. `py_obj_dealloc.c` had regressed the trashcan depth,

## [Investigation: GC3 million-vthread release is quadratic](../investigations/gc-backend3-vthread-million-release-quadratic.md)

- No.1 Give GC3 malloc objects a new explicit allocation-origin bit [DENIED]
- DENIED — Adding `PY_FLAG_GC_MALLOC_ALLOC` to GC3 fallback/oldified objects made the C

## [Investigation: backend #4 intermittent exit-time SIGSEGV — stale list item pointer under instance churn](../investigations/gc-backend4-churn-exit-list-item-uaf.md)

- + pins around py_dict_set [DENIED — still fails;
- No.4 Pin/unpin owned dict-literal temps across py_dict_set [DENIED — pin writes flags through possibly-forwarded pointers; re-broke churn_bare and did not fix the bootstrap gate; removed]
- No.7 Deferred-destroy IMPLEMENTED both tiers (PccGcZPage.pending_forwardings + zombie; port page grows 96->112 with offsets 96/104; install_forwarding increments via owner index gated on ZPAGE_ALLOC; forwarding_remove/remove_target decrement via addr-span lookup; zpage_remove defers recycle while count>0, zombie pages stay addr-lookupable and destroy at last decrement) [DENIED as cure — churn-600
- No.8 Header-clobber residency bug FOUND+FIXED both tiers [real bug, also DENIED as cure]: relocate_copy's header memcpy/memmove overwrites `to`'s flags with `from`'s, losing `to`'s true ZPAGE_ALLOC/MINOR_ARENA bits (chained relocations then undercount pending_forwardings; reverse direction mis-frees/leaks). Fixed by capturing to-residency bits before the copy and restoring after.

## [Investigation: Backend 4 relocation lacks a mutator-quiescent payload phase](../investigations/gc-backend4-relocation-mutator-quiescence.md)

- The pre-lock preparation portion of the audit was denied as unsafe on the
- The requested dynamic RED was denied honestly: before implementation, both
- The first revision design was `[DENIED]`: restarting from the head after every
- surviving scheduler roots become old. Pointer replacement was also a denied
- The first strict probe's managed `object_is_known` tail was `[DENIED]`: stage
- The raw-byte-offset part of Proposal No.2 was `[DENIED]` before implementation.
- high-level contender operation was `[DENIED]` as a cross-runtime lock oracle
- owners permanently rooted. Both shapes are `[DENIED]`.
- `[DENIED]` The transaction did not lose the owner or child and did not skip an
- free old raw bases and any denied replacement allocation only after unlock,
- `[DENIED]` Draining the young insert edge before rehash is not a valid fix. It
- preserve. `[DENIED]` The historical "two enqueue" assertion is not the right
- `[DENIED]` Candidate-identity revalidation is not correct for `list.remove`.
- recorded above rather than the denied candidate-identity rule. The removed
- `[DENIED]` A dynamic C-extension deallocator cannot currently serve as the
- mirroring only the C tag guard was tested and denied. That independent C-API
- The C-extension probe correction and denied tag-only fix remain recorded above
- `[DENIED]` Wrapping the working list in a new LIFO temp root across custom
- DENIED Proposal No.12c tag admission before allocator support — Function has one allocation owner and iterator has two, but the strict GC4
- DENIED Proposal No.12d suspended-execution tag admission — GEN/COROUTINE/CONTINUATION/TASK formal constructors all pass in strict GC4
- DENIED py_obj_sorted mutable-list pin proposal — Pinning input/output/iterator/scratch once per sort looked like an O(1) way to
- Update — 2026-08-24 prior DENIED attribution overturned
- valid failure evidence but its attribution to mutable-list pinning is denied.
- DENIED ordinary movable comparison callback proof — Three controls show why no dynamic ordinary-instance comparison callback is
- `[DENIED]` shapes were not retried: fake C-extension `tp_hash` and ordinary
- `[DENIED]` for this slice: the strict `#if PCC_PROBE_STRICT` carve-out drafted

## [GC4 retained zpage free fallback and malloc-origin fast path](../investigations/gc-backend4-retained-zpage-free-fallback.md)

- Denied. The worker abort above shows the runtime can later see an address

## [Investigation: gc3/gc4 stage2 ~10-14x slower than gc0 — per-frame index-entry malloc](../investigations/gc-frame-index-entry-pool-perf.md)

- No.1 LIFO shadow-stack to drop the per-frame `frame_index` hash [DENIED]
- DENIED — Regressed gc3 from ~226s to a **900s timeout**. Frames are entered/left at
- Regressed gc3 from ~226s to a **900s timeout**. Frames are entered/left at
- frame-root code (same family as the DENIED No.1 — a wrong change = leak/UAF) and
- from removing the hash (No.1, which regressed and was reverted). Correctness was
- wire stays DENIED), (c) No.3 frame-NODE pool — deliberate, full 5-backend
- Update (2026-06-10): gc4 codegen-window self-sample capture — index maintenance dominates; read-barrier-as-primary DENIED — Capture: stage2-only gc4 probe reusing the fresh matrix `pcc1`
- hypothesis is DENIED as the primary cost; the outlier is per-alloc/track
- Update (2026-06-12 #8): initial-capacity split DENIED by measurement; replaced with tombstone deletion — The initial-capacity split was implemented and passed correctness gates, but
- measurement denied it as a performance slice:
- added memory overhead. The experiment is DENIED and the code was replaced with

## [Investigation: Task completion must release waiter roots](../investigations/gc-task-completion-waiter-release.md)

- No.1 Clear task waiter on completion [DENIED]
- DENIED — This was necessary but incomplete. The task no longer retained the waiter, but

## [Investigation: task waiter cycles survive colored relocation](../investigations/gc-task-waiter-forwarded-shadow-weakref.md)

- No.1 Resolve old slot values in `pcc_gc_store_ptr` [DENIED]
- No.2 Resolve and pin weakref targets only [DENIED]
- DENIED — The task-completion probe still failed with backend 4 printing `4:1:0`. The
- DENIED — The C and pcc-Python task-completion probes still failed with backend 4 printing

## [Investigation: GC0 bootstrap runtime hot paths](../investigations/gc0-bootstrap-runtime-hotpaths-2026-06-05.md)

- No.3 four-byte `_strs_eq` prefix rejection [DENIED]
- No.6 AST wire reuse between export/codegen workers [DENIED]
- No.7 AST wire reuse after native JSON escape fix [DENIED]
- DENIED — Extending the prefix rejection from two bytes to four bytes preserved semantics
- but regressed the focused GC0 profile:
- max-active=3/jobs=8: GC4 stage2+stage3 regressed to 226833 ms.
- from this flamegraph, but the first implementation was denied by bootstrap
- rewrites are denied by this flamegraph.
- DENIED — The measured bottleneck includes duplicated codegen-worker parse/lift, so I
- DENIED — After fixing native JSON string escapes, control-character dumping, and
- export and worker phases that the total wall time regressed. The feature is
- GC4 stage2 AST-wire denied experiment, followed by reverted stage2 barrier pass
- denied as a performance optimization even after its JSON correctness blockers
- full matrix under 200s. An export-wire list fast path was denied by a pcc2 ->

## [Investigation: GC1 bootstrap auto-step backend fast path](../investigations/gc1-bootstrap-auto-step-backend-fastpath-2026-06-05.md)

- automatic sweep of existing candidates was denied because it can reclaim
- frame-index shortcut was denied after causing GC3 bootstrap timeout.
- No.1 make allocation-time auto-step sweep existing candidates [DENIED]
- DENIED — This was already rejected in

## [Investigation: GC2 bootstrap pcc-Python object backend fast path](../investigations/gc2-bootstrap-py-obj-backend-fastpath-2026-06-05.md)

- No.1 optimize CMS worker startup / queue / tracing step [DENIED]
- DENIED — The GC2 bootstrap sample did not show CMS worker/step cost:
- Do not revive the previously denied LIFO frame-index replacement from

## [Investigation: GC3 bootstrap pcc-Python GC dispatch fast path](../investigations/gc3-bootstrap-config-fastpath-2026-06-05.md)

- No.1 remove/rework frame-index tracking [DENIED]
- DENIED — `docs/investigations/gc-frame-index-entry-pool-perf.md` already rejected the

## [Investigation: GC3 cpy-handle oldification loses foreign ownership](../investigations/gc3-cpy-handle-oldify-foreign-ownership.md)

- The initial spelling `py_cpy_handle_move_owned_ref` is [DENIED]. Strict

## [Investigation: GC4 bootstrap pcc-Python GC config fast path](../investigations/gc4-bootstrap-config-fastpath-2026-06-05.md)

- No.1 optimize worker startup / shell wrapper [DENIED for current GC4 hotspot]
- No.4 AST-wire reuse for repeated parse/lift [DENIED]
- DENIED for current GC4 hotspot — The delayed sample and profile do not support worker startup as the main cost.
- DENIED — The flamegraph made duplicated parse/lift look like a high-leverage target, so
- DENIED — The flamegraph showed `_native_export_from_wire` in each worker, so a fast path
- bootstrap gate denied the change:

## [Investigation: backend #4 zpage bootstrap hot paths](../investigations/gc4-zpage-bootstrap-hotpaths-2026-06-02.md)

- No.1 zpage offset-24 double-link cycle [DENIED]
- No.5 index payload span removal immediately [DENIED for current hotspot]
- DENIED — The zpage node layout in the pcc-Python runtime is not the same as the other
- DENIED for current hotspot — Static review is correct that `_backend4_zpage_remove_payload_spans(owner)`

## [Granule span lookup radix](../investigations/granule-span-lookup-radix.md)

- hash map and record `[DENIED]`; never weaken provenance to obtain the number.

## [Investigation: Harness agent loop exposes inconsistent self stack-map state at err.exit](../investigations/harness-agent-loop-self-stackmap-err-exit-join.md)

- No.1 Reduce the generated control flow and repair the owning root-state transition [DENIED — current source already owns and cleans the transition]

## [Investigation: Harness GUI needs a pcc1 that matches the current runtime import policy](../investigations/harness-gui-current-source-pcc1-runtime-policy.md)

- No.1 Reuse the host-compiler runtime archive with the 2026-08-08 pcc1 [DENIED]
- No.2 Remove the current type-scaffold import from GC telemetry [DENIED]
- DENIED — This can produce a linked Harness executable but leaves CPython-hosted pcc as
- DENIED — `i64` is a compile-time annotation marker already supported by current source.

## [Investigation: in-flight 32bfed70 relocation-rework regressions (pcc-Python port relocation + backend-#4 collect)](../investigations/in-flight-32bfed70-relocation-rework-regressions.md)

- Several previously-**resolved** relocation behaviors regressed. They cluster
- pass, so only the relocation-specific port functions regressed. These exact
- `python-substrate-spike-test-sweep.md`), i.e. they worked and regressed.
- backend-#4 explicit-collect reachability that regressed.

## [Investigation: LLVM bootstrap structured-format auto-index ABI mismatch](../investigations/llvm-bootstrap-format-structured-spec-auto-idx-abi.md)

- No.1 Regenerate the static L1 method table unchanged [DENIED]
- No.3 Change format semantics to keep `auto_idx` unboxed [DENIED]
- DENIED — `_emit_structured_spec_obj` is not present in the generated static table. The
- DENIED — The project contract defines ordinary Python `int` as a semantic type with a

## [Investigation: select a real pcc-native extension canary by generic gap cost](../investigations/m1-real-extension-canary-selection.md)

- This is a selection/design gap rather than a regressed behavior. The proposed

## [Investigation: import the real simplejson pcc-native extension and cross behavior](../investigations/m1-simplejson-pcc-native-import-behavior.md)

- and regressed independently. With those fixes in place, the real installed

## [Investigation: strict-self synthetic PEP 489 extension loses C-API exports to dead stripping](../investigations/m1-synthetic-pep489-capi-symbol-dead-strip.md)

- No.1 Preserve the whole generic C-API shim when native extensions are loaded [DENIED]
- DENIED — The source substitution was compiled with `--no-cache --verbose`. Its final

## [Investigation: NumPy L4 import under pcc1/self/no-libpython](../investigations/m2-numpy-l4-import.md)

- dynamic names and zero CPython fallback calls. The first complete build denied

## [Investigation: iterable min/max silently converts dynamic floats to integers](../investigations/minmax-dynamic-float-integer-fold.md)

- Do not repeat the denied attempts to change runtime custom-__lt__ dispatch in

## [Investigation: native file open returns NULL without an exception](../investigations/native-file-open-null-without-exception.md)

- No.1 Initialize the with-open GC root slot at function entry [DENIED]
- DENIED — This is required root hygiene once the open call can branch around the binding
- store, but it is not the cause of the observed NULL return. After this change

## [Investigation: native fragment PCO label publication adds compute work](../investigations/native-fragment-pco-label-publication-regression.md)

- denied scalar-wrapper migrations and required full-record ABI discipline;

## [Native provider closure scan reparses the export AST](../investigations/native-provider-closure-scan-reparses-export-ast.md)

- Denied alternatives — Accept the second parse: violates a tested frontend performance contract.

## [Investigation: native re.compile pattern OBJECT (replace literal-alias rewriting; numpy `.cpy.attr.compile`~71)](../investigations/native-re-compiled-pattern-object.md)

- Update (2026-06-10): phase (i) as originally designed is DENIED by engine-fidelity inspection; dependency order revised — Code inspection of `pcc/py_runtime/src/py_re.c` (336 lines) shows the
- The original No.1 phase (i) is therefore **DENIED as designed**.

## [Investigation: native subprocess provider omitted from shallow multi-file closure](../investigations/native-subprocess-provider-omitted-from-shallow-multi-file-closure.md)

- No.3 Restore a generic C exception for shallow multi-file builds [DENIED]
- DENIED — `recursive_stdlib=False` is the documented shallow multi-file contract.
- DENIED — That would remove the `CalledProcessError` fields fixed by the predecessor

## [Investigation Report: `nbody_shootout` Was Not "Just Missing Vectorization"](../investigations/nbody-shootout-fp-contract-and-vectorization.md)

- Experiments That Ruled Out Easy Explanations

## [Investigation: numpy closure widened by dyn-flow references runtime symbols the pcc1 self-backend link cannot resolve](../investigations/numpy-dyn-reachability-selfbackend-link-gap.md)

- helper" is DENIED as the sole trigger. Remaining discriminating variables

## [Investigation: pcc1 stage2 times out inside `codegen[pcc.py_frontend.codegen.layer1]`](../investigations/pcc-bootstrap-stage2-layer1-codegen-timeout.md)

- `[DENIED]` with the new sampling evidence and move on to

## [Investigation: compiler-design references for faster, exact, maintainable PCC](../investigations/pcc-compiler-design-reference-audit.md)

- Requires allocation and sampling profiles. It is denied if a benchmark shows

## [Investigation: transfer mold's packed and frozen-pass linker design into pcc's owned Mach-O path](../investigations/pcc-macho-packed-linker-mold-design-transfer.md)

- No.3 Global symbol-name interning and integer-only downstream identity [DENIED]
- No.5 Read-only mmap input lifetime [DENIED — incomplete]
- unchanged. This is not the denied trusted-provenance shortcut: disk/cache
- DENIED — Both key modes passed 54 focused tests; integer IDs passed the complete 121
- DENIED — incomplete — The candidate passed 126 focused tests, including mapping cleanup and full

## [Investigation: connect pcc-Python Thread objects to the owned pthread kernel](../investigations/pcc-python-thread-object-pthread-bridge.md)

- Update — compiler transfer denied; bridge source removed — The runtime bridge passed all focused gates, but its intended compiler consumer

## [Investigation: pcc1 exact compiler-arena field projection loses the append4 method ABI](../investigations/pcc1-exact-compiler-arena-field-method-abi.md)

- No.1 Break the parser dependency cycle and annotate the exact seed receiver [DENIED]
- No.2 Add exact class-level types for every base/seed slot [DENIED]
- DENIED — The 426-test focused packet and four pcc1 module closures passed. Source-frozen
- DENIED — The 427-test packet and four pcc1 closures passed. Source-frozen v71 Stage1
- No.1 and No.2 remain historically denied on their original source because both

## [Investigation: pcc1 exact-string concat chains allocate one object per edge](../investigations/pcc1-exact-str-concat-chain-object-tax.md)

- replacing a five-part chain with `"".join([parts])` is `[DENIED]`: the
- Stage2 is no slower than the non-regressed Stage1 target;
- DENIED — The candidate was correct on its focused GC0 boundary, built a source-frozen
- [`020-no99-exact-str-chain-denied.md`](../goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/020-no99-exact-str-chain-denied.md).

## [Investigation: pcc1 traps while scaling finite float literals through raw integers](../investigations/pcc1-float-literal-bignum-scale-raw-int-trap.md)

- No.2 Reduce the exact-integer threshold and retain repeated float scaling [DENIED]
- DENIED — The old repeated-scaling path already produced measurable decimal-to-binary64 errors such as `1e100 -> 1.0000000000000006e+100`. Avoiding a compiler trap by restoring known incorrect literal semantics would violate the existing precision oracle.

## [`2**52` does not round-trip through pcc1's float formatting or parsing](../investigations/pcc1-float-repr-strtod-17-digit-defect.md)

- [DENIED] Fixing only the digit-count search — Applied exactly the "smallest viable change" proposed above — compute the
- [DENIED] fix 1 exact digit count only -> output got worse, and revealed
- [DENIED] fix 2 full exact assembly -> buffer read underflow; the

## [Investigation: pcc1 rejects an admitted read-only GC query while rebuilding its runtime](../investigations/pcc1-freestanding-readonly-gc-registry-static-export.md)

- No.2 Add `pcc_gc_relocation_set_size` as an unconditional verifier exception [DENIED]
- DENIED — That would create a second allowlist and admit the symbol independently of its exact source signature. The finite owner-module registry plus static function export preserves the existing fail-closed checks and fixes the self-hosted representation issue instead.

## [Investigation: pcc1 frontend rebuilds text that the self backend parses back into the same indexed kernel](../investigations/pcc1-frontend-direct-indexed-kernel-plane.md)

- than the non-regressed Stage1, followed by sequential pcc2/pcc3 fixed point;
- An eager call-record publication sub-experiment is separately denied. It
- text-reparsing capture implementation is denied as a performance candidate.
- non-regressed-v9 denominator gives 2.179x; the terminal target is still open.
- non-regressed v9 Stage1 199.40s.
- against the non-regressed 199.40s denominator it is 1.829x. The remaining
- The five-module shape is `[DENIED]` against its pre-registered <=1.5x RSS
- Result — streamed native recycling `[DENIED]` — The ownership/policy packet passed 104/104 and both changed modules passed
- AST ownership. The candidate misses both <=1.5x and <=2GB lines and is denied;
- finalize fast path, which sizing denied, and it does not change unsupported
- Result — fixed-shape/lazy metadata `[DENIED]` — Focused direct/contextual/debug gates passed 11/11 and the adjacent Stage1/
- for making the object cheaper: pcc1 CPU regressed and footprint improved only
- Result — recordless direct order `[DENIED]` — The implementation deleted the hot `InstructionRecord` and empty text entries
- plane is denied; a future design must publish final order directly into one
- [`030-recordless-direct-order-plane-denied.md`](../goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/030-recordless-direct-order-plane-denied.md).
- already-final `IndexedCallPlane`. This is not the denied signature-cache
- Final result — retained structure, whole-Stage2 performance `[DENIED]` — The scalar adapter covers 158,356 of 169,890 module1 calls (93.2%). Its
- Thus the complete dense-ID plus fixed-call line is `[DENIED]` as a whole-Stage2
- Update — v15 full-cost emit window selects a native final-order plane `[DENIED]` — The memory-safe Stage2 work restored host Stage1 to 149.15 seconds but its
- The old recordless-order candidate remains denied: its Python `list[int]`
- Successor proposal — canonical native order, not a mirror `[DENIED]` — The complete denied recordless source remains at
- The complete denied recordless source remains at
- `[DENIED]` at its host prefilter, before a pcc1 build; this avoids repeating

## [Investigation: generated type-tag alias loads crash current pcc1 during frontend module initialization](../investigations/pcc1-generated-type-tag-alias-module-init-pin.md)

- No.2 Disable generated aliases in self-hosted frontend modules [DENIED]
- DENIED — Restoring handwritten tags or conditional self-host behavior would recreate a second ABI source of truth and make the native compiler differ from the host compiler. The generated aliases are valid compiler inputs; only their module-initialization representation must be static.

## [Investigation: L1CodeGen mixin state outside the host contract aliases host slots under pcc1](../investigations/pcc1-host-mixin-state-outside-contract-aliases-slots.md)

- [DENIED] treating the KeyError as a subscript-fix regression — The raising subscript only exposed the corruption; reverting it would restore

## [Investigation: migrate pcc1's compiler hot path to an Indexed Function Kernel](../investigations/pcc1-indexed-function-kernel-native-data-plane.md)

- early/late caller flamegraphs, and the denied No.78 substitution. The new
- No.2 Pass-local wrappers or one-view removal [DENIED BY PREDECESSOR]
- No.3 Global managed-pointer/refcount bypass [DENIED BY PREDECESSOR]
- DENIED BY PREDECESSOR — The prior private cursor removed seven generator/view projections, kept
- DENIED BY PREDECESSOR — The managed-refcount ABI dereferenced raw C pointers stored through Dyn and
- is `[DENIED]` for kernel scalar tables; they were restored to the v2 projection.
- Update 2026-08-28 — per-field packed raw arena is denied — The first parser-to-emitter packed proposal stored opcode, tags, payload IDs,
- This proposal is `[DENIED]`. It repeats the earlier per-scalar-arena mistake at
- remains `[DENIED]`; the intrinsic is retained only as the foundation for
- Update 2026-08-28 — dense slots reach zero object projection; wrapper ABI denied — The supported AArch64 path now materializes zero SlotInfo and zero AllocaInfo
- The wrapper ABI is `[DENIED]` as a final hot interface, not the dense table.
- `[DENIED]` as the final call-operand interface. The next slice must batch
- Update 2026-08-28 — call-site batch denied; block dictionaries removed — The fixed-width call-site experiment used the confirmed `load_i64x4`
- `[DENIED]` and its source was removed. The operand population is simply too
- kind-specific graph rather than repeating the denied tagged-record or
- The naive interface was decisively wrong: nested named slot helpers regressed
- lists and 209,361 payload tuples without repeating the denied generic decoder.
- The first memory explanation was `[DENIED]`. A single v12 compiler with old
- the already-denied per-scalar interface. With a similarly scalarized packed
- regressed item311 to 447.856B / 34.84s; both forms were removed. v15 separately
- `[DENIED]` before pcc1 rebuild: controlled host instructions rose 95.086M ->
- and its Stage1 instructions improved, but item311 regressed to 445.783B /
- s while saving only another 0.5% footprint. It is `[DENIED]` and removed.
- This is nevertheless `[DENIED]`. Source-frozen strict pcc1 v22
- therefore not the transition-replay shape denied as historical No.78.
- It is still `[DENIED]`. Source-frozen v25, against the same runtime/build
- Update 2026-08-28 — fixed direct publication is structurally right but denied on pcc1 — Extending the parser-owned final plane to load/store/cast/icmp/binop/select
- Source-frozen pcc1 denied it. With an identical current 186-object runtime and
- the proposal is `[DENIED]` and removed. Source is byte-identical to the
- on host. Its first prefix handoff was nevertheless `[DENIED]`: packing the
- `[CONFIRMED]`; the packed-prefix variant remains explicitly denied.
- Performance has not converged. Per-record use bookkeeping was denied at
- Removed `[DENIED]` shapes: payload-only construction (430.5B), base final-mode
- Update 2026-08-29 — construct-then-handoff terminator/PHI arenas are denied — Candidate v61 closes the pending current-source pcc1 receipt: exact item311
- `[DENIED]`: the shape still allocated every ordinary terminator/PHI object and
- retention improved while execution regressed 0.91% versus v61. It was removed
- worse in instructions than v63. `[DENIED]`; both variants were removed and
- `prepare begin` (exit 139), before any item311 measurement. `[DENIED]` and
- Three interface remedies are `[DENIED]`: split base/final publication v68
- regressed to 434.749B; pointer-bearing valueclass publication v69 made Stage1
- Update 2026-08-29 — direct terminator parsing is denied on pcc1 — Source-frozen v74 completes Stage1 and passes the 114-byte canary, but the
- exact-field ABI repair. `[DENIED]`; restore accepted v73 narrowly. Do not
- `[DENIED]`. The changed stackprep path replaced v63's deliberately retained
- lookups. V77 is `[DENIED]` as built. V78 adds only
- `py_incref_managed` design denied earlier. Even impossible deletion of the
- wall by at least 1.25x, keep aggregate CPU/instructions non-regressed, and keep
- Width-12 result `[DENIED]` — The first pair is decisive and the early-stop line applies. Both arms used
- non-regressed-compute requirement. Width 10/11 cannot supply the missing
- The zoomed-out unsplit hypothesis is also denied by sizing. The complete
- V80 result `[DENIED]`; eager diagnostic spelling identified — V80 removed `_Definition` and its bucket graph, passed 19 verifier/inventory
- Update 2026-08-29 — shared definition facts win; local and bit planes denied — V81 delayed diagnostic spelling but still rebuilt a verifier-local arena. Its
- versus V79 385.292B / 31.58 / 32.39 / 3.105GB. `[DENIED]` and removed.
- signals non-regressed and exact assembly. `[CONFIRMED]` as a roughly 1.9% small
- V85's packed provenance/liveness plane is nevertheless `[DENIED]` on
- V106-V109 whole-boundary result `[DENIED; removed]` — The focused path result did not survive the complete owner boundary. The
- across iterations. It regressed Stage1 instructions by 20.7% and wall by
- another Stage1 build without a cheap summary-equivalence proof are denied.
- Dense semantic-summary plane `[DENIED; removed]` — The next candidate tested that proposal without stacking another file-handoff
- reviving the denied text/path adapters. After export metadata converges, the
- Short-lived summary workers `[DENIED; removed]` — The single permitted Stage1 succeeded and materially reduced host work rather
- denied JSON writer inside vthread analysis is forbidden.
- Line-codec worker integration `[DENIED; removed]` — The proven line codec was connected through thin adapters to the previously
- Stage1 succeeded but its wall/CPU envelope regressed despite stable
- discovery is denied.
- This is not worker reuse (which remains denied), not a codegen widening, and

## [Investigation: inline post-call error edges instead of one CFG triplet per call](../investigations/pcc1-inline-error-edge-data-plane.md)

- [DENIED] as a performance solution: shared cleanup dispatcher without measurement — Not implemented: sharing cleanup exits needs the released/unpinned operands in
- [DENIED] lazily reading source files at traceback print time — Considered to avoid the source-text table: it changes output whenever the
- [DENIED] `import traceback` / `pcc.extern` in a frontend module for the frame-trail diagnostic — Either import adds a module to the stage1 closure (`test_direct_publication_uses_exact_static_abi_in_stage1_context`

## [Investigation: pcc1 passes aggregate index zero as a NULL scaffold handle](../investigations/pcc1-ir-scaffold-extract-value-zero-index-null-handle.md)

- No.2 Special-case zero inside `IRBuilder.extract_value` [DENIED]
- DENIED — `NULL` is an invalid Python argument and cannot distinguish an accidentally erased object from the integer zero. Treating it as lane zero would hide the cross-module ABI mismatch and leave every nonzero integer operand encoded as an invalid raw pointer.

## [Investigation: native arena projection lost after field discovery expansion](../investigations/pcc1-native-arena-projection-loss-after-field-discovery.md)

- No.2 Raise resource limits or repeat full Stage2 [DENIED].
- DENIED — The native A/B is roughly3x instruction/CPU work and substantially higher memory

## [Investigation: pcc1 native JSON string escapes](../investigations/pcc1-native-json-string-escapes.md)

- preserved GC4 `pcc2`/`pcc3` byte identity but regressed focused GC4 wall time,

## [Where pcc1 native code beats CPython and where it loses, measured per operation](../investigations/pcc1-native-vs-cpython-per-operation-cost.md)

- [DENIED] Reusing an already-loaded root value to skip one reload — `_leave_container_temp_root` re-reads the slot to get the value it unpins, and

## [Investigation: pcc1 loses an owned object across an IfExpr local assignment](../investigations/pcc1-owned-ifexpr-local-transfer.md)

- alloca-keyed roots/owned flags and records denied runtime/GC guesses;
- No.1 Rewrite the tuple-isinstance caller to avoid IfExpr [DENIED]
- DENIED — This hides one pcc1 symptom while leaving ordinary user conditional
- No.2 landed; No.1 remains denied. The fix is an ownership-aware CFG join, not

## [Investigation: pcc1 pip-numpy chain regressed at HEAD — default acquire mode fails closed, and the compiled numpy app breaks at import on C-API shim holes](../investigations/pcc1-pip-numpy-runtime-import-capi-regressions.md)

- Investigation: pcc1 pip-numpy chain regressed at HEAD — default acquire mode fails closed, and the compiled numpy app breaks at import on C-API shim holes

## [Investigation: pcc1 pproxy GC4 live proxy readuntil returns empty](../investigations/pcc1-pproxy-gc4-live-proxy-readuntil-empty.md)

- No.1 `StreamReader.readuntil` / `_fill_once` loses already-rolled-back data [DENIED]
- No.2 pproxy monkey-patched `rollback` via `_buffer.__setitem__(slice(...), s)` corrupts buffer state [DENIED]
- DENIED — Standalone `StreamReader` reductions with both `feed_data(...)` and a real
- DENIED — The reduced pproxy-style monkey patch with

## [Investigation: pcc1 startup fails after raw GC globals enter runtime ABI type registry](../investigations/pcc1-runtime-abi-gc-global-type-registry-startup.md)

- No.1 Separate raw GC storage kinds from managed runtime LLVM type objects [DENIED as startup fix; retained]
- No.2 Disable the stage publish/exec-smoke barrier [DENIED]
- No.3 Patch `_SingletonType` inheritance without isolating the triggering registry [DENIED]
- DENIED as the startup fix; retained as the raw-storage contract — The exact source-contract tests passed, but a fresh v2 stage1 still failed its
- DENIED — The barrier correctly caught a deterministic startup regression after the
- DENIED — The singleton implementation predates this regression and existing compiler

## [Investigation: pcc1 self-host loses `_generator_ctx` after first assignment](../investigations/pcc1-self-host-generator-ctx-slot.md)

- No.1 disable yield-sentinel cache to rule out cache corruption [DENIED]
- No.2 add `self._generator_ctx = None` slot in `__init__` [DENIED]
- DENIED — Even with the cache short-circuited, pcc1 still fails with the same
- DENIED — After rebuilding stage0→pcc1 with the slot pre-allocated, inserting a
- init slot didn't help. Matches the "default-None slot, setattr clobbers
- Pros vs. denied alternatives:**
- exposure surface enough that user-visible failure regressed between then

## [Investigation: self-hosted pcc1 SIGBUSes at startup in the compiled-module init registry](../investigations/pcc1-self-host-module-init-startup-sigbus.md)

- are all DENIED; the "garbage init_fn" evidence they rest on was a misread
- `py_compiled_module_register_init` [DENIED — the init_fn values were never
- modules of the pcc closure [DENIED — materialization is correct]
- corruption class of the bit-42 investigation [DENIED as a cause — the
- Correction: the "garbage init_fn" evidence was a misread dump [DENIED] — The earlier round's headline finding — "210 `register_init` calls receive

## [Investigation: the Stage2 small lane's in-process native-object path is quadratic under pcc1](../investigations/pcc1-small-lane-native-object-worker-memory.md)

- all 193 modules at width <= 4 with floor admission (1712 denied polls, 3
- Update 2026-09-04 — where pcc1's time goes (v8 worker + coordinator), two experiments denied — Human decision: 1350 s is not acceptable; the contract is Stage2 <= Stage1 on
- Experiment: GC0-static barrier layer `[DENIED as designed]` — A knob that turned every frontend-emitted `pcc_gc_load_ptr` into a plain
- Experiment: GC0 cycle-collector frequency `[DENIED]` — `PCC_GC_DEBT_THRESHOLD=4294967296` versus default on the same v8 pcc1:
- Update 2026-09-04 — native-export JSON slicing `[DENIED; removed]` — The current real `py_ast` PCO worker confirms that repeated export projection
- compiled/ran at 198MB with output 42. The real boundary denied it: two frozen
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/033-native-export-slice-denied-stack-overflow.md`.
- Update 2026-09-04 — explicit frontend-cycle teardown `[DENIED; removed]` — The next candidate attacked the roughly 20% GC0 mark/subtract share without
- removed collector work. `[DENIED]`; production code was forward-restored and
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/034-pre-emit-cycle-teardown-denied.md`.
- Update 2026-09-04 — owned JSON export projection `[DENIED; removed]` — Reusing `json.loads` dictionaries while restoring tuple-valued ABI fields
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/042-owned-export-projection-denied.md`.
- contextual L1CodeGen host schema. It does not recreate the denied 224-shard +

## [Investigation: `pcc1 -> pcc2` emit throughput and native-worker memory](../investigations/pcc1-stage2-emit-throughput-and-memory.md)

- `PCC_PYTHON_IR_PASSES=off` default is not the cause.
- Who sent SIGKILL is unknown.** Ruled out: the system OOM killer (96 GB machine,
- and both **DENIED** by that probe: it is not memory accumulated across the four
- Did not help — recorded so it is not retried — Three further keyword-only signatures were made positional
- DENIED** by reading the real lowered IR:
- The `py_func_call_kwargs` frame is unidentified.** See the DENIED list
- Update — No.15 DENIED: host-CPython profiling is the wrong cost model for pcc1 — Attempted the largest remaining item the host profile named, and it was a large
- Hypothesis 1 — DENIED: "allocation beats loop iterations under pcc1" — That was the conclusion recorded in No.15. A microbenchmark of the two exact
- Hypothesis 2 — DENIED: "per-allocation cost scales with the live heap" — Plausible, because the real emit runs with ~10 GB live and a huge
- Denied along the way — Each was disproved with a seconds-long pcc1 probe rather than a rebuild, and
- `[DENIED]` `isinstance(block.phis, list)` mis-answering under pcc1 (a
- `[DENIED]` `@dataclass(slots=True)` field reassignment failing under pcc1.
- `[DENIED]` pcc1 losing exception messages, i.e. the empty message being a
- `[DENIED]` `dict.get()` mis-lowering under pcc1 (also a recorded defect).
- `[DENIED]` the shared `_EMPTY_SEQUENCE` for `ParsedBlock.phis` plus its
- `[DENIED]` A user-level minimal reproducer does **not** reproduce: returning a
- `[DENIED]` Carrying the link as an attribute on the IR value (which would
- Every `[DENIED]` verdict below is withdrawn as unsupported.** They compared a
- `[DENIED]` **Object projection alongside the i64 phi in `int(<dyn>)`.** Each
- `[DENIED]` **Recovering the source object inside `marshal_to_object`.** Isolated
- `[DENIED]` Carrying the unbox link as an attribute on the IR value instead of an
- Update No.26 -- pcc1 is 5.5x slower than CPython on the same work; one point-fix measured NEGATIVE [DENIED] — Same module (`pcc/parse/py_lift.py`, 1303 lines), same flags:
- `[DENIED]` Caching the callee signature text — `_irbuilder_call_from_args_list` rebuilt each callee's signature text on every
- Update No.32 — flame graphs land; Update No.31's hypothesis is [DENIED] — `scripts/pcc_flamegraph.py` produces both graphs from an Apple tool's call
- Update No.31 is [DENIED].** It predicted `find_slot` would be attributed to a
- Update No.33 — the GC tax quantified, and a [DENIED] verdict whose mechanism is now gone — Both flame graphs now use the same estimator (blocked frames excluded on both
- Two optimisations were measured and **[DENIED]** on the way here, both aimed at
- This is where a recorded [DENIED] verdict becomes live again.** A callee
- denied with measurements. The highest-value lead is a previously refuted change
- Re-trying a change this file records as **[DENIED]**, with the evidence that
- The change this file recorded as **[DENIED]** now measures a win, because the
- Update No.37 — two more emitter caches [DENIED], and the rule that predicts it — Baseline pcc1 (module-try fix + signature cache): **53.1 s** first, and **53.9 s**
- also making _replace_record_text O(1) instead of a linear scan) 55.6 s -3.2% DENIED
- was re-stringified once per emitted instruction) 55.1 s -2.2% DENIED
- Both [DENIED] with measurements and reverted. The signature cache (1.13x) stands
- Update No.42 — fixed-arity IRBuilder cache hits are [DENIED] — The candidate moved declared-callee cache hits for `IRBuilder.call0/call1/call2`
- same-source baseline; another wrapper/dispatcher split is the same denied
- `[DENIED]` and reverted. Cache correctness and the generic no-three-item-list
- Update No.43 — the direct-mapped MRO method cache is [DENIED] — The 1024-entry `(class address, name address) -> (MRO index, method index)`
- `[DENIED]` and reverted. Semantics and independent field-cache correctness
- Update No.44 — the mutation-safe per-Module signature cache is [DENIED] — After the MRO cache denial, the next task-board boundary was measured before
- fixed-arity renderer denied in Update No.42.
- Module dict and no process global. It must replace, not stack on, the denied
- `[DENIED]` as the production shape. The source was restored after building
- Update No.45 — exact Function-slot call rendering is [DENIED] — The final signature-render proposal removed memoization and used an exact
- `[DENIED]` as a stage performance optimization. Correct exact-Function versus
- `while + arena[index]` rewrite is pre-denied because it still allocates a view
- Update No.47 — the reusable precise-stackmap instruction cursor is [DENIED] — The Update No.46 profile justified one fail-first implementation: keep the
- `[DENIED]` and reverted. Do not retry generator/view removal alone. The next
- `[DENIED]` for stage2 and was not implemented in production source.
- `[DENIED]` for both a `find_slot`-only edit and a safepoint-record
- `[DENIED]` without a stage rebuild.
- Update No.50 — the `compute_free_names` common/cold split is `[DENIED]` — The proposed split mechanically promoted the stateless helper cluster and
- `[DENIED]` and removed. Do not retry this exact helper-hoist or common/cold
- Update No.51 — medium emit concurrency reduction is `[DENIED]` — The current-source phase-aligned diagnostic produced a valid self/no-libpython
- `[DENIED]`; production remains at eight fresh batch1 workers and no source
- Update No.52 — emit-text lifecycle DENIED from the frozen capture; IR sidecar wire pre-registered — No source change and no new sampling. Two evidence steps, then one
- Step 1 — the emit-text hypothesis is DENIED by leaf mining — The direction under test was: per-instruction `list[str]`/concat/temporary
- DENIED — text restructuring cannot wholesale-remove the tax; per the
- Step 2 — parse-local shapes are pre-denied by measurement, without code — Host probes on the frozen module98 IR (sha `47289e1d0517d365...`, 1,831,588
- Update No.53 — sidecar producer is infeasible: frontend instructions are text-only records [DENIED BY ANALYSIS] — Implementation due diligence on No.52's producer found a structural blocker
- VERDICT: No.52's sidecar proposal is DENIED BY ANALYSIS and was not
- Update No.54 — the provenance predicate was five calls deep; fusing it is [CONFIRMED], a cached reciprocal is [DENIED] — No.53 closed the emit-local route and named the remaining owners as the uniform
- `[DENIED]` Caching a multiply-by-reciprocal in the span descriptor — The fused predicate still divides once (`carve_offset // stride`). A
- combined 12/24 no effect
- Fusion `[CONFIRMED]` and kept. Reciprocal `[DENIED]` and removed. The next
- Update No.57 — class-name compare in one pass `[CONFIRMED]` 1.0200x; first-byte prefilter `[DENIED]` — Update No.56 named MRO method lookup as the next owner: `strs_eq` 5.2% +
- already holds a 2026-08-20 `[DENIED]` for a 1024-entry direct-mapped location
- `[DENIED]` — first-byte prefilter at the walk call sites — Hoist the wanted name's first byte out of both loops and reject a candidate
- `PERF-P1-CLASS-NAME-COMPARE-SINGLE-PASS`; prefilter `[DENIED]` and removed.
- Update No.58 — the native-emit worker profiled; inlining a hot guard `[DENIED]` — Updates No.56 and No.57 were both measured with `--emit-llvm`, which returns
- `[DENIED]` — inlining `pcc_gc_frame_roots_disabled_fast` — All four frame entry points open with `if pcc_gc_frame_roots_disabled_fast()
- Denied (two), both the same shape — first-byte prefilter at the MRO/field walks -0.6%
- The three accepted changes removed work. The two denied changes relocated work
- setup for an uncertain 2-3%, in exactly the shape that had just been denied
- from all denied cache/guard/stackmap-local shapes above: the proposal removes
- the same direction, and peak footprint <=1.02x. If it misses, mark `[DENIED]`
- Update No.65 — disabled memory-pair pass still parses every line `[DENIED]` — After the radix fixed the distributed runtime tax, exact medium ranking moved
- Proposal No.65 `[DENIED]` — Add one `enabled=False` fast path that scans once, updates only barrier depth,
- Update No.68 — disabled pair pass with indexed while `[DENIED]` — After No.67, render fell 13.06% -> 3.79% and the still-unmodified memory-pair
- pass rose to 12.31%. No.65 denied a `for line in lines` barrier-only path at
- Update No.69 — bounded IR passes for compiler modules `[DENIED]` — After No.67, valid cold Stage2 is 686.160 s, 86.160 s above target. Native
- footprint 1.00043x. `[DENIED]`; no default changed and no Stage2 was run.
- Update No.70 — one semantic validation per owned-link boundary `[DENIED; raw final validator retained]` — After No.67, the valid cold Stage2 is 686.160 s and the pcc-owned link driver
- Update No.73 — the restored reload planning is recomputed per safepoint `[DENIED]` — The first correct cold Stage2 completed at **892.439 s** (profile total
- Proposal No.73 `[DENIED BY MEASUREMENT — its premise is false]` — memoize, do not micro-optimize — Do **not** start by packing the sort key into an int. Frame offsets are
- seconds. The proposal is denied on its premise, before a line of it was
- written, and the interning half is denied with it — there is no point interning
- already ruled out by measurement:
- half of the denied No.73, re-justified on its own terms (one frozenset
- Update No.78 — the planner recomputes every frame-protocol transition `[DENIED]` — After closing the linker-local round, the No.75 complete profile restores the
- Proposal No.78 `[DENIED]` — compute transitions once, replay them in emit — Split transition decoding from active-state mutation. `_block_entry_states`
- for the fast first pass was tested and DENIED (cold rerun within 4 s).
- Update No.80 (2026-08-27): wide-cap single wave DENIED on a loaded machine; the env-existence hypothesis DENIED by its own control [both DENIED] — Attempt: scale the admission cap to physical memory in bootstrap.sh
- already compiled and executed the same code. DENIED.
- scan exists. DENIED.
- The old stable-key and scheduler hypotheses stay denied: stable-key work is
- Update No.83 — promote the structural call fallback `[DENIED]` — The accepted No.72 profile still attributed 5.17% to the regex engine and
- denied. Full receipt:
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/002-structural-call-parser-denied.md`.
- Update No.84 — confident scalar-call parser `[DENIED]` — No.83 showed that promoting the full structural fallback moved regex cost into
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/003-scalar-call-fast-path-denied.md`.
- Update No.85 — provenance/raw-span/batch-intrinsic sizing `[DENIED]` — After restoring No.72, the self-time distribution is almost entirely generic
- container-wide managed ABI remains denied.
- denied second decode/copy boundary. No parser source changed.
- per-word raw plane remains denied; any root-state proposal must fuse the full
- No.86 result `[DENIED; removed]` — The candidate reduced the real closure's type-mask calls 1510 -> 433, scanned
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/005-import-scan-cache-denied.md`.
- the denied long-lived parent shape is not reintroduced.
- No.87 result `[DENIED: scan cache; isolation mechanism retained for No.88]` — The frozen candidate contains 1,137 files and differs from accepted No.72 in
- denied in the environment it was meant to optimize: CPython-host sizing and
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/006-closure-worker-cache-denied.md`.
- No.88 result `[DENIED; worker removed]` — The dependency-closure implementation was restored byte-for-byte to accepted
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/007-isolation-only-closure-worker-denied.md`.
- No.90 result `[DENIED; raw projection removed]` — The first frozen build failed before producing pcc1 because a runtime
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/009-raw-i64-span-projection-denied.md`.
- the previously denied pair-pass and transition-cache rewrites covered smaller
- No.91 result `[DENIED; fully removed]` — The exact-three-file v2 candidate built successfully under CPython 3.15.0rc1.
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/010-block-parallel-denied.md`.
- No.92 result `[DENIED; fully removed]` — The implementation changed only the private body handoff and passed 56 focused
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/011-body-line-transfer-denied.md`.
- the arena word-helper route is denied by No.85. No new stack-map cache/fusion
- No.93 result `[DENIED; compiler/runtime candidate removed]` — The explicit-mode v2 receipt is valid. Source-frozen pcc1
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/012-threaded-block-parallel-denied.md`.
- Update No.94 — fork/COW block workers are not an owned pcc-Python boundary `[DENIED by capability audit]` — After shared-heap pthreads failed on their 1.62x atomic/safepoint tax, the only
- reintroducing the dominant 42% parser and the denied object/wire adapters.
- No code or process experiment is justified. This parallel route is `[DENIED]`
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/013-fork-cow-capability-denied.md`.
- already unsafe and denied. The new structural owner is compiler-internal AST
- slice; replacing one `isinstance` branch or adding an id-keyed cache is denied.
- Update No.97 result — dense compiler-AST kind projection `[DENIED]` — The final exact9 candidate is correct on the scoped gates and emits the exact
- footprint is neutral. It is denied before Stage2. The dense-kind path is
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/016-dense-ast-kind-projection-denied.md`.
- adjacent proposals are denied by existing/current evidence: runtime
- Update No.98 result — shared vthread analysis `[DENIED]` — The corrected v2 pcc1 ran both shared and force-unshared worker canaries with
- registered 1.025/0.98 lines and is denied before Stage2. The vthread handoff is
- `docs/goal/evidence/PERF-P0-PROVEN-NATIVE-DATA-PLANE-PARALLEL-EMIT/018-shared-vthread-analysis-denied.md`.
- Per-function post-scan result `[DENIED]` — A bounded attempt converted instruction strings to scalar records after each
- focused tests were exact, but the adjacent pcc1 `py_ast` result regressed:
- Quiescent decoded-module collection `[DENIED]` — The same v56 pcc1 compared an explicit GC-off control with releasing the
- were exact, but CPU regressed 17.99->18.32s and RSS was flat at 1.68GB. More
- Existing summary override under 8 GiB `[DENIED]` — A same-v58 checkpoint requested summary width seven, but the existing API
- Hybrid ASM plus packed stack-map sidecar `[DENIED]` — For `cli_bootstrap`, moving stackmaps out of ASM shrank text 61.1->39.9MB and
- Mixed ASM/PCO scheduler `[DENIED after Stage2 transfer]` — A stable floor-descending mixed replay completed all 227 frozen artifacts in
- Lazy verifier diagnostic projection `[DENIED]` — The architectural zoom-out's raw module-1 sample attributed 49.6% inclusive
- Handwritten packed verifier `[DENIED; mechanism confirmed]` — A raw-arena verifier prototype reduced the retained module-1 verifier caller
- only about 2.4% instructions. The physical implementation is denied: it grew
- Borrowed integer spans across verifier helpers `[DENIED]` — A follow-up kept the ordinary verifier as the sole semantic owner and replaced
- The span transfer itself is denied. On the same retained module-1 sidecar,
- v58 versus v64 produced byte-identical 61,075,757-byte ASM, but wall regressed
- Packed verifier CFG/dominator scratch `[DENIED]` — A second in-place rewrite removed the verifier's CFG dict, predecessor and
- Singleton-first provenance probe `[DENIED]` and native stack-map heapsort `[CONFIRMED]` (2026-09-06, v83/v84) — Hoisting the four immortal-singleton compares ahead of the granule walk in
- floor (fused walk; reciprocal division denied earlier), so the owner is the
- so the runtime cannot trust any dyn operand; the denied
- first) measured +3.2% instructions and is denied.
- Update 2026-09-06 (evening): raw-pointer static typing landed; probe removal DENIED — Phase A (raw addresses typed `int` in application modules, `c_obj`/`c_rawptr`
- GC0..2) is **[DENIED]**: pcc1 crashed in Stage2 (`pcc_allocator_take_small_object`

## [Investigation: with reloads restored, pcc1 rejects its own IR — `self` outlives its active root in 19 functions](../investigations/pcc1-stage2-stale-managed-self-outlives-root.md)

- lets a managed value outlive its root" — is therefore `[DENIED]` as the cause
- liveness against a frame-protocol-bounded active set — is therefore `[DENIED]`
- failure to parse the negative initializer is ruled out.

## [`str.find` was O(n) in its own result conversion — stage2 2.49x](../investigations/pcc1-str-find-quadratic-codepoint-conversion.md)

- Update — reordering the provenance disjunction [DENIED] — Lever 1 was tried in its cheapest form first: `_pointer_is_managed_no_lock`
- No effect.** Reverted in both mirrors. The 6.9% the profile attributes to this
- `_text_lines` and `str(Type)` [DENIED], singleton reordering [DENIED], this one

## [Investigation: backend 0 double-free / refcount underflow under high-scale threaded explicit gc.collect](../investigations/pcc1-threaded-explicit-gc-backend0-double-free-highscale.md)

- Ruled out — Torn refcounts**: `pcc_refcount_incref/decref/load` use `__atomic_*`
- DENIED/deferred: tracing-sweep guard alone (incomplete; see experiment above).

## [pcc1 self-host miscompile: tuple-unpack corrupts codegen `_str_counter`](../investigations/pcc1-tuple-unpack-self-host-str-counter-corruption.md)

- (refuse to relocate a dead/transiently-zero source). No effect on the crash
- `py_decref(child)` no-ops. No effect on the crash ⇒ oldify-frees-source is
- gc4 relocate `refcount<=0` guard in `pcc_gc_relocate_copy_unlocked` — NO effect.
- `_mark_forwarded_source_inactive` — NO effect.
- pages two remaps late instead of one) — NO effect ⇒ the stale reader holds
- ruled out: heap corruption of the backing attr storage, or the failing read

## [Investigation: pcc1 varargs postprocess calls unsupported regex replacement](../investigations/pcc1-varargs-postprocess-callable-replacement.md)

- No.2 Return early when no helper marker is present [DENIED]
- DENIED — That would repair the minimal publication smoke but fail later when pcc1

## [Investigation: wheel-installed pcc1 loses its runtime resource root](../investigations/pcc1-wheel-installed-runtime-resource-location.md)

- No.1 Require `PCC_SOURCE_ROOT` for wheel users [DENIED]
- DENIED — This would expose a repository-only implementation detail in the normal user

## [pcc1 deferred-worker object-protocol tax (nested_walk 78.7%)](../investigations/pcc1-worker-object-protocol-tax.md)

- Proposal No.1 — dict membership index for the walk's name sets `[DENIED as a speed fix]` — Replaced the per-Name `name_in(local_scope, …) + name_in(builtins_ns, …)`
- Candidate queue (each needs its own [CONFIRMED]/[DENIED] verdict) — Exception-free attribute probe** `[CONFIRMED, small]` — implemented
- GC0 per-op check hoisting** `[DENIED as a speed fix]` — mechanism
- elision is already explicitly `[DENIED]` by
- Update 2026-09-04 — bounded node pool retained; five-module reuse `[DENIED]` — The tracking-node freelist measures about 5% on the allocation row and 3.4%

## [Investigation: pproxy HTTP listener resets Claude Code gateway requests](../investigations/pproxy-claude-code-http-proxy-compat.md)

- No.1 Proxy environment precedence routes Claude outside the intended listener [DENIED]
- No.3 Upstream SOCKS relay resets a valid CONNECT tunnel [DENIED]
- DENIED — High-frequency socket sampling showed the Claude process opening two TCP
- DENIED — The modified listener, the old-detector control, and actual 8082 all carried

## [Investigation: process-tree sampler aborts a healthy Stage2 on transient `ps` timeout](../investigations/process-tree-sampler-ps-timeout.md)

- No.2 Ignore missing RSS samples indefinitely [DENIED]
- DENIED — That could under-report the actual RSS peak, lose newly spawned children from

## [Investigation: production archive external-resource host stdio symbol collision](../investigations/production-archive-external-resource-host-stdio-symbol-collision.md)

- No.2 Change production archive symbol ownership for host harnesses [DENIED]
- DENIED — That would weaken the archive's production no-libc ownership contract to serve
- The runtime implementation was not the cause. The test crossed two distinct

## [Investigation: boxed NaN operators inherit container identity semantics](../investigations/python-boxed-nan-value-comparison.md)

- bootstrap, and a recursion-guard attempt that did not help; the later resolved

## [Investigation: Class(args) for a class without an explicit `__init__` synthesises a phantom user_<module>_<class>___init__ symbol → link error](../investigations/python-class-init-phantom-symbol-link-fail.md)

- No.2 Route through a runtime helper for exception subclasses [DENIED — bigger, not minimal for link fix]
- DENIED — bigger, not minimal for link fix — Would require detecting that the class's MRO ends in a builtin exception base,

## [Investigation: class instantiation lowering rejects `*args` / `**kwargs` in `__init__`](../investigations/python-class-init-vararg-not-supported.md)

- No.1 Filter varargs out of "missing required arg" check (insufficient) [DENIED]
- DENIED — The IR signature of `__init__` is still 3 params (`self, path, extra`). If

## [Investigation: cpython-compat `import numpy` fails at `_multiarray_umath` C-extension init](../investigations/python-cpython-compat-import-numpy-multiarray-init-fails.md)

- DIFFERENT path (the C-API `PyImport_ImportModule` shim), NOT the cause of THIS failure.
- Ruled out (2026-05-29): `py_cpy_getattr` itself is a CORRECT thin wrapper —

## [Investigation: class accessed via dotted module path `import p.sub; p.sub.Thing(...)` fails (no-libpython)](../investigations/python-dotted-module-class-attr-no-libpython.md)

- `kind=="class"` branch's dotted case through it. This had NO effect on the

## [Investigation: exact-int branch rebind loses the scalar fallthrough value](../investigations/python-exact-int-branch-representation-join.md)

- No.2 Repair a missing pointer value at each read [DENIED]
- No.3 Force every typed `int` into raw `i64` [DENIED]
- DENIED — The old scalar value has no reliable control-flow or ownership identity after
- DENIED — This would make Python `int` wrap, truncate, or fail where Python requires
- This is not No.2's denied read-time recovery and must not be repaired by
- No.3's denied force-i64 projection.

## [Investigation: function-local class hoisting publishes the class after its first use](../investigations/python-function-local-class-hoist-order.md)

- DENIED — This would make the NULL symptom disappear but would still give repeated

## [Investigation: range(...) for-loop inside a generator stops after the first yield](../investigations/python-generator-range-loop-counter-not-persisted.md)

- No.2 Persist the inline range induction counter in the generator frame [DENIED — more invasive, no upside]
- DENIED — more invasive, no upside — Would require teaching `_collect_generator_frame_names` to reserve a slot for
- range path on the unboxed fast path. DENIED No.2 was the alternative

## [Investigation: `libpy_runtime_pcc.a` duplicate-symbol `pcc_gc_thread_unregister_buffers`](../investigations/python-pcc-built-archive-weak-symbol-duplicates.md)

- pcc's C frontend [DENIED]
- DENIED — out of scope for this slice — The pcc C parser is shared with the `--separate-tus` and Lua/SQLite

## [Investigation: pcc1 list-of-functions value-position lowering via syntactic FuncDef fallback](../investigations/python-pcc1-list-of-functions-syntactic-fallback.md)

- and rejected a name-based fallback because it didn't help —
- No.1 Name-based fallback on element type's `name` attribute [DENIED]
- DENIED — Didn't help — pcc1's type-info propagation seems to also leave the
- Didn't help — pcc1's type-info propagation seems to also leave the

## [Investigation: Python Valhalla value-model status overclaim](../investigations/python-valhalla-value-model-actual-state.md)

- The first V0 source-shape diagnostic patch initially regressed the
- The first direct-method receiver patch regressed the raw per-module self-compile
- The scalar valuebox object-boundary path regressed because the pcc-Python

## [Investigation: HEAD ad60403d drops the entry module's trailing `main()` call — every self-backend program ending in `main()` silently does nothing (exit 0, no output)](../investigations/self-backend-entry-main-call-dropped-exitcode-regression.md)

- touched 2026-07-22 / 2026-08-01), so something recent regressed them.

## [Investigation: self-backend Mach-O stage publish race](../investigations/self-backend-mach-o-stage-publish-race.md)

- During backend #4 GenZGC work, the mandatory self-bootstrap gate regressed

## [Investigation: self-backend short-lived emit worker fanout](../investigations/self-backend-short-lived-emit-worker-fanout.md)

- medium and 193 small workers; pressure produced 2,161 denied admission polls

## [Investigation: self-host oracle cold warmup cascades across xdist fixtures](../investigations/self-host-oracle-cold-warmup-xdist-fixture-cascade.md)

- No.2 Increase the 600-second per-stage timeout [DENIED]
- DENIED — The isolated cold chain completes inside the existing limits. Raising the

## [Investigation: sorted()/min()/max() ignore a custom __lt__ (no-libpython)](../investigations/sorted-min-max-custom-lt-not-used-no-libpython.md)

- No.1 cmp_threeway dispatches __lt__ via py_obj_call_method1 [DENIED — ineffective]
- No.2 cmp_threeway: getattr(bound) + 1-arg call [DENIED — see No.1 update]
- No.3 cmp_threeway: py_class_lookup + py_obj_call [DENIED — reached-but-ineffective]
- DENIED — ineffective — Even with the dispatch compiled into the archive (verified: `__lt__` appears in

## [Investigation: stage1 self-backend ir-scaffold segfault](../investigations/stage1-self-backend-ir-scaffold-segfault.md)

- CONFIRMED|DENIED|DENIED BY USER — Pending.

## [Investigation: Stage2 coordinator exceeds memory cap at deferred checkpoint](../investigations/stage2-deferred-checkpoint-coordinator-memory-cap.md)

- denied full-IR handoff, collection-only and repeatedly-decoded AST proposals,
- rederive those denied memory explanations or widen the8GiB cap.

## [Investigation: strict decref ignores dynamic C-extension type tags](../investigations/strict-cext-dynamic-tag-decref-parity.md)

- No.1 Mirror only the C dynamic-tag exemption in strict decref preparation [DENIED]
- DENIED — The exact guard mirror compiled and passed strict closure, but the original

## [Investigation: tuple completion scans run for no-op publication backends](../investigations/tuple-publication-noop-quadratic-scan.md)

- them. The prior mixed ASM/PCO experiment was denied after full transfer; read

## [Investigation: a tuple-unpack loop variable shadowing the enclosing method name infers as 'callable'](../investigations/type-infer-loop-var-shadowing-method-name.md)

- No.1 bind tuple-unpack for-targets into the scope, per-slot precise types `[DENIED — host-green, pcc1-red]`
- No.1 per-slot precise binding — DENIED, host-green / pcc1-red — The root cause is broader than the report: `_infer_stmt`'s For branch binds a
- and none of the host gates can see it. DENIED as a rider; slot precision needs

## [Investigation: typed-int unboxed arithmetic silently wraps on i64 overflow (violates Python bignum semantics)](../investigations/typed-int-unboxed-overflow-silent-wraparound.md)

- `*`/`<<` from the safe-preserving BinOp set. NO effect — this predicate
- "eligible for native int ABI" predicate, typed_int_abi.py:120). NO effect
- experiment 2 (excluding `*` from the body-shape predicate) had no effect.

## [Investigation: uv-locked pcc1 restart loses CalledProcessError returncode](../investigations/uv-locked-pcc1-restart-called-process-error-returncode.md)

- That ruled out a current native-lowering regression. The wheel fixture instead
- pcc1 restart path [DENIED]
- No.2 Hide the missing field with `getattr(exc, "returncode", 1)` [DENIED]
- No.3 Disable locked-environment restart for compiled pcc1 [DENIED]
- DENIED — The current fixed pcc1 forwarded exit 7 and compiled the exact locked NumPy
- DENIED — That would turn a broken exception object into a generic exit code and regress
- DENIED — The restart is the boundary that selects the deterministic uv-owned pcc
- lowering were denied because each would weaken semantics or patch the wrong

## [Investigation: prove a valueclass hot loop without mistaking tagged-int calls for heap allocation](../investigations/valueclass-hot-loop-zero-allocation-proof.md)

- No.1 Require a call-free hot function [DENIED]
- DENIED — This confuses calls with allocation and would reward deleting Python's bignum

## [Investigation: renamed valueclass exports retain the alias as class identity](../investigations/valueclass-renamed-export-canonical-identity.md)

- No.2 Permit coercion between unrelated same-layout classes [DENIED].
- DENIED — The source names are aliases of one class; the descriptor must preserve that

## [Investigation: virtual-thread handler throughput trails asyncio by 9.7x](../investigations/vthread-asyncio-throughput-gap.md)

- No.3 initialize fixed-size generator frames in one operation [DENIED as a speed claim]
- No.3 DENIED as a speed claim — The fixed-size frame helper passed C and pcc-Python runtime checks under all

## [Investigation: a single carrier exits with sleeping children pending](../investigations/vthread-single-carrier-pending-timer-exit.md)

- No.1 Include pending timers in single-carrier idle handling [DENIED]
- DENIED — The existing cooperative-cancellation test calls run() to let an infinite

## [Investigation: wheel runtime archive loses target and freshness evidence](../investigations/wheel-pcc-runtime-archive-freshness.md)

- No.1 Trust any archive found in the wheel [DENIED]
- DENIED — Skipping the target and source-freshness checks would allow cross-architecture

## [Investigation Report: zlib Integration Exposed Layout and Static-Local Array Bugs](../investigations/zlib-integration-static-local-arrays-and-layout.md)

- That mattered because it ruled out:
