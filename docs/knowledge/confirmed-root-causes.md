# Confirmed root causes

GENERATED by `scripts/distill_investigations.py` from
`docs/investigations/*.md`. Do not edit by hand.

Each line is a mechanism that was established by an experiment, not a
plausible explanation. Use it to recognise a repeat of a known failure
instead of re-diagnosing it.

1541 confirmations across 448 investigations.

## [Investigation: direct AArch64 instruction capture order and lifetime](../investigations/aarch64-direct-instruction-capture-order.md)

- Test [CONFIRMED] — All four nodes failed on the resumed WIP before production edits. HFA stores

## [Investigation: backend-0 finalizer re-enters the tracked-object table lock](../investigations/backend0-finalizer-table-lock-reentrancy.md)

- Test [CONFIRMED] — The regression must finish under the five-second child-process deadline for
- No.1 Native-owner lock reentry plus deferred node reclamation [CONFIRMED]

## [Investigation: bare imported decorators crashed function declaration (`@finalize_array_function_like`)](../investigations/bare-imported-decorator-declaration-crash.md)

- Test [CONFIRMED] — The minimal repro failure was observed; CPython prints 2 (the
- No.1 Extend the imported-metadata principle to bare cpy-imported decorator names [CONFIRMED]
- CONFIRMED — Observed (2026-06-10): minimal repro compiles; auto-mode binary prints

## [Investigation: BOC speedup proofs collapse under xdist CPU contention](../investigations/boc-speedup-proofs-xdist-cpu-contention.md)

- Test [CONFIRMED] — The two nodes passed once while independently scheduled through two workers:

## [Investigation: current bootstrap five-GC audit](../investigations/bootstrap-five-gc-current-audit-2026-05-07.md)

- Test [CONFIRMED] — Self-bootstrap:
- No.1 Keep backend 0 as the default [CONFIRMED]
- CONFIRMED — Backend 0 is still the best default for the current tree. It is the only

## [Investigation: bootstrap five-GC matrix after backend 1 fix](../investigations/bootstrap-five-gc-matrix-after-backend1-fix.md)

- Test [CONFIRMED] — Self-bootstrap:
- No.1 Keep backend 0 as the default [CONFIRMED]
- CONFIRMED — Backend 0 is still the best default for the current tree:

## [Investigation: bootstrap chain should expose and run all five GC backends](../investigations/bootstrap-five-gc-matrix.md)

- Test [CONFIRMED] — The current tree was bootstrapped through stage2 with the self backend:
- No.1 Run a focused bootstrap GC matrix and benchmark audit [CONFIRMED]
- CONFIRMED — No production source change landed. Temporary probe scripts under

## [Investigation: bootstrap five-GC audit after arena deallocator fix](../investigations/bootstrap-five-gc-post-arena-deallocator-audit-2026-05-07.md)

- Test [CONFIRMED] — Self-bootstrap result:
- No.1 Keep backend 0 as default [CONFIRMED]
- CONFIRMED — Backend 0 remains the best default for the current tree. It is still the only

## [Investigation: types dotted-name rsplit reintroduced libpython fallback](../investigations/bootstrap-types-rsplit-libpython-fallback.md)

- Test [CONFIRMED] — env -u LC_ALL uv run pytest tests/test_type_annotations_optional_dotted.py -q -n0
- No.1 Replace `str.rsplit(".", 1)` in `types._class_type_from_dotted` with a native-loop last-dot scan [CONFIRMED]
- CONFIRMED — The focused regression in `tests/test_type_annotations_optional_dotted.py`

## [Investigation: LowIR fallback and borrowed return ownership broke full bootstrap](../investigations/bootstrap-user-function-low-ir-fallback-2026-06-01.md)

- Test [CONFIRMED] — Both failures were observed before fixing:
- `user_function_lowering` [CONFIRMED]
- No.4 Retain borrowed object returns in callee return lowering [CONFIRMED]
- CONFIRMED — The broken module was part of the recent LowIR / layer1 split work, not an
- CONFIRMED — The LLDB backtrace and generated IR tied the double-free to a user-function

## [Investigation: DynType boxed-float `-` / `*` / comparison against int or DynType operands produce wrong results (no py_obj_sub/py_obj_mul; falls to the i64 path)](../investigations/boxed-float-dyntype-sub-mul-compare-wrong.md)

- Test [CONFIRMED] — Run 2026-05-30 (after #26). DynType-float op FLOAT-literal is diff-IDENTICAL

## [Investigation: bytearray repr + mutable methods unsupported (no-libpython cluster)](../investigations/bytearray-repr-and-methods-cluster-no-libpython.md)

- Root cause (CONFIRMED by read) — repr** (tag 18 = PY_TYPE_BYTEARRAY): py_print_fmt.c `py_format` / `py_format_repr`
- No.1 bytearray repr formatter [CONFIRMED #51]

## [Investigation: generic bytes membership always reports no match](../investigations/bytes-membership-dispatch-missing.md)

- Test [CONFIRMED] — `tests/python/test_bytes_membership_runtime.py` failed before the change for

## [Investigation: C frontend has no shared pcc-Python libc link route](../investigations/c-frontend-freestanding-libc-link-route.md)

- Test [CONFIRMED] — `tests/c/test_c_freestanding_libc_link.py::test_c_cli_freestanding_libc_runs_pcc_python_mem_and_allocator`
- No.1 Add an explicit shared freestanding-libc link mode [CONFIRMED]
- CONFIRMED — The public `--freestanding-libc` route is C-only and implies a final system

## [Investigation: large C aggregate assignment causes SelectionDAG blow-up](../investigations/c-large-aggregate-assignment-selectiondag-blowup.md)

- Test [CONFIRMED] — `test_large_struct_assignment_uses_bounded_aggregate_copy_ir` reproduces the
- No.1 Lower large direct addressable aggregate assignment with `llvm.memmove` [CONFIRMED]
- CONFIRMED — Direct `py_re_engine.c --emit-obj`: 8.19 seconds after the fix, versus a

## [Investigation: C llvmdump writes generated files into the caller directory](../investigations/c-llvmdump-top-level-pollution.md)

- Test [CONFIRMED] — `tests/c/test_c_evaluator_dump_artifacts.py` exercises both the default
- No.1 Normalize llvmdump to a cache-owned or explicit artifact directory [CONFIRMED]
- CONFIRMED — The new focused packet proves boolean, explicit, and multi-TU ownership. The

## [Investigation: C LVN reuses a string literal as an array initializer](../investigations/c-lvn-array-string-initializer-reuse.md)

- Test [CONFIRMED] — The focused test above failed deterministically before the source change. The
- No.1 Exclude C array initialization from source-level LVN [CONFIRMED]
- CONFIRMED — passed in 0.14s

## [Investigation: C-API port modules extern() runtime names with no exported definition (py_type_of, py_int_rem)](../investigations/capi-port-extern-static-inline-py-type-of.md)

- Test [CONFIRMED] — New static regression: tests/python/test_port_extern_symbols_resolve.py —
- No.1 Exported wrapper pcc_py_type_of + rebind six externs [CONFIRMED]
- No.2 Rebind py_int_rem extern to the real py_int_mod [CONFIRMED]
- CONFIRMED — Full `make -B` port archive rebuild completes; the numpy demo app link no
- CONFIRMED — test_port_extern_symbols_resolve went red-on-py_int_rem -> green with only

## [cc-runtime `pcc_trash_should_defer` switch fall-through → trash-node double-free](../investigations/cc-runtime-trash-should-defer-fallthrough-double-free.md)

- Repro (minimal, [CONFIRMED]) — class T:
- Root cause [CONFIRMED] — `pcc/py_runtime/src/py_obj.c::pcc_trash_should_defer` was a `switch` whose
- Test [CONFIRMED] — New focused regression: `tests/python/test_gc_trashcan.py::test_list_of_finalizable_instances_cleared`

## [Investigation: HEAD truth dependency sync invokes the release build hook](../investigations/ci-head-truth-editable-build-hook-bootstrap.md)

- Test [CONFIRMED] — The focused workflow-contract regression requires the documented
- No.1 Scope PCC_BUILD_SKIP to dependency installation [CONFIRMED]
- CONFIRMED — With `PCC_BUILD_SKIP=1` scoped to dependency installation, an isolated

## [Investigation: heavy HEAD truth cold run cascades after runtime setup and xdist oversubscription](../investigations/ci-head-truth-heavy-cold-run-cascade.md)

- Test [CONFIRMED] — GitHub run `29094810339` is the clean-run integration reproducer. The uploaded
- No.1 Register an explicit runtime-archive preflight [CONFIRMED]
- No.2 Make the five-GC xdist group visible before xdist rewrites node IDs [CONFIRMED]
- No.3 Recalibrate measured timeout and worker budgets after removing contention [CONFIRMED]
- No.4 Preserve TIMEOUT when artifact inspection adds failure detail [CONFIRMED]
- CONFIRMED — The exact make command completed successfully in an isolated Python 3.13
- CONFIRMED — The regression invokes a nested real xdist session with two workers, loads the
- CONFIRMED — The first post-grouping complete run proved the first five gates and completed
- CONFIRMED — The first post-fix all-suite exposed that a five-GC process killed at 900

## [Investigation: HEAD truth workflow timeouts cannot cover keep-going gates](../investigations/ci-head-truth-keep-going-timeout-envelope.md)

- Test [CONFIRMED] — The workflow-contract test derives each selected suite's worst-case gate
- No.1 Widen and mechanically gate the workflow timeout envelopes [CONFIRMED]
- CONFIRMED — The registry-derived regression passes with 10/25-minute light step/job limits

## [Investigation: HEAD truth workflows require an absent committed uv lockfile](../investigations/ci-head-truth-locked-sync-missing-uv-lock.md)

- Test [CONFIRMED] — The focused workflow-contract regression requires both locked workflows to
- No.1 Track the valid root lockfile used by locked CI syncs [CONFIRMED]
- CONFIRMED — The root lockfile is valid (`uv lock --check` and local `uv sync --dev

## [Class method borrowed parameters were not registered as GC roots](../investigations/class-method-borrowed-parameter-roots.md)

- Test [CONFIRMED] — The focused IR regression passed on 2026-08-14 in 0.32 seconds. It asserts
- CONFIRMED — The focused emitted-IR gate and the original module's native self-backend

## [Investigation: `with` __exit__ not called on exception (py_context_exit leaves the exception pending)](../investigations/context-manager-exit-not-called-on-exception.md)

- Test [CONFIRMED] — `tests/python/test_context_manager_full.py` 6/6 pass (default mode); the self
- CONFIRMED — Single- and multi-manager unwind now run `__exit__` R→L with the exception

## [Investigation: contextual per-module fallback gate for Python self-host](../investigations/contextual-per-module-fallback-gate.md)

- Test [CONFIRMED] — The gate now has three separate models:
- No.3 Add contextual per-module hard gate [CONFIRMED]
- CONFIRMED — This matches the real self-host model while keeping a hard gate. Contextual
- CONFIRMED — The focused contextual count is zero after the change. Literal self-method
- CONFIRMED — `PROBE_POLICY_CLOSED_WORLD` now selects the real sibling export context for

## [Investigation: migrate pcc's default host to CPython 3.15.0rc1](../investigations/cpython-315rc1-host-migration.md)

- Test [CONFIRMED] — The version mismatch above is directly observed. Dependency and compiler
- No.1 Install 3.15.0rc1 and atomically replace the project environment [CONFIRMED]
- CONFIRMED — uv run python -VV

## [Investigation: `type(cpy).__name__` routed via native getattr instead of libpython](../investigations/cpython-type-name-not-routed-via-libpython.md)

- Test [CONFIRMED] — `test_cpython_type_name_inline_dispatches_via_libpython` passes; `@.cpy.attr.__name__`
- CONFIRMED — Inert in no-libpython (the argument never looks cpy there, so the branch returns

## [Investigation: imported valueclass return annotations lose aggregate ABI](../investigations/cross-module-imported-valueclass-return-abi.md)

- Test [CONFIRMED] — The three-module regression fails deterministically. `records.Quad` and the
- CONFIRMED — Require the three-module aggregate definition/call regression, the existing

## [Investigation: exported class fields omit nested method writes](../investigations/cross-module-nested-method-field-order.md)

- Test [CONFIRMED] — The real native test fails its unchanged stdout assertion in 7.62s; the serial
- records source/binary identities and receipts. No.1 is CONFIRMED through the

## [Investigation: shadowed cross-module class method dispatch](../investigations/cross-module-shadowed-class-method-dispatch.md)

- Test [CONFIRMED] — env -u LC_ALL uv run pytest tests/test_industrial_cross_module_abi.py -q -n0
- No.1 Preserve qualified extern-class registry keys for shadowed sibling classes [CONFIRMED]
- CONFIRMED — The generated IR for the repro now dispatches the second chained call to

## [Investigation: cross-module valueclass methods lose aggregate ABI](../investigations/cross-module-valueclass-aggregate-abi-export.md)

- Test [CONFIRMED] — The current merged compiler IR contains both incompatible shapes. The focused
- CONFIRMED — Require focused merged-IR agreement, no-libpython closure, same-module

## [Investigation: free-function call signatures retain valueclass shells](../investigations/cross-module-valueclass-free-function-call-signature.md)

- Test [CONFIRMED] — A three-module records/provider/consumer reduction reproduces the failure

## [Investigation: user __eq__ in dict/set key lookup (no-libpython)](../investigations/custom-obj-eq-dict-set-key-no-libpython.md)

- active — gap CONFIRMED + a fix ATTEMPTED and REVERTED 2026-05-30 (it works for
- Root cause (CONFIRMED) — `py_obj_eq` (py_obj_ops_compare.c + the pcc-Python port) handles

## [Investigation: function decorators silently return null under --python-libpython=off (first-class-function limitation)](../investigations/decorator-first-class-fn-silent-null-no-libpython.md)

- Test [CONFIRMED] — : `@trace`-decorated `add(3,4)` -> `<null>` under =off (compiles, no

## [Investigation: module-level dict() builtin leaks its temp GC root into err.exit joins](../investigations/dict-builtin-module-top-stackmap-err-exit-join.md)

- Repro [CONFIRMED] — Deterministic minimal reproducer — the dict() **must be module-level**
- Root Cause [CONFIRMED] — Probe on the real stage1 module (`self_backend_module_77.ll`,
- Test [CONFIRMED] — Failure observed 2026-08-15 with the Repro command above and with the
- cleanup mechanism [CONFIRMED]
- CONFIRMED — All five bisect probes compile and run correctly under

## [Investigation: dict comprehension over `enumerate(set)` produces an empty dict](../investigations/dict-comprehension-enumerate-set-empty.md)

- Test [CONFIRMED] — `tests/python/test_native_comprehension_over_generator.py::`
- No.1 Restrict the indexed enumerate-comprehension fast path to proven sequences [CONFIRMED focused]
- CONFIRMED focused — The red-first regression passed. Generator/scope/mapping-family adjacent gates

## [Investigation: dict-splat operands bypass rooted unwind cleanup](../investigations/dict-splat-stackmap-rooted-unwind.md)

- Root cause [CONFIRMED] — `_emit_dict_literal_with_splat` created a dict and entered its temporary GC
- Fix [CONFIRMED] — The splat path now uses the same ownership contract as the ordinary native

## [Investigation: entry-alloca hoist fix (call-ret GC root slots) was semantically right but made stage2 quadratic — 282s -> 5245s under pcc1, plus a leftover-children hour-long shell](../investigations/entry-alloca-cursor-quadratic-stage2-regression.md)

- Test [CONFIRMED] — The failed gate's `profile/stage2.time`: real_s=5246.168, user_s=1728.444

## [Investigation: eager fallback fixture prevents bounded phase validation](../investigations/fallback-baseline-eager-fixture-prevents-sharding.md)

- Test [CONFIRMED] — The two watchdog failures are observed. Before changing the fixture, add a
- No.1 is CONFIRMED. Three lazy phase computations preserve the original node

## [Investigation: fallback-baseline gates red at HEAD — three unregistered-surface regressions from recent commits (self_module_contracts, c_varargs closure import, udiv/urem scaffold gap)](../investigations/fallback-baseline-head-regressions-unregistered-closure-modules.md)

- Test [CONFIRMED] — All three causes observed as py_cpy call instructions in the respective IR

## [Fallback baseline RED: 12 `py_cpy_*` in `compile_python` GPU dispatch (legacy `ir_scaffold=off`)](../investigations/fallback-baseline-legacy-gpu-dispatch-regression.md)

- Root construct (CONFIRMED) — All 12 `py_cpy_*` are emitted inside `user_pcc_py_frontend_pipeline_compile_python`
- Isolation (CONFIRMED) — Not** the class-attr-override fix landed the same day: forcing

## [Investigation: marshal raw per-module fallback ratchet exceeds baseline](../investigations/fallback-baseline-marshal-raw-ratchet-2026-07-10.md)

- Test [CONFIRMED] — The minimized test fails deterministically on current HEAD. Direct probes under
- No.3 Identify and remove the incremental scaffold-off dynamic call class [CONFIRMED]
- CONFIRMED — The raw scaffold-off count fell from 341 to 316, scaffold-on stayed zero, the

## [Investigation: fallback-baseline red at HEAD — `pcc.py_frontend.pipeline` 14 py_cpy calls from `subprocess.run(..., check=True)`](../investigations/fallback-baseline-pipeline-subprocess-run-kwargs-regression.md)

- Test [CONFIRMED] — In-process probe (no archive, no link step) reproduces it directly:

## [Investigation: shared field walker lacks a standalone static export](../investigations/field-walk-static-export-fallback-regression.md)

- Test [CONFIRMED] — All 11 class_gen additions occur in `_collect_method_instance_fields`; all

## [Investigation: key= callables via inlining (no first-class functions, no-libpython)](../investigations/first-class-fn-key-via-inline-no-libpython.md)

- No.1 FRONTEND: inline a simple attr/index key lambda into a key-comparing sort [CONFIRMED #56]
- No.2 min/max(key=<simple lambda>) via the same inline key in an object fold [CONFIRMED #57]
- No.1 sorted(xs, key=<simple lambda>) [CONFIRMED #56]
- CONFIRMED — `/tmp/gap_probe/sortkey.py` (int-attr, str-attr, +reverse, index-key over
- No.2 min/max(key=<simple lambda>) [CONFIRMED #57]
- CONFIRMED — `/tmp/gap_probe/minmax_key.py` (attr key min/max, index key min/max) IDENTICAL;

## [Investigation: first-install runtime copy loses static port ABI exports](../investigations/first-install-runtime-copy-loses-port-abi.md)

- Test [CONFIRMED] — The complete cold runtime build fails in 30.52 seconds. Both direct emission
- No.1 Preserve the existing isolated runtime directory contract [CONFIRMED]

## [Investigation: five-GC matrix timeout contract drift](../investigations/five-gc-matrix-timeout-contract-drift.md)

- Test [CONFIRMED] — Profiles produced before timeout:
- No.1 Align local task/startup contracts with the measured head-truth envelope [CONFIRMED]
- No.2 Run one corrected-envelope matrix and do not repeat it blindly [CONFIRMED]
- No.3 Treat a second timeout as a measured performance blocker [CONFIRMED]
- No.4 Add content-addressed complete-backend resume [CONFIRMED]
- No.5 Add content-addressed stage2 resume and close the aggregate gate [CONFIRMED]
- CONFIRMED — The two local contracts now match `scripts/head_truth_manifest.py` and the
- CONFIRMED — The corrected run emitted three pytest dots, proving complete GC0, GC1, and
- CONFIRMED — The corrected-envelope timeout is now the second independent aggregate
- CONFIRMED — Focused tests covered complete reuse, output tamper, source mismatch, missing
- CONFIRMED — The focused cache/scheduling gate passed:

## [Investigation: freestanding backend-0 collector orchestration](../investigations/freestanding-gc-backend0-collector-orchestration.md)

- Test [CONFIRMED] — The focused ownership gate fails because
- No.1 Move the existing collector state machine into a strict object [CONFIRMED]
- CONFIRMED — The strict object now uniquely owns `py_gc_collect` and nine named raw phases.

## [Investigation: freestanding backend-0 GC slot actions](../investigations/freestanding-gc-backend0-slot-actions.md)

- Test [CONFIRMED] — Require one strict production owner for subtract/mark/clear callbacks, exact
- No.1 Move backend-0 raw slot actions behind a strict ABI [CONFIRMED]

## [Investigation: freestanding shared GC barrier and dispatcher](../investigations/freestanding-gc-barrier-dispatcher.md)

- Test [CONFIRMED] — The initial ownership test was observed red. The final strict object has exact
- No.1 Move shared barrier/dispatch policy into one strict object [CONFIRMED]
- CONFIRMED — Focused tests, production archive ownership, and C-oracle differentials are

## [Investigation: freestanding common tracing mark-cycle orchestration](../investigations/freestanding-gc-common-mark-cycle-orchestration.md)

- Test [CONFIRMED] — The pre-change focused gate failed because
- raw ABI declarations. It will be marked CONFIRMED after the pre-change
- No.1 Move the shared mark-cycle closure into one strict object [CONFIRMED]
- CONFIRMED — The final strict object owns seven raw phases, including the separate

## [Investigation: move raw GC control exports into strict freestanding pcc-Python](../investigations/freestanding-gc-control-production-ownership.md)

- Test [CONFIRMED] — The first source/archive ownership regression failed because
- No.1 Extract the eleven raw control exports unchanged [CONFIRMED]
- CONFIRMED — The initial test produced the intended red result (`FileNotFoundError` for the

## [Investigation: Freestanding GC forwarding and identity substrate](../investigations/freestanding-gc-forwarding-identity-substrate.md)

- Test [CONFIRMED] — The initial owner test failed in 0.10 seconds because the strict source did not
- No.1 Move the shared forwarding/identity substrate to strict pcc-Python [CONFIRMED]
- CONFIRMED — strict owner gate: 5 passed in 60.03 seconds;

## [Investigation: freestanding Backend 4 forwarding retirement](../investigations/freestanding-gc-forwarding-retirement.md)

- Test [CONFIRMED] — The strict object has exact LLVM and self-emitter closure and defines only the
- No.1 Move forwarding retirement into one strict object [CONFIRMED]
- CONFIRMED — Focused tests and the C-oracle differential are green. A current-source

## [Investigation: Freestanding Backend 3 copy-oldification](../investigations/freestanding-gc-generational-oldification.md)

- Test [CONFIRMED] — The initial owner test failed in 0.09 seconds because the strict owner did not
- No.1 Move Backend 3 oldify-copy orchestration to strict pcc-Python [CONFIRMED]
- CONFIRMED — strict closure/archive gate: 5 passed in 66.03 seconds;

## [Investigation: Freestanding generational promotion and stable roots](../investigations/freestanding-gc-generational-promotion.md)

- Test [CONFIRMED] — The initial owner test failed in 0.09 seconds because the strict source did not
- CONFIRMED — strict closure/archive gate: 5 passed in 63.44 seconds;

## [Investigation: Freestanding Backend 3 remembered-owner queue](../investigations/freestanding-gc-generational-remembered-owners.md)

- Test [CONFIRMED] — The initial owner test failed in 0.10 seconds because the strict source did not
- CONFIRMED — strict closure/archive gate: 5 passed in 62.53 seconds;

## [Investigation: Freestanding Backend 3 root scheduler](../investigations/freestanding-gc-generational-scheduler.md)

- Test [CONFIRMED] — The initial owner test failed in 0.09 seconds because the strict source did not
- No.1 Move Backend 3 TLS/root scheduling to strict pcc-Python [CONFIRMED]
- CONFIRMED — strict closure/archive gate: 5 passed in 63.67 seconds;

## [Investigation: freestanding incremental/concurrent GC scheduler](../investigations/freestanding-gc-incremental-concurrent-scheduler.md)

- Test [CONFIRMED] — The first focused node failed because the strict scheduler source was absent.
- No.1 Move backend-1/2 scheduling into one strict object [CONFIRMED]
- CONFIRMED — The new strict object uniquely owns all eleven scheduling symbols in source and

## [Investigation: move production GC pointer indexes from C to freestanding pcc-Python](../investigations/freestanding-gc-index-table-production-ownership.md)

- Test [CONFIRMED] — `tests/python/test_freestanding_gc_index_table.py` compiles the replacement
- No.1 Port the open-addressed tables to strict freestanding pcc-Python [CONFIRMED]
- CONFIRMED — The focused suite reports `28 passed in 5.49s`. A separate production

## [Investigation: freestanding mapped-root visitor production ownership](../investigations/freestanding-gc-mapped-root-visitor-ownership.md)

- No.1 Move the complete mapped-root visitor into one strict module [CONFIRMED]
- CONFIRMED — The strict module is the sole production owner of the two public continuation

## [Investigation: Freestanding GC object-node and young-list substrate](../investigations/freestanding-gc-object-node-young-list.md)

- Test [CONFIRMED] — The initial owner test failed in 0.09 seconds with `FileNotFoundError`, proving
- No.1 Move object-node/list/young-worklist primitives to strict pcc-Python [CONFIRMED]
- CONFIRMED — strict source/closure/archive gate: 5 passed in 64.32 seconds;

## [Investigation: production GC link-map ownership](../investigations/freestanding-gc-production-link-map.md)

- Test [CONFIRMED] — The archive contains 633 public collector definitions. All map to pcc-Python
- No.1 Add exact production source-attribution ratchets [CONFIRMED]
- CONFIRMED — The three focused link-map tests pass against the content-addressed production

## [Investigation: freestanding GC public collection and config](../investigations/freestanding-gc-public-collection-config.md)

- Test [CONFIRMED] — The focused pre-change gate fails because the strict source is absent. The
- No.1 Move config and public tracing collection into one strict object [CONFIRMED]
- CONFIRMED — The new strict object uniquely owns all seven config/public-collection symbols

## [Investigation: freestanding Backend 4 relocation copy transaction](../investigations/freestanding-gc-relocation-copy.md)

- Test [CONFIRMED] — The strict test proves exact LLVM/self object closure, exact production archive
- No.1 Move the relocation copy transaction behind two strict ABIs [CONFIRMED]
- CONFIRMED — The strict object exports exactly the two copy symbols. Its undefined set is

## [Investigation: freestanding Backend 4 relocation drain](../investigations/freestanding-gc-relocation-drain.md)

- Test [CONFIRMED] — The strict object owns both public drains and six helper ABIs. Its safepoint
- No.1 Move the bounded evacuation drains to a strict object [CONFIRMED]
- CONFIRMED — Exact LLVM/self closure, unique production archive ownership, five adjacent

## [Investigation: freestanding Backend 4 relocation payload copying](../investigations/freestanding-gc-relocation-payload.md)

- Test [CONFIRMED] — The strict ownership test proves exact LLVM/self object closure, exact
- No.1 Move shared payload copying behind strict ABI exports [CONFIRMED]
- CONFIRMED — The strict object has exactly ten public definitions. Its undefined set is

## [Investigation: freestanding Backend 4 relocation remap](../investigations/freestanding-gc-relocation-remap.md)

- Test [CONFIRMED] — The strict test proves exact LLVM/self object closure, exact production archive
- No.1 Move eligibility and remap behind four strict ABI exports [CONFIRMED]
- CONFIRMED — The strict module exports exactly the four symbols and imports only the

## [Investigation: freestanding Backend 4 relocation selector](../investigations/freestanding-gc-relocation-selector.md)

- Test [CONFIRMED] — All ten selector functions now have finite ABI names. The safepoint cadence
- No.1 Move page-grouped candidate selection to a strict object [CONFIRMED]
- CONFIRMED — The strict object exports exactly ten definitions and imports only thirteen

## [Investigation: move GC root introspection into strict freestanding pcc-Python](../investigations/freestanding-gc-root-introspection-ownership.md)

- Test [CONFIRMED] — The focused test pins one strict source owner, exact LLVM/self raw closure,
- No.1 Split the five read-only root-introspection exports and share the existing graph lock [CONFIRMED]
- CONFIRMED — The five public functions and one internal slot-span helper now compile as one

## [Investigation: freestanding GC root registry production ownership](../investigations/freestanding-gc-root-registry-production-ownership.md)

- No.1 Split scheduler and continuation registry mutation into one strict module [CONFIRMED]
- CONFIRMED — The six public registry mutation APIs and five shared helpers now have one

## [Investigation: move production GC state storage into a freestanding object](../investigations/freestanding-gc-state-production-ownership.md)

- Test [CONFIRMED] — The production archive ownership was inspected with `nm -A`. The three
- No.1 Extract declarations into strict `freestanding_gc_state.py` [CONFIRMED]
- CONFIRMED — `freestanding_gc_state.py` now owns all 130 `py_gc_*` / `pcc_gc_*` raw

## [Investigation: freestanding GC telemetry counter ABI drift](../investigations/freestanding-gc-telemetry-counter-abi-drift.md)

- Test [CONFIRMED] — `tests/python/test_freestanding_gc_telemetry.py` extracts the C dispatcher,
- No.1 Align every pcc-Python branch with the public C counter enum [CONFIRMED]
- CONFIRMED — The complete focused file passes with both emitters and the production archive:

## [Investigation: freestanding tracing sweep collector](../investigations/freestanding-gc-tracing-sweep-collector.md)

- Test [CONFIRMED] — The pre-change focused gate fails because the strict collector source is
- No.1 Move the common tracing sweep kernel into one strict object [CONFIRMED]
- CONFIRMED — The strict object uniquely owns all four raw phases and has the same exact

## [Investigation: freestanding tracing sweep slot actions](../investigations/freestanding-gc-tracing-sweep-slot-actions.md)

- Test [CONFIRMED] — The pre-change focused gate failed because `freestanding_gc_sweep_slots.py`
- No.1 Share strict clear-referents mechanics with distinct candidate guards [CONFIRMED]
- CONFIRMED — The strict source owns all seven candidate/slot/metadata/PASS-1 exports and is

## [Investigation: freestanding Backend 4 ZPage allocation](../investigations/freestanding-gc-zpage-allocation.md)

- Test [CONFIRMED] — The strict object now owns the two allocation transactions. C-oracle versus
- No.1 Move ZPage carving and owner registration to a strict object [CONFIRMED]
- CONFIRMED — Exact LLVM/self closure, unique production archive ownership, three page-class

## [Investigation: freestanding Backend 4 ZPage lifecycle](../investigations/freestanding-gc-zpage-lifecycle.md)

- Test [CONFIRMED] — The strict object has exact LLVM and self-emitter closure and defines only the
- CONFIRMED — Focused tests and the C-oracle differential are green. A current-source

## [Investigation: freestanding Backend 4 ZPage mechanics](../investigations/freestanding-gc-zpage-mechanics.md)

- Test [CONFIRMED] — The strict object has exact LLVM and self-emitter closure and defines only the
- CONFIRMED — Focused tests are green. A current-source no-libpython/self pcc1 compiled the

## [Investigation: production pcc-Python runtime still archives C objects](../investigations/freestanding-runtime-final-no-c-closure.md)

- Test [CONFIRMED] — The ownership ratchet failed before the implementation because the immutable
- No.1 Move the three small semantic ABIs into existing pcc-Python modules [CONFIRMED]
- Test [CONFIRMED] — gtimeout 300s env -u LC_ALL uv run pytest -q -n0 \

## [Investigation: freestanding void intrinsics materialize `py_None`](../investigations/freestanding-void-intrinsics-py-none.md)

- Test [CONFIRMED] — `tests/python/test_freestanding_module.py::test_freestanding_void_unsafe_intrinsic_does_not_materialize_py_none`
- freestanding mode [CONFIRMED]
- CONFIRMED — The minimized regression changed from the validator error naming

## [Investigation: dynamic f-string format spec f"{v:>{w}}" (no-libpython)](../investigations/fstring-dynamic-format-spec-no-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_fstring_dynamic_spec.py` (dynamic width >/</^, dynamic
- No.1 LIFT: assemble the spec at runtime for bare-identifier nested fields [CONFIRMED #61]
- No.1 LIFT-level runtime spec assembly [CONFIRMED #61]
- CONFIRMED — `/tmp/gap_probe/dynspec.py` IDENTICAL to python3 (dynamic width/align/precision/

## [Investigation: nested replacement field inside an f-string format spec (f"{val:>{width}.2f}") unsupported under no-libpython](../investigations/fstring-nested-replacement-field-in-format-spec.md)

- Root cause (CONFIRMED by reading) — `pcc/parse/py_parse.py` `_parse_fstring_expr` (~line 2002) extracts the spec as

## [Investigation: __del__ finalizers on reference-cycle members are not run by the tracing collect (#1/#2/#3/#4)](../investigations/gc-5backend-cycle-finalizer-not-run-no-libpython.md)

- Test [CONFIRMED] — `tests/python/gc_production_contract/test_finalizer_cycle.py` — parametrized

## [Investigation: a live exception's referents (message) are reclaimed by the tracing collect (#1/#2/#3/#4)](../investigations/gc-5backend-exception-referent-roots-no-libpython.md)

- Test [CONFIRMED] — `tests/python/gc_production_contract/test_exception_roots.py` — #0 asserts
- Update 2026-05-31: LLDB root-cause CONFIRMED — the exception is never GRAYED (not an offset gap, not a tracking-at-alloc gap) — The "missing offset" and "untracked-at-alloc" hypotheses are both **wrong**.
- No.4 FRONTEND: GC-root locals that own a strong PyObject ref via a borrowed-source assignment (`s_exc = e`) and/or root the except-handler binding properly through the standard owned-local lifecycle [CONFIRMED — scoped implementation tracks except-handler names and roots locals assigned from them]
- CONFIRMED — `tests/python/gc_production_contract/test_exception_roots.py` -> 5 passed.

## [Investigation: object resurrection in __del__ (PEP 442) is mishandled by the two-phase tracing sweep](../investigations/gc-5backend-finalizer-resurrection-no-libpython.md)

- Test [CONFIRMED] — `tests/python/gc_production_contract/test_finalizer_resurrection.py` — #0 asserts
- reachable, so PASS-1/PASS-2 skip it. [CONFIRMED]
- CONFIRMED — Repro returns `2 [42, 43]` on all five backends (was #1/#2 heap-corruption abort,

## [Investigation: GC backends #1/#2/#3 fail the basic object-lifetime contract under no-libpython self-backend](../investigations/gc-5backend-object-lifetime-contract-no-libpython.md)

- Test [CONFIRMED] — `tests/python/gc_production_contract/test_object_lifetime.py` — parametrized

## [Investigation: reentrant gc.collect() from a cycle member's __del__ segfaults on the tracing backends](../investigations/gc-5backend-reentrant-collect-during-finalizer-no-libpython.md)

- Test [CONFIRMED] — `tests/python/gc_production_contract/test_gc_collect_reentrancy.py` — two shapes
- No.1 reentrancy guard in `pcc_gc_collect`: a tracing collect already in progress -> reentrant call returns 0 [CONFIRMED]
- CONFIRMED — After wipe+rebuild, the cycle repro returns `[1, 2]` on all five backends (was

## [Investigation: valueclass pointer payload roots under backend #4 relocation](../investigations/gc-5backend-valueclass-pointer-payload-roots-no-libpython.md)

- Test [CONFIRMED] — The focused test above is the gating regression. It compiles the same strict
- No.2 Do not speculatively execute dynamic valueclass getattr fallback [CONFIRMED partial]
- No.3 Root borrowed user-function object parameters as GC frame slots [CONFIRMED]
- No.4 Treat stale runtime archives as a test/build invalidation failure [CONFIRMED]
- No.5 Preserve pcc-Python runtime raw-pointer/new-reference ownership [CONFIRMED]
- No.6 Match cross-module `-> None` extern function ABI to same-module declarations [CONFIRMED]
- No.7 Suppress implicit GC roots/retains in pcc-Python runtime-library primitives [CONFIRMED]
- No.8 Rebuild pcc-emitted runtime archives when compiler sources are newer [CONFIRMED]
- No.15 Backend #3/#4 focused root-slot fast path verification [CONFIRMED focused]
- No.16 Backend #3 full bootstrap correctness restored, 60s still open [CONFIRMED]
- No.17 Backend #4 forwarding-target lookup index [CONFIRMED focused]
- No.18 Direct valueclass payload locals root pointer fields [CONFIRMED focused]
- No.19 Closure-captured direct valueclass payload crosses object ABI boundary [CONFIRMED focused]
- No.20 Tuple-unpacked direct valueclass payload targets need payload storage roots [CONFIRMED focused]
- No.21 For-loop direct valueclass payload targets need object-to-payload conversion [CONFIRMED focused]
- No.22 Comprehension direct valueclass payload targets need nested attr type recovery and roots [CONFIRMED focused]
- No.23 Set/dict comprehension direct valueclass payload targets share the indexed comprehension root path [CONFIRMED focused]
- No.24 List/tuple subscript direct valueclass payload targets preserve payload roots [CONFIRMED focused]
- No.25 Bool-op direct valueclass payload targets preserve payload roots [CONFIRMED focused]
- No.26 Module-global direct valueclass payload targets need module storage and field roots [CONFIRMED focused]
- No.27 Module-global direct valueclass payload roots need overwrite and teardown ownership [CONFIRMED focused]
- No.28 Boxed list-setitem valueclass constructor fields need ownership transfer [CONFIRMED focused]
- No.29 Covered ValueBox object-boundary constructor fields need ownership proof and attr temp release [CONFIRMED focused]
- CONFIRMED partial — This removed the misleading `AttributeError: extra/items` side effect and
- CONFIRMED — The decisive substitution was to change only backend #4's final call from
- CONFIRMED — The stale-archive issue was a real test/build-system gap, not the semantic root
- CONFIRMED — After removing `_resolve_instance()`, backend #0 still failed
- CONFIRMED — Focused IR regression and pcc1 stage-smoke evidence are green:
- CONFIRMED — The bad interaction was inside the runtime substrate itself, not in ordinary
- CONFIRMED — The build gap was user-visible: `libpy_runtime_pcc.a` /
- CONFIRMED focused — Focused GC3/GC4 evidence after the guard:
- CONFIRMED — Focused checks:
- CONFIRMED focused — Focused validation after the target-index patch:
- CONFIRMED focused — env -u LC_ALL uv run pytest -q -n0 \
- CONFIRMED focused — The minimized `/tmp` probe compiled and ran under backend #4 with the expected
- CONFIRMED focused — The minimized `/tmp` probe compiled under strict no-libpython self-backend and
- CONFIRMED focused — No implementation change was required for this slice. The indexed
- CONFIRMED focused — No implementation change was required for this slice. The existing direct
- CONFIRMED focused — No implementation change was required for this slice. The existing bool-op
- CONFIRMED focused — After the patch, the minimized strict self-backend probe compiled and printed
- CONFIRMED focused — The strict self-backend reassignment probe now matches CPython under all five
- CONFIRMED focused — The strict self-backend list-overwrite probe now matches CPython under all five
- CONFIRMED focused — Focused gates on the final code state:

## [Investigation: native gc callbacks should avoid libpython fallback](../investigations/gc-api-callbacks.md)

- Test [CONFIRMED] — The failing baseline was observed with:

## [Investigation: native gc freeze surface should avoid libpython fallback](../investigations/gc-api-freeze-surface.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Add a minimal native freeze-count runtime surface [CONFIRMED]
- CONFIRMED — The focused fallback repro now passes:

## [Investigation: native gc.get_referents should avoid libpython fallback](../investigations/gc-api-get-referents.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Expose a native referents-list runtime helper [CONFIRMED]
- CONFIRMED — The focused fallback repro now passes:

## [Investigation: gc.is_finalized atomic values should not need libpython](../investigations/gc-api-is-finalized-atomic.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Add native codegen for atomic `gc.is_finalized()` [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_frontend/pipeline.py` and

## [Investigation: native gc object enumeration should avoid libpython fallback](../investigations/gc-api-object-enumeration.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Expose tracked-object and direct-referrer runtime helpers [CONFIRMED]
- CONFIRMED — The focused repro now passes without libpython fallback:

## [Investigation: passive gc API surfaces should avoid libpython fallback](../investigations/gc-api-passive-surfaces.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Lower `gc.garbage` and `gc.get_stats()` natively for current contracts [CONFIRMED]
- CONFIRMED — The focused fallback repro now passes:

## [Investigation: Backend 1 auto step should sweep candidates and discharge debt](../investigations/gc-backend1-auto-step-sweep-debt.md)

- Test [CONFIRMED] — The focused gate fails before the fix:
- No.2 Clear automatic debt once tracing reaches sweep-candidate state [CONFIRMED]
- CONFIRMED — The focused pcc-Python churn gate now passes:

## [Investigation: Backend #1 gc.callbacks.remove does not stop later callbacks](../investigations/gc-backend1-callback-remove.md)

- Test [CONFIRMED] — The failing Backend #1 baseline was observed with:
- No.1 Probe whether callback list mutation survives Backend #1 collection [CONFIRMED]
- No.2 Allocate pcc-Python function wrappers through the tracing allocator [CONFIRMED]
- CONFIRMED — The Backend #1 probe showed:
- CONFIRMED — After rebuilding `libpy_runtime_pcc_py.a`, the failing test passed:

## [Investigation: Backend 1 closure cycle gate](../investigations/gc-backend1-closure-cycle-gate.md)

- Test [CONFIRMED] — The focused gate has been observed failing:
- No.1 Replace unsupported function-attribute cycle with closure/list cycle [CONFIRMED]
- No.2 Add explicit tracing collect sweep fallback [CONFIRMED]
- CONFIRMED — The replacement gate reaches GC and exposes the real failure:
- CONFIRMED — The backend #1 verdict command is now green:

## [Investigation: Backend 1 explicit collect must sweep with live roots](../investigations/gc-backend1-explicit-collect-sweep.md)

- Test [CONFIRMED] — The focused gate fails before the fix:
- No.1 Sweep tracing candidates after explicit collect reaches mark termination [CONFIRMED]
- CONFIRMED — The focused gates now pass:

## [Investigation: Backend 1 incremental tricolor pacer](../investigations/gc-backend1-incremental-tricolor-pacer.md)

- Test [CONFIRMED] — `tests/test_gc_backend_incremental.py` is the focused gate for this slice.
- No.1 Add backend #1 allocation debt pacer and pause telemetry [CONFIRMED]
- CONFIRMED — The focused backend #1 pacer gate now passes:

## [Investigation: Backend #1 collects function-local live cycles](../investigations/gc-backend1-live-cycle-roots.md)

- Test [CONFIRMED] — `tests/test_gc_effectiveness.py::test_collect_does_not_break_live_cycle` is the gate. It was run under Backend #1 and failed with the output mismatch above.
- No.1 Fix Backend #1 root/referent marking for function-local live cycles [CONFIRMED]
- CONFIRMED — The root cause was class reachability, not the dynamic-attribute dict itself. A live instance did not trace its `cls` pointer, and generated class globals were not registered as module roots. Backend #1 could therefore sweep `class N`; subsequent instance attribute operations failed because `instance_pointer_is_instance()` requires `inst->cls` to still be a valid `PY_TYPE_CLASS`.

## [Investigation: Backend 1 must trace module global roots](../investigations/gc-backend1-module-global-roots.md)

- Test [CONFIRMED] — The focused gate fails before the fix:
- No.1 Register module object global slots as long-lived GC roots [CONFIRMED]
- CONFIRMED — The focused failing gate now passes under Backend #1 and the default backend:

## [Investigation: Backend #1 object registry performance gates](../investigations/gc-backend1-object-registry-performance.md)

- Test [CONFIRMED] — The full Backend #1 gate was observed with:
- No.1 Audit Backend #1 object-node lookup/removal complexity [CONFIRMED]
- No.2 Add an O(1) object-node index and unlink path for pcc-Python Backend #1 [CONFIRMED]
- CONFIRMED — The original three failures shared the same shape: object-heavy workloads
- CONFIRMED — After rebuilding `libpy_runtime_pcc_py.a`, the focused Backend #1 performance

## [Investigation: Backend 1 needs codegen roots for owned object locals](../investigations/gc-backend1-owned-local-frame-roots.md)

- Test [CONFIRMED] — Focused gate fails before the fix:
- No.1 Register owned local object allocas as one-slot GC frames [CONFIRMED]
- CONFIRMED — Focused gates pass:

## [Investigation: Backend 1 pcc-Python runtime should report pause telemetry](../investigations/gc-backend1-pcc-py-pause-telemetry.md)

- Test [CONFIRMED] — The focused pcc-Python pause-budget gate fails before the fix:
- No.1 Add shared runtime microsecond clock and mirror pause recording [CONFIRMED]
- CONFIRMED — The focused failing gate now passes:

## [Investigation: pcc-Python backend 1 aborts after allocation churn and collect](../investigations/gc-backend1-pcc-py-runtime-collect-abort.md)

- Test [CONFIRMED] — The failure was observed for all three compiler stages in the matrix:
- No.1 Diff pcc-Python backend #1 trace/collect path against C runtime [CONFIRMED]
- CONFIRMED — The failing regression now passes:

## [Investigation: Backend #1 reclaims a resurrected-then-dropped object one explicit collect late](../investigations/gc-backend1-resurrected-object-reclaim-lags-one-collect.md)

- Test [CONFIRMED] — Observed with the pytest command above on current source: 1 failed on
- any cycle [CONFIRMED]
- CONFIRMED — Instrumentation, since no existing facility could see mark-cycle state: one

## [Investigation: backend 1 threaded explicit collection invalidates a live list](../investigations/gc-backend1-threaded-explicit-collect-invalid-list.md)

- Test [CONFIRMED] — `tests/python/test_pcc1_threading_gc_runtime.py::test_pcc1_c_runtime_threads_and_explicit_gc_collect_all_backends[1]`
- No.1 Identify the first invalidation boundary with crash evidence [CONFIRMED]
- CONFIRMED — The macOS crash report placed the invalid-list call at worker offset 400: the

## [Investigation: Backend #1 drops transitive resurrection cycle](../investigations/gc-backend1-transitive-resurrection.md)

- Test [CONFIRMED] — The failing Backend #1 baseline was observed with:
- No.1 Compare Backend #1 finalizer path against default cycle collector [CONFIRMED]
- No.2 Root class-attribute globals during module execution [CONFIRMED]
- CONFIRMED — The failure was not simply "Backend #1 clears referents before running
- CONFIRMED — The focused Backend #1 test now passes:

## [Investigation: Backend 2 buffered write barrier](../investigations/gc-backend2-buffered-write-barrier.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Add a thread-local Backend #2 write-barrier buffer [CONFIRMED]
- CONFIRMED — The C runtime now keeps a small `_Thread_local` Backend #2 write-barrier

## [Investigation: Backend #2 CMS worker is unstable under focused and full GC gates](../investigations/gc-backend2-cms-worker-instability.md)

- Test [CONFIRMED] — The full GC gate failure was observed twice after the freeze API slice, both
- No.1 Protect CMS worker graph access and queue progress with a runtime lock [CONFIRMED]
- CONFIRMED — The focused Backend #2 gate now passes:

## [Investigation: Backend 2 CMS worker should stop and restart cleanly](../investigations/gc-backend2-cms-worker-shutdown.md)

- Test [CONFIRMED] — The focused lifecycle gate fails before the fix:
- No.1 Replace detached CMS worker lifetime with stop/join/restart [CONFIRMED]
- CONFIRMED — The focused lifecycle gate now passes:

## [Investigation: Backend 2 concurrent mark worker](../investigations/gc-backend2-concurrent-mark-worker.md)

- Test [CONFIRMED] — The focused gate has been observed failing:
- No.1 Add CMS worker ticket queue and mutator assist telemetry [CONFIRMED]
- CONFIRMED — The focused threaded backend #2 gate now passes:

## [Investigation: Backend #2 write barrier starts mark before root seeding](../investigations/gc-backend2-precycle-write-barrier.md)

- Test [CONFIRMED] — Observed failing before the fix:
- No.1 Gate Backend #2 write barrier on an active mark cycle [CONFIRMED]
- CONFIRMED — Backend #2 now only shades through the store barrier when a mark cycle is

## [Investigation: Backend #2 sweep/allocation TSan race](../investigations/gc-backend2-sweep-allocation-tsan.md)

- Test [CONFIRMED] — Observed before the fix with the focused TSan gate:
- No.1 Keep tracing sweep under STW during explicit collect [CONFIRMED]
- CONFIRMED — The focused TSan gate now passes:

## [Investigation: Backend #2 TSan allocation-size race](../investigations/gc-backend2-tsan-alloc-size-race.md)

- Test [CONFIRMED] — Observed before the fix with the formal TSan gate:
- No.1 Remove allocation scratch size and make allocation telemetry atomic [CONFIRMED]
- CONFIRMED — The focused TSan gate now passes:

## [Investigation: Backend #2 worker mark termination](../investigations/gc-backend2-worker-mark-termination.md)

- Test [CONFIRMED] — `tests/test_gc_backend_concurrent.py::test_concurrent_backend_worker_reaches_mark_termination_without_mutator_gc_step`
- No.1 Let the Backend #2 worker run STW mark termination [CONFIRMED]
- CONFIRMED — The worker now calls the shared tracing step with `finish_cycle=1`, so a

## [Investigation: Backend #2 worker should trace allocation work](../investigations/gc-backend2-worker-positive-work.md)

- Test [CONFIRMED] — `tests/test_gc_backend_concurrent.py::test_concurrent_backend_worker_traces_positive_allocation_work`
- No.1 Let CMS worker run bounded trace steps for positive work [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_runtime/src/py_gc_backend.c`.

## [Investigation: Backend 2 worker traces gray work](../investigations/gc-backend2-worker-tracing-step.md)

- Test [CONFIRMED] — The focused gate has been observed failing:
- No.1 Trace already-gray work from the CMS worker [CONFIRMED]
- CONFIRMED — The proposal fixes the focused gate and preserves the existing backend tests:

## [Investigation: Backend 3 class metadata slot rewrite](../investigations/gc-backend3-class-metadata-slot-rewrite.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Add borrowed class metadata barrier and slot rewrite [CONFIRMED]
- CONFIRMED — Implemented in both runtime implementations:

## [Investigation: Backend 3 copy oldification read barrier](../investigations/gc-backend3-copy-oldification-read-barrier.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Copy oldify remembered young children through forwarding/read barrier [CONFIRMED]
- CONFIRMED — The focused gate now passes:

## [Investigation: Backend #3 forwarded minor source cleanup](../investigations/gc-backend3-forwarded-minor-source-cleanup.md)

- Test [CONFIRMED] — The two pytest nodes above are the fix gate. They compile a C probe against
- No.1 Mark forwarded minor source inactive after forwarding install [CONFIRMED]
- CONFIRMED — C runtime and pcc-Python runtime mirror now share this policy:

## [Investigation: Backend 3 frame root slot rewrite](../investigations/gc-backend3-frame-root-slot-rewrite.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Scan frame roots with slot-aware Backend #3 promotion [CONFIRMED]
- CONFIRMED — Implemented `pcc_gc_promote_frame_roots()` in the C runtime and

## [Investigation: Backend 3 generational minor heap](../investigations/gc-backend3-generational-minor-heap.md)

- Test [CONFIRMED] — `tests/test_gc_backend_generational.py` is the focused gate for this slice.
- No.1 Add backend #3 minor-heap fast-path accounting and reset gate [CONFIRMED]
- CONFIRMED — The focused backend #3 gate now passes:

## [Investigation: Backend #3 generator and coroutine state slots](../investigations/gc-backend3-generator-coroutine-state-slots.md)

- No.1 Make generator/coroutine state GC-aware [CONFIRMED]
- CONFIRMED — The focused C runtime + pcc-Python runtime gate passed:

## [Investigation: Backend #3 minor bump arena allocation](../investigations/gc-backend3-minor-bump-arena.md)

- Test [CONFIRMED] — `tests/test_gc_backend_generational.py::test_generational_backend_c_runtime_uses_minor_bump_arena`
- No.1 Add a C-runtime minor arena behind backend #3 `pcc_gc_alloc()` [CONFIRMED]
- CONFIRMED — The implementation adds a Backend #3-only minor arena allocation path under

## [Investigation: Backend #3 minor refill promotion](../investigations/gc-backend3-minor-refill-promotion.md)

- Test [CONFIRMED] — `tests/test_gc_backend_generational.py::test_generational_backend_minor_refill_promotes_remembered_young_child`
- No.1 Run bounded Backend #3 promotion work at minor refill [CONFIRMED]
- CONFIRMED — The implementation extracts Backend #3's young-object promotion and

## [Investigation: Backend 3 non-list remembered slot rewrite](../investigations/gc-backend3-non-list-slot-rewrite.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Use slot-aware promotion for owned non-list referents [CONFIRMED]
- CONFIRMED — Landed in the C runtime and pcc-Python runtime mirror:

## [Investigation: backend 3 pcc-Python arena deallocator abort](../investigations/gc-backend3-pcc-py-arena-deallocator.md)

- Test [CONFIRMED] — `tests/test_gc_backend_generational.py::test_generational_backend_pcc_python_runtime_class_instances_deallocate_from_minor_arena`
- No.1 Route all `pcc_gc_alloc()`-backed pcc-Python deallocators through `pcc_gc_free_object_memory()` [CONFIRMED]
- CONFIRMED — Focused regression:

## [Investigation: backend 3 pcc-Python constructors clobber GC header flags](../investigations/gc-backend3-pcc-py-constructor-header-flags.md)

- Test [CONFIRMED] — Focused regression:
- No.1 Stop pcc-Python `pcc_gc_alloc()` constructors from resetting GC flags [CONFIRMED]
- CONFIRMED — Focused regression:

## [Investigation: Backend 3 pcc-Python copy oldification read barrier](../investigations/gc-backend3-pcc-py-copy-oldification-read-barrier.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Mirror C runtime scalar copy-oldification in pcc-Python runtime [CONFIRMED]
- CONFIRMED — The focused pcc-Python parity gate now passes:

## [Investigation: backend3 pcc-Python runtime minor arena parity](../investigations/gc-backend3-pcc-py-minor-arena.md)

- Test [CONFIRMED] — Focused regression:
- No.1 Mirror the C-runtime minor arena in pcc-Python runtime [CONFIRMED]
- CONFIRMED — The focused pcc-Python runtime-high gate now passes:

## [Investigation: backend 3 pcc-Python minor refill promotion parity](../investigations/gc-backend3-pcc-py-minor-refill-promotion.md)

- No.1 Add pcc-Python minor-refill remembered-set promotion coverage [CONFIRMED]
- CONFIRMED — Focused regression:

## [Investigation: pcc-Python GC3 oldify probes currently promote in place](../investigations/gc-backend3-pcc-py-oldify-current-regression.md)

- Test [CONFIRMED] — The two node ids above are the focused failing gate. Their C-runtime counterparts

## [Investigation: Backend 3 pcc-Python remembered slot rewrite](../investigations/gc-backend3-pcc-py-remembered-slot-rewrite.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Mirror remembered list slot rewrite in pcc-Python runtime [CONFIRMED]
- CONFIRMED — The focused gate now passes:

## [Investigation: Backend 3 pcc-Python threaded minor current](../investigations/gc-backend3-pcc-py-threaded-minor-current.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Move pcc-Python Backend #3 current minor block to C-hosted TLS [CONFIRMED]
- CONFIRMED — Focused pcc-Python runtime-high threaded gate:

## [Investigation: Backend #3 pcc-Python threaded object graph synchronization](../investigations/gc-backend3-pcc-py-threaded-object-graph.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Reuse the pcc-Python runtime-high graph lock for object graph registry access [CONFIRMED]
- CONFIRMED — Landed the graph-lock protection in `pcc/py_runtime/py/py_gc_backend.py`.

## [Investigation: Backend 3 remembered slot rewrite](../investigations/gc-backend3-remembered-slot-rewrite.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Rewrite remembered list slots during Backend #3 oldification [CONFIRMED]
- CONFIRMED — The focused C-runtime slot-rewrite gate now passes:

## [Investigation: Backend #3 suspended generator frame slot rewrite](../investigations/gc-backend3-suspended-generator-frame-slot-rewrite.md)

- No.1 Add focused suspended generator frame slot rewrite gate [CONFIRMED]
- CONFIRMED — The focused gate passed for both runtime implementations:

## [Investigation: Backend 3 threaded minor arena ownership](../investigations/gc-backend3-threaded-minor-arena.md)

- Test [CONFIRMED] — Observed before the fix:
- No.1 Make Backend #3 C minor arenas thread-owned [CONFIRMED]
- CONFIRMED — Focused TSan gate:

## [Investigation: GC3 million-vthread release is quadratic](../investigations/gc-backend3-vthread-million-release-quadratic.md)

- Test [CONFIRMED] — The failing production benchmark and native sample above confirm the regression.
- No.2 Use the existing GC3 arena-ownership bit as the physical split [CONFIRMED]
- CONFIRMED — The final C ownership and production-runtime group passed:

## [Investigation: backend #4 intermittent exit-time SIGSEGV — stale list item pointer under instance churn](../investigations/gc-backend4-churn-exit-list-item-uaf.md)

- Test [CONFIRMED] — Observed 2026-06-12 under
- No.3 Register container payload spans in the port containers (mirror the five C sites) [CONFIRMED partial — cures churn_bare 5/6 -> 0/12; full churn + compiler scale still red]
- No.6 ROOT CAUSE [CONFIRMED 2026-06-12]: lazy-heal vs page-destroy invariant violation. There is NO global heal/remap pass in either tier — slots heal lazily via the read barrier, which must READ THE OLD COPY'S HEADER (`_gc_relocation_candidate` loads flags) to decide to resolve forwarding. But `relocate_copy` calls `zpage_remove(from)` at COPY time (decrementing the old page's object_count), so on

## [Investigation: Backend 4 colored relocating forwarding](../investigations/gc-backend4-colored-relocating-forwarding.md)

- Test [CONFIRMED] — The focused gate has been observed failing:
- No.1 Add side-table forwarding and read-barrier slot healing [CONFIRMED]
- CONFIRMED — The focused backend #4 forwarding gate now passes:

## [Investigation: backend-4 concurrent flake — entry-loss lead RETRACTED as probe artifact](../investigations/gc-backend4-concurrent-entry-loss.md)

- Test [CONFIRMED] — `/tmp/b4attr/attr4.c` run personally 2026-08-26: rc=20,
- Update (2026-08-26, CONFIRMED capture): SWEEP_CANDIDATE set on the reachable survivor — Instrumented the ACTUAL overlap probe (last_ctx classification +

## [Investigation: Backend #4 dict relocation must retain owned tables](../investigations/gc-backend4-dict-relocation-owned-tables.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Add dict-specific relocation table ownership [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Backend #4 forwarded sources retain copied payload ownership](../investigations/gc-backend4-forwarded-source-payload-retirement.md)

- Test [CONFIRMED] — The command in `## Repro` was observed red on 2026-08-22:
- Adjacent source-owned metadata audit [CONFIRMED] — The same retirement boundary owns more than referent refcounts. The public
- CONFIRMED Proposal No.1 — The C and strict implementation now exists under one ABI and is called from

## [Investigation: Backend #4 forwarding table must be read under graph lock](../investigations/gc-backend4-forwarding-table-concurrency.md)

- Test [CONFIRMED] — The focused TSan gate fails before the fix:
- No.1 Lock C runtime forwarding install/read public paths [CONFIRMED]
- CONFIRMED — The focused C runtime TSan gate now passes:

## [Investigation: Backend #4 free hook must lock side-table removal](../investigations/gc-backend4-free-hook-side-table-concurrency.md)

- Test [CONFIRMED] — Before the fix, the focused TSan gate failed in 4.55s. TSan reported
- No.1 Lock C runtime free-hook side-table removal [CONFIRMED]
- CONFIRMED — Implemented the C runtime lock boundary change in

## [Investigation: Backend #4 instance relocation must retain owned fields](../investigations/gc-backend4-instance-relocation-owned-fields.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Add instance-specific relocation field ownership [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Backend #4 list relocation must own item arrays](../investigations/gc-backend4-list-relocation-owned-items.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Add list-specific relocation payload copy [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Backend #4 pcc-Python relocate copy size validation](../investigations/gc-backend4-pcc-py-relocate-copy-size.md)

- Test [CONFIRMED] — `tests/test_runtime_substrate_spike.py::test_pcc_python_relocate_copy_rejects_oversized_copy`
- No.1 Add allocation size to pcc-Python object nodes [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_runtime/py/py_gc_backend.py`.

## [Investigation: Backend #4 relocate copy must be single-use](../investigations/gc-backend4-relocate-copy-single-forward.md)

- Test [CONFIRMED] — Both pytest nodes listed in `## Repro` failed before the fix.
- No.1 Consume relocation entries after successful copy [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_runtime/src/py_gc_backend.c` and

## [Investigation: Backend #4 needs a copy relocation primitive](../investigations/gc-backend4-relocate-copy.md)

- Test [CONFIRMED] — `tests/test_gc_backend_relocating.py::test_colored_relocating_copy_moves_selected_simple_object`
- No.1 Add restricted copy relocation for selected simple objects [CONFIRMED]
- CONFIRMED — Implemented `pcc_gc_relocate_copy(obj, size)` in

## [Investigation: Backend 4 relocation lacks a mutator-quiescent payload phase](../investigations/gc-backend4-relocation-mutator-quiescence.md)

- Source audit [CONFIRMED] — The C and pcc-Python `pcc_gc_store_ptr` paths invoke the slot write barrier and
- Additional source audit: plain phase STW is insufficient [CONFIRMED] — A stop-the-world request only waits for registered threads to report a
- CONFIRMED helper transaction; real container tenure remains open — Proposal No.7's generic `pcc_gc_store_ptr` path is implemented in C and strict
- CONFIRMED Proposal No.8 for dict/set; list growth remains open — C and strict dict/set rehash now allocate and zero replacement tables outside
- CONFIRMED Proposal No.9 base replacement; retained arguments remain open — C and strict list growth now preserve backend0's direct `realloc` path and use
- CONFIRMED Proposal No.10 retained-root mechanics — C and strict append/insert root owner and item, extend roots destination and
- CONFIRMED Proposal No.11a — C and strict get/getitem now pair raw load with prepared retain under one graph
- CONFIRMED Proposal No.11b equality search and remove — C and strict `contains`, `index`, range-index, `count` and remove-search now
- CONFIRMED Proposal No.11b.1a — C and strict preserve backend0's direct clear path and give backends 1-4 an
- CONFIRMED Proposal No.11b.1b — C and strict now root owner/step/lo/hi, invoke each bound conversion once while
- CONFIRMED Proposal No.11b.2 — C and strict root destination/replacement/bounds, convert each bound once,
- CONFIRMED Proposal No.11c frontend-owned paths — All frontend `list.sort` branches now copy, clear the receiver, sort the hidden
- CONFIRMED Proposal No.12a — C and strict allocator owners mark only list/dict/set/tuple fresh on Backend 4.
- CONFIRMED Proposal No.12b property/classmethod/weakref — C and strict now mark property, classmethod and weakref fresh on Backend 4 and
- CONFIRMED Proposal No.13a — C production/oracle and strict owners pin tuple/list/dict borrowed results before
- CONFIRMED Proposal No.13b — C production/oracle and strict `PySequence_Fast_ITEMS` pin the exact fast owner
- CONFIRMED Proposal No.13c — A dedicated C/strict `pcc_capi_str_utf8_pinned` ABI owns C-API unicode raw
- CONFIRMED Proposal No.13d — Each C/oracle/strict Py_buffer now owns an active-list metadata node recording
- CONFIRMED Proposal No.14 py_obj_sorted callback-owner pins — The exact candidate-off control reproduced the 500-item failure, and an LLDB
- CONFIRMED Proposal No.14a — C and strict `py_obj_sorted` now register input and iterator locals as
- CONFIRMED Proposal No.15 under current callback semantics — C and strict `py_obj_min_max` now use updateable iterator/best/element slots,
- CONFIRMED Proposal No.16 — C and strict eager enumerate now root/reload iterator, item and boxed index,
- CONFIRMED Proposal No.17 — C and strict internal sequence/callable iterator paths now root every retained
- CONFIRMED Proposal No.18 — C and strict tuple methods share one rooted callback scan and C now releases
- CONFIRMED Proposal No.19 — C and strict dict get/contains now root before hash, retain/root equality
- CONFIRMED Proposal No.20 — C and strict set membership now root before hash, retain/root equality
- CONFIRMED Proposal No.21 target-side commit; strict source release open — C and strict set remove share rooted/restartable lookup and publish tombstone +
- CONFIRMED Proposal No.22 — C and strict set add now share the rooted/restartable lookup and publish the
- CONFIRMED Proposal No.23 — C and strict dict set now share `py_dict_rooted_op` / `_dict_rooted_op` with
- CONFIRMED Proposal No.24 — C `py_dict_del_rooted_slot` and strict `_dict_del_rooted_slot` publish the

## [Investigation: Backend #4 needs a relocation set](../investigations/gc-backend4-relocation-set.md)

- Test [CONFIRMED] — `tests/test_gc_backend_relocating.py::test_colored_relocating_selects_unpinned_relocation_set`
- No.1 Add object-level backend #4 relocation set [CONFIRMED]
- CONFIRMED — Implemented the object-level relocation-set side table in

## [Investigation: backend-4 relocation duplicates the shared object-slot contract](../investigations/gc-backend4-relocation-shared-slot-contract.md)

- Test [CONFIRMED] — The structural gate is confirmed red on the pre-change implementation. The
- No.1 Pair source and target slots through the shared visitor [CONFIRMED]
- CONFIRMED — Implemented one paired-slot pass in both runtime tiers. Per-type relocation

## [Investigation: Backend #4 relocation targets must wait for phase reset](../investigations/gc-backend4-relocation-target-phase-progress.md)

- Test [CONFIRMED] — Before the fix, both focused gates failed: C runtime and pcc-Python mirror
- No.1 Mark relocated targets until relocation-set reset [CONFIRMED]
- CONFIRMED — Implemented `PY_FLAG_GC_RELOCATION_TARGET` in the C runtime and the same

## [Investigation: Backend #4 scheduler queue free must heal forwarded entries](../investigations/gc-backend4-scheduler-queue-free-read-barrier.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Heal scheduler queue entries before free [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Backend #4 set relocation must retain owned entries](../investigations/gc-backend4-set-relocation-owned-entries.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Add set-specific relocation entry ownership [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Backend #4 needs stable relocation identity](../investigations/gc-backend4-stable-relocation-id.md)

- Test [CONFIRMED] — `tests/test_gc_backend_relocating.py::test_colored_relocating_stable_id_survives_forwarding`
- No.1 Add backend #4 stable identity side table [CONFIRMED]
- CONFIRMED — Implemented a `PyObject * -> int64_t` identity side table in

## [Investigation: Backend #4 step relocation must lock object graph traversal](../investigations/gc-backend4-step-object-graph-concurrency.md)

- Test [CONFIRMED] — Before the fix, the focused TSan gate failed in 5.67s with data races between
- No.1 Lock Backend #4 selection/copy step traversal [CONFIRMED]
- CONFIRMED — Implemented locked public wrappers plus `_unlocked` helpers for Backend #4

## [Investigation: Backend #4 step relocates simple objects](../investigations/gc-backend4-step-relocates-simple-object.md)

- Test [CONFIRMED] — Both pytest nodes listed in `## Repro` failed before the fix.
- No.1 Relocate selected simple objects during backend #4 step [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_runtime/src/py_gc_backend.c` and

## [Investigation: Backend #4 must heal task and scheduler queue references](../investigations/gc-backend4-task-queue-forwarding.md)

- Test [CONFIRMED] — The focused tests fail before the queue-pop fix:
- No.1 Heal scheduler queue entries through the relocation read barrier [CONFIRMED]
- CONFIRMED — Landed. `pcc_gc_scheduler_queue_pop_into()` now first reads the entry slot

## [Investigation: Backend #4 Task relocation must retain state slots](../investigations/gc-backend4-task-relocation-owned-slots.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Add Task-specific relocation slot ownership [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Backend #4 tuple relocation must retain owned items](../investigations/gc-backend4-tuple-relocation-owned-items.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.1 Add tuple-specific relocation item ownership [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: suspended heap-frame roots across all GC backends](../investigations/gc-coroutine-roots-backend-matrix.md)

- Test [CONFIRMED] — The matrix test passes:
- No.1 Add suspended heap-frame backend matrix [CONFIRMED]
- No.2 Balance owned refs when suspended generator frames store locals [CONFIRMED]
- CONFIRMED — The C runtime and pcc-Python runtime variants both pass backend 0..4. This

## [Investigation: default GC should collect closure capture cycles](../investigations/gc-default-closure-cycle-capture.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Release owned temporaries in native list append [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_frontend/codegen/layer1.py`.

## [Investigation: closure-cell cycle effectiveness gate](../investigations/gc-effectiveness-closure-cell-cycle.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Retarget closure-cell gate away from unsupported function attributes [CONFIRMED]
- CONFIRMED — Confirmed the original blocker with a direct binary run:

## [Investigation: generator self-cycle effectiveness gate](../investigations/gc-effectiveness-generator-self-cycle.md)

- Test [CONFIRMED] — The failing baseline was observed with:
- No.1 Release unused owned expression-statement results [CONFIRMED]
- CONFIRMED — The focused gate now passes:

## [Investigation: native __del__ finalizers are not called](../investigations/gc-finalizer-del-not-called.md)

- Test [CONFIRMED] — The command above was run on 2026-05-08 and produced:
- No.1 Treat `_ = owned_object` as discard after confirming lifetime source [CONFIRMED]
- CONFIRMED — Runtime logging showed that native finalizer dispatch already worked, but

## [Investigation: gc3/gc4 stage2 ~10-14x slower than gc0 — per-frame index-entry malloc](../investigations/gc-frame-index-entry-pool-perf.md)

- Test [CONFIRMED] — Per-backend byte-identity (`pcc2 == pcc3` after signature-normalize) is the
- No.2 Pool the `PccGcPtrIndexEntry` allocations (keep the hash) [CONFIRMED]
- No.2 Pool `PccGcPtrIndexEntry` (CONFIRMED)
- CONFIRMED — Built clean; verified **byte-identical on all 5 backends** (gc0/1/2/3/4). Stage2:
- Byte-identity (authoritative gate) — gc3 [CONFIRMED]:** `pcc1(new
- Byte-identity — gc4 [CONFIRMED]:** complementary run stressing the
- Update (2026-06-10): drain re-verify CONFIRMED — both queued items closed — The pool + TLS/cap/drain code is committed (6c71f9fd; drain call verified

## [Investigation: last-decref resurrection loses GC3/GC4 metadata](../investigations/gc-last-decref-resurrection-metadata.md)

- Test [CONFIRMED] — Source inspection confirms both C and strict mirrors use the invalid ordering;
- CONFIRMED Proposal No.1 — C and strict mirrors delay instance/user-class metadata removal through

## [Investigation: module-global finalizers do not run at shutdown](../investigations/gc-module-global-finalizer-shutdown.md)

- Test [CONFIRMED] — Observed locally on 2026-05-08:
- No.1 Emit module teardown functions that release object globals [CONFIRMED]
- CONFIRMED — The xfail marker was removed from

## [Investigation: scheduler queue root slots must be TSan-clean](../investigations/gc-scheduler-queue-root-slot-race.md)

- Test [CONFIRMED] — The focused TSan test fails on the pre-fix C runtime:
- No.1 Synchronize scheduler queue root slot writes [CONFIRMED]
- CONFIRMED — The focused TSan gate is clean after the refcount diagnostic and root-slot

## [Investigation: scheduler queue entries should own GC root slots](../investigations/gc-scheduler-root-queue-api.md)

- Test [CONFIRMED] — The focused C runtime and pcc-Python runtime mirror gates pass:
- No.1 Add scheduler root queue API [CONFIRMED]
- CONFIRMED — No.1 passes the focused Backend #3 C runtime and pcc-Python runtime mirror

## [Investigation: scheduler root registry must be safe under concurrent GC](../investigations/gc-scheduler-root-registry-thread-safety.md)

- Test [CONFIRMED] — The focused TSan test fails on the pre-fix C runtime:
- No.1 Lock scheduler root registry mutations [CONFIRMED]
- CONFIRMED — The fix removes the observed TSan race while preserving Backend #3 scheduler

## [Investigation: GC scheduler root slot registry](../investigations/gc-scheduler-root-slot-registry.md)

- No.1 Add scheduler root slot registry [CONFIRMED]
- CONFIRMED — The focused C runtime + pcc-Python runtime gate passed:

## [Investigation: Task completion must release waiter roots](../investigations/gc-task-completion-waiter-release.md)

- Test [CONFIRMED] — The focused tests fail before the fix:
- No.2 Clear waiter and invalidate weakrefs during backend-0 cycle collection [CONFIRMED]
- CONFIRMED — The focused C runtime and pcc-Python runtime mirror gate now passes:

## [Investigation: Task objects must expose GC-updatable state slots](../investigations/gc-task-state-slots.md)

- Test [CONFIRMED] — Initial run exposed a setup-sensitive failure in the new gate:
- CONFIRMED — Landed. The C runtime and pcc-Python runtime mirror now expose the same task

## [Investigation: task waiter cycles survive colored relocation](../investigations/gc-task-waiter-forwarded-shadow-weakref.md)

- Test [CONFIRMED] — The failure was observed with:
- No.3 Pin weakref targets and clear forwarded task shadows [CONFIRMED]
- CONFIRMED — The focused regression now passes in both C runtime and pcc-Python runtime

## [Investigation: cycle GC clears referents before resurrection finalizers](../investigations/gc-transitive-resurrection-clear-order.md)

- Test [CONFIRMED] — Observed locally on 2026-05-08:
- No.1 Confirm resurrection failure shape [CONFIRMED]
- No.2 Finalize unreachable user objects before clearing referents [CONFIRMED]
- CONFIRMED — The focused gate reproduces the same two failures recorded in
- CONFIRMED — The two focused xfail nodes now pass with `--runxfail`:

## [Investigation: trashcan dealloc skips finalizers in deep chains](../investigations/gc-trashcan-finalizer-count.md)

- Test [CONFIRMED] — Observed locally on 2026-05-08 as part of the 14-node GC closure rerun
- No.1 Confirm trashcan finalizer failure shape [CONFIRMED]
- No.2 Fix owned field traversal lifetime in frontend lowering [CONFIRMED]
- CONFIRMED — The failure is not a crash or timeout. The executable returns `0`, so the
- CONFIRMED — The deep-list probe now reports exact finalizer counts:

## [Investigation: weakref-call result used as an intermediate attr-access receiver (`r().v`) leaks a strong ref on #0](../investigations/gc-weakref-call-intermediate-attr-refleak-no-libpython.md)

- active — finding CONFIRMED + SCOPED 2026-05-31 (surfaced while probing the
- Test [CONFIRMED] — Manual repro above. Not yet a committed regression test (would be an inverted

## [Investigation: Close remaining GC xfail debt](../investigations/gc-xfail-closure-audit.md)

- Test [CONFIRMED] — The command above was run on 2026-05-08 and produced:
- No.1 Classify the 14 remaining GC xfails [CONFIRMED]
- No.2 Implement `gc.callbacks` without libpython fallback [CONFIRMED]
- No.3 Fix BUG #110 / function-scope string-list RSS leak [CONFIRMED]
- No.4 Complete `__del__`, shutdown-finalizer, resurrection, and trashcan semantics [CONFIRMED]
- No.5 Implement weakref lambda callbacks [CONFIRMED]
- No.6 Implement weak dictionaries [CONFIRMED]
- No.7 Replace the tracing-backend performance placeholder with a measured backend ratchet [CONFIRMED]
- CONFIRMED — All 14 xfailed GC tests still fail when run with `--runxfail`; none are
- CONFIRMED — The xfail marker was removed from
- CONFIRMED — Both RSS xfails use the same reproducer: create a local list, append 100
- CONFIRMED — The affected tests all require production finalizer semantics, including
- CONFIRMED — Focused repro before implementation:
- CONFIRMED — Command:

## [Investigation: GC0 bootstrap runtime hot paths](../investigations/gc0-bootstrap-runtime-hotpaths-2026-06-05.md)

- No.1 inline the hottest `py_obj.py` backend fast path sites [CONFIRMED WEAK]
- No.2 add prefix rejection before `_strs_eq` calls `strlen` [CONFIRMED]
- No.4 active-GC lease / matrix scheduling [CONFIRMED WEAK]
- [CONFIRMED WEAK]
- CONFIRMED WEAK — The split GC files are xdist-parallel, but running all five full bootstrap

## [Investigation: GC0 graph lock mirror drift — pcc-Python mirror pays the full TLS+CAS lock under threads-off builds](../investigations/gc0-graph-lock-mirror-drift-threads-off.md)

- resolved — No.1 CONFIRMED and accepted at the worker replay level on
- `pcc_threads_enabled() == 0`. [CONFIRMED]
- CONFIRMED — Implemented exactly as pre-registered; no other source changed. Verification

## [Investigation: GC1 bootstrap auto-step backend fast path](../investigations/gc1-bootstrap-auto-step-backend-fastpath-2026-06-05.md)

- No.2 avoid exported backend lookup inside GC1 auto-step dispatch [CONFIRMED
- CONFIRMED WEAK — Against the focused pre-patch GC1 profile:

## [Investigation: GC2 bootstrap pcc-Python object backend fast path](../investigations/gc2-bootstrap-py-obj-backend-fastpath-2026-06-05.md)

- No.2 avoid exported backend lookup in hot pcc-Python object ops [CONFIRMED]
- CONFIRMED — The pcc-Python object-op backend fast path reduced backend-dispatch sampling

## [Investigation: GC3 bootstrap pcc-Python GC dispatch fast path](../investigations/gc3-bootstrap-config-fastpath-2026-06-05.md)

- No.2 cache backend dispatch inside GC3 hot paths [CONFIRMED]
- CONFIRMED — Against the focused pre-patch GC3 profile:

## [Investigation: GC3 cpy-handle oldification loses foreign ownership](../investigations/gc3-cpy-handle-oldify-foreign-ownership.md)

- Source audit [CONFIRMED] — `pcc_gc_relocate_copy_supported_tag` includes `PY_TYPE_CPY_HANDLE` and calls
- Test [CONFIRMED] — During the GC3 TLS-cleanup A3b slice, a C true-pthread probe placed a cpy handle
- Update 2026-08-23 — exclusive transfer [CONFIRMED] — Proposal No.2 is accepted as an exclusive-transfer contract, not as a foreign

## [Investigation: GC3 `gc.collect()` undercounts a 10k two-node cycle workload (test_gc_collect_cycle_throughput red under PCC_GC_BACKEND=3)](../investigations/gc3-cycle-collect-undercount-10k-cycles.md)

- Test [CONFIRMED] — Observed 2026-08-07 under the pytest gate (`PCC_GC_BACKEND=3 ... -n0
- is not yet CONFIRMED.

## [Investigation: GC4 bootstrap pcc-Python GC config fast path](../investigations/gc4-bootstrap-config-fastpath-2026-06-05.md)

- No.2 reduce pcc-Python GC config/backend double-dispatch [CONFIRMED stage2]
- [CONFIRMED WEAK]
- CONFIRMED — GC4 stage2 compile wall improved in the focused probe:
- CONFIRMED WEAK — `pcc.cli_bootstrap` is the largest module in the bootstrap closure and the
- CONFIRMED WEAK — The next measured tail was `pcc.cli_bootstrap`, so the largest pcc1-native

## [Investigation: pcc2 under GC4 deadlocks on the minor graph lock — stage2-only, pcc1 is fine (suspected pcc1 codegen miscompile)](../investigations/gc4-pcc2-graph-lock-deadlock-stage2-miscompile.md)

- resolved — root cause CONFIRMED (self-host-only export-descriptor
- Test [CONFIRMED] — Observed repeatedly under the command above; hang stack (native, lldb attach):
- Update (2026-08-07, session 3 continued): root cause CONFIRMED — self-host-only `-> None` descriptor degradation to ("dyn",); fix = consume the schema `returns_none` bool in the plain-function extern paths — Fresh-chain verification first (current worktree, before today's fix): the

## [Investigation: GC4-only stack overflow — trashcan fails to defer the `__del__`-chain dealloc cascade (test_gc_trashcan segfaults under PCC_GC_BACKEND=4)](../investigations/gc4-trashcan-del-chain-dealloc-recursion-overflow.md)

- Test [CONFIRMED] — Observed 2026-08-07 via the pytest gate (`PCC_GC_BACKEND=4 ... -n0
- unrun, so the hypothesis is not yet CONFIRMED.

## [Investigation: backend #4 zpage bootstrap hot paths](../investigations/gc4-zpage-bootstrap-hotpaths-2026-06-02.md)

- Test [CONFIRMED] — The original focused gate now passes:
- No.2 index zpage owner/page lookups [CONFIRMED]
- No.3 make frame-leave index misses O(1) [CONFIRMED]
- No.4 make zpage allocation active-page-only [CONFIRMED]
- CONFIRMED — This removed the original `free N objects -> scan zpage list N times` shape.
- CONFIRMED — After No.2, sampling a stuck `gc4` stage2 process showed
- CONFIRMED — Before this change, a 5-second sample of a `pcc1` worker still showed:

## [Investigation: generator bodies iterating CPython-backed iterables — LLVM dominance failure, then runtime SEGV](../investigations/generator-cpython-iteration-dominance.md)

- Test [CONFIRMED] — The minimal repro produced the verifier failure (observed, x4 violations);
- No.1 Slot-spill the cpy iterator in `_emit_for_cpython_iter` [CONFIRMED — necessary, not sufficient]
- No.2 Clear compile-time diagnostic for cpy iteration inside generators [CONFIRMED]
- No.4 Second dominance site: gc-root reload (`pcc_gc_load_ptr` of `call.ret.root`) in native protocol-for inside generators (fortran.py's `for line in pushbackiter:`) [CONFIRMED]
- CONFIRMED — Observed (2026-06-10): minimal repro (user-class iterator + `for x in
- CONFIRMED — Observed (2026-06-10): minimal repro verifier failures 4 -> 0;

## [Investigation: closing a generator suspended in except skips finally](../investigations/generator-handler-close-skips-finally.md)

- Test [CONFIRMED] — Control failed twice under GC0 (6.85 s / 5.60 s). No compiler optimization had

## [Investigation: native generator frame planning misses nested expressions](../investigations/generator-native-ast-reflection-misses-expression.md)

- Test [CONFIRMED] — The native v4 reproduction failed. The retained tests in

## [Granule span lookup radix](../investigations/granule-span-lookup-radix.md)

- Current reproducer and baseline `[CONFIRMED]` — `benchmarks/python/granule_heavy_object.py` is the retained query workload.
- Proposal No.1 `[CONFIRMED]` — Use an immortal four-level radix of 4096 pointer slots per level. A 4 KiB key
- Result `[CONFIRMED]` — The four-level radix passed the complete 13-item granule/provenance/layout

## [Investigation: Harness agent loop exposes inconsistent self stack-map state at err.exit](../investigations/harness-agent-loop-self-stackmap-err-exit-join.md)

- Test [CONFIRMED] — The failure was observed on 2026-08-14 with the repository-root `pcc1`, dated
- No.2 Select the newest canonical verified pcc1 instead of the stale root artifact [CONFIRMED]

## [Investigation: Harness GUI needs a pcc1 that matches the current runtime import policy](../investigations/harness-gui-current-source-pcc1-runtime-policy.md)

- Test [CONFIRMED] — The exact command above failed on 2026-08-14 at

## [Investigation: host source installation wraps a project with top-level helpers](../investigations/host-source-install-project-helper-layout.md)

- Test [CONFIRMED] — The expanded host source-overlay test fails with the same missing

## [Investigation: initial installer did not bind helper manifests to stage receipts](../investigations/initial-pcc1-installer-receipt-binding.md)

- No.1 Validate retained stage evidence before creating an installation [CONFIRMED]
- CONFIRMED — The focused installer cases pass. A read-only validation of retained v84

## [Investigation: initial installer timeouts lose output and can orphan helpers](../investigations/initial-pcc1-installer-timeout-orphans.md)

- No.1 Use one in-process repository sampler for each command [CONFIRMED]
- CONFIRMED — Both timeout and externally terminated watcher tests retain output and terminal

## [Investigation: installed pcc1 helper imports can use the caller checkout](../investigations/installed-pcc1-helper-cwd-shadowing.md)

- Test [CONFIRMED] — The read-only interpreter probe above returned the mutable checkout before
- No.1 Disable Python's implicit unsafe path at the installed boundary [CONFIRMED]
- CONFIRMED — The focused generated-launcher execution regression passes. No native compiler

## [Investigation: native `int.from_bytes` rejects bytearray and memoryview](../investigations/int-from-bytes-byteslike-no-libpython.md)

- Test [CONFIRMED] — gtimeout 90s env -u LC_ALL uv run pytest -q -x -n0 \

## [Investigation: dynamic isinstance classinfo tuples return false](../investigations/isinstance-dynamic-classinfo-tuple.md)

- Test [CONFIRMED] — The standalone `/tmp/pcc_type_tuple_probe.py` compiled by v4 printed

## [libpy_runtime_pcc.a pure-C chain crashes (implicit decls + py_decref stack-free)](../investigations/libpy-runtime-pcc-archive-pure-c-chain-crashes.md)

- Symptom 1 [CONFIRMED FIXED]: FILE*/64-bit returns truncated by implicit declarations — First observed as: stage1 pcc1 (pure-C archive) built and passed its
- Discriminator [CONFIRMED 2026-07-31]: the same stage1 with
- Update 2026-07-31 evening: symptom 2 root cause localized [CONFIRMED] — Chain of evidence (each step observed, not inferred):
- pcc1-side residual [CONFIRMED by probe]: under the self-hosted compiler the

## [Investigation: Linux x86_64 self-backend docker harness has rotted](../investigations/linux-x86-64-docker-harness-rot.md)

- Test [CONFIRMED] — Observed 2026-06-12 under

## [Investigation: Linux zero-libc pcc-Python process entry](../investigations/linux-zero-libc-python-start.md)

- Test [CONFIRMED] — The focused unit suite covers the `_start(ptr)` signature, original stack

## [Investigation: mixed cpy/native list literal wrongly built as an all-CPython list](../investigations/list-literal-mixed-cpy-native-builds-cpython-list.md)

- Test [CONFIRMED] — After the fix, all four list-literal tests pass together:
- CONFIRMED — All four list-literal tests pass. Provably inert in no-libpython (no cpy values

## [Investigation: LLVM bootstrap structured-format auto-index ABI mismatch](../investigations/llvm-bootstrap-format-structured-spec-auto-idx-abi.md)

- Test [CONFIRMED] — The same failure was observed in the all-suite truth runner. Its LLVM bootstrap
- No.2 Preserve the typed auto-index across the helper return [CONFIRMED]
- CONFIRMED — The focused export-contract test failed first because both the return and final

## [Investigation: select a real pcc-native extension canary by generic gap cost](../investigations/m1-real-extension-canary-selection.md)

- No.1 Pin the lowest-cost real candidate that exercises PEP 489 [CONFIRMED]
- CONFIRMED — `simplejson` 4.1.1 is the only compared candidate that combines a single-TU

## [Investigation: import the real simplejson pcc-native extension and cross behavior](../investigations/m1-simplejson-pcc-native-import-behavior.md)

- Test [CONFIRMED] — The original full-package probe exposed several stacked generic frontend and
- No.1 Identify and minimize the first real pcc-native import boundary [CONFIRMED]
- No.2 Run the direct extension through the bounded host-runtime oracle [CONFIRMED]
- No.3 Bridge C-API imports to already-compiled pcc modules [CONFIRMED]
- No.4 Preserve cross-module exception and nested function metadata [CONFIRMED]
- No.5 Dispatch dynamic `.decode` by runtime receiver semantics [CONFIRMED]
- No.6 Lock the real positive and negative package boundaries [CONFIRMED]
- CONFIRMED — The full package compile fails with stable `PCC-PY-COMPILE-001` diagnostics
- CONFIRMED — With `PCC_RUNTIME_CC=cc` explicitly labeling the localization oracle, pcc1/self
- CONFIRMED — The compiled-module proxy path is generic and shares the registered module
- CONFIRMED — Cross-module exception subclasses retain constructor text and `args`; nested
- CONFIRMED — The minimized class-constructor regression passes, and the real package prints:
- CONFIRMED — Fresh pcc1 plus the real install passes both tests. The gate explicitly uses

## [Investigation: build the pinned simplejson sdist as pcc-native with no host](../investigations/m1-simplejson-pcc-native-source-build.md)

- Test [CONFIRMED] — The pinned pcc1 command deterministically returned success without an artifact
- No.1 Connect pcc1 sdist install to generic extension build execution [CONFIRMED]
- No.2 Implement the first missing public C-API surface revealed by the real compile [CONFIRMED]
- No.3 Implement the next coherent public C-API/header batch [CONFIRMED]
- No.4 Close the final three compile-time C-API gaps without faking writable Unicode [CONFIRMED]
- CONFIRMED — The install path now extracts sdists to a bounded staging tree, structurally
- CONFIRMED — The real pcc1 compile confirmed the static locator. The runtime now implements
- CONFIRMED — The strict self/no-libpython behavior extension passes with score 15: UTF-8
- CONFIRMED — The strict self/no-libpython behavior extension passes with score 15 for

## [Investigation: strict-self synthetic PEP 489 extension loses C-API exports to dead stripping](../investigations/m1-synthetic-pep489-capi-symbol-dead-strip.md)

- Test [CONFIRMED] — The focused pytest node above was observed failing in `5.43s`. It builds a
- No.2 Detect absolute `from package import extension` native imports [CONFIRMED]
- CONFIRMED — The structural alias check now applies to every `ImportFrom`, not only bare

## [Investigation: join C-API imports to the pcc-Python module object graph](../investigations/m2-capi-import-pcc-python-module-graph.md)

- Test [CONFIRMED] — The existing compiled-sibling bridge regression is green:
- No.1 Register compiled native modules behind the C-API import-by-name seam [CONFIRMED existing]
- No.2 Materialize native-builtin module objects through the same registry [CONFIRMED]
- CONFIRMED — The existing mechanism satisfies the generic pcc-Python sibling half of the
- CONFIRMED — The focused C extension imports all five builtin providers through

## [Investigation: NumPy L4 import under pcc1/self/no-libpython](../investigations/m2-numpy-l4-import.md)

- No.1 Compile and publish `numpy._globals` plus its ordinary relative closure [CONFIRMED]
- No.2 Expose `__name__` for registered C-extension type objects [CONFIRMED]
- No.3 Return `py_None` from successful pointer-ABI fallthrough/bare return [CONFIRMED]
- No.4 Preserve a registered C-extension type object's custom metaclass [CONFIRMED]
- No.5 Support generic `PyArg_Parse*` `p`, `O!`, and `O&` units [CONFIRMED]
- No.6 Populate/inherit `tp_alloc` during `PyType_Ready` [CONFIRMED]
- No.7 Dispatch C-extension `nb_int`/`nb_index` numeric slots [CONFIRMED]
- No.8 Support `Py_BuildValue` list containers [CONFIRMED]
- CONFIRMED — The generic compiled-module closure and subsequent focused runtime/frontend
- Test [CONFIRMED] — The existing `specdemo` extension fixture now publishes a type created by
- CONFIRMED — The focused regression passes (`1 passed in 6.25s`) and the adjacent
- CONFIRMED — The focused strict self/no-libpython regression compiled successfully and
- CONFIRMED — The focused regression first compiled and ran under strict self/no-libpython but
- CONFIRMED — The package-neutral focused regression failed at its no-argument call before
- CONFIRMED — Before the change, the focused readied base/derived fixture returned `4`: both
- CONFIRMED — The focused extension first failed with the same `unsupported operand type(s)
- CONFIRMED — The focused nested dict/list/tuple regression first failed on `[` with the same
- CONFIRMED — The focused fixture first failed with `AttributeError: marker`. After the
- CONFIRMED — The focused ratchet/head-truth tests pass (`19 passed in 0.23s`). Each real
- CONFIRMED — The focused singleton first failed on line 5 with `AttributeError:
- CONFIRMED — The focused singleton now passes (`1 passed in 0.48s`), including two class
- CONFIRMED — The focused test first failed at line 1 with `AttributeError: __new__`. With
- CONFIRMED — The focused regression first failed at the method definition with `NameError:
- CONFIRMED — The fake-extension closure regression failed before the scanner change and
- CONFIRMED — The package-neutral method was initially omitted. The tuple-to-vector adapter
- CONFIRMED — The separated metadata regression first raised `AttributeError: __module__`
- CONFIRMED — The direct-layout regression first crashed its child with `SIGSEGV` and now
- CONFIRMED — The mixed-call regression first ran the preceding plain FASTCALL calls and
- CONFIRMED — The custom dynamic-type test initially routed its high type tag through the
- CONFIRMED — The focused tuple/map test first raised `NameError: map` and now prints the
- CONFIRMED — The package-neutral custom type now exposes a read-only `T_LONG` member and
- CONFIRMED — The focused test first failed with the same `TypeError` as NumPy and now
- CONFIRMED — The reduced module-scope program first raised `AttributeError: sort`. The
- CONFIRMED (provider slice) — Before the provider existed, the strict executable raised `No module named
- CONFIRMED — The provider assertion and package-neutral IR regression pass (`2 passed in
- CONFIRMED — The regression first executed the same dynamic module path and failed with
- CONFIRMED — The minimized strict compile first failed with `PCC-PY-COMPILE-001` (`1 failed
- CONFIRMED — The focused test first constructed the instance successfully and then failed on
- CONFIRMED (host-cc runtime oracle) — The package-neutral program first failed at its module-scope bare read (`1
- CONFIRMED — The reduced type first compiled successfully and exited with signal 11 in the
- CONFIRMED — The reduced extension first failed with the same unsupported-operands
- CONFIRMED — The extended numeric fixture first passed subtraction but failed on
- CONFIRMED — Before the change, the reduced fixture completed but printed the semantically
- CONFIRMED — The expanded fixture first stopped at its first addition with the expected

## [Investigation: replay one Meson extension target through the pcc-native package executor](../investigations/m2-numpy-package-target-replay.md)

- Test [CONFIRMED red boundary] — Add a synthetic Meson/Ninja graph with one shared module, one recursive static
- No.1 Generic Meson target replay in `pcc.package build-exec` [CONFIRMED]
- Result [CONFIRMED] — The generic executor now selects the exact 136-object recursive target closure,

## [Investigation: mac_diff_app — pcc-GUI file-compare productization](../investigations/mac-diff-app-productization.md)

- Test [CONFIRMED] — `ret.py` → exit 5 (host + pcc1).

## [Investigation: method ABI rejects distinct equal literal aggregates](../investigations/method-literal-aggregate-abi-equality.md)

- Test [CONFIRMED] — The native compile test failed in 1.37s before executing an output. A smaller
- for identities, exact receipts and timings. No.1 is CONFIRMED at the full

## [Investigation: direct-link tests require an unprepared mutable C runtime](../investigations/multi-file-direct-link-tests-require-prebuilt-runtime.md)

- Test [CONFIRMED] — The first failure in `build/correctness-20260906-a/focused-02.stdout.log` is that
- No.1 Use the existing immutable C-runtime test fixture [CONFIRMED]
- CONFIRMED — The original failing node and all four affected execution shapes pass from the

## [Investigation: native array CLI silently quantizes float64 values](../investigations/native-array-float64-precision.md)

- Test [CONFIRMED] — The source differential was observed in the architecture review and issue191.

## [Investigation: native binary file write serializes bytes with `repr`](../investigations/native-binary-file-write-byteslike.md)

- Test [CONFIRMED] — `tests/python/test_native_file_readline_seek_tell.py::`
- No.1 Honor binary mode and write raw bytes-like payloads [CONFIRMED focused]
- CONFIRMED focused — The red-first binary roundtrip passed under the pcc-Python runtime (21.46s) and

## [Investigation: native file open returns NULL without an exception](../investigations/native-file-open-null-without-exception.md)

- Test [CONFIRMED] — The minimized test above was observed failing on 2026-07-22. LLVM and self
- No.2 Make native open obey the NULL-plus-exception contract [CONFIRMED]
- CONFIRMED — The following focused command passed after the change:

## [Investigation: label records can be reinterpreted as instruction text](../investigations/native-fragment-label-record-text-injection.md)

- Test [CONFIRMED] — The first malformed payload test fails with DID NOT RAISE EncodeError in 0.10s.

## [Investigation: native fragment PCO label publication adds compute work](../investigations/native-fragment-pco-label-publication-regression.md)

- Test [CONFIRMED] — Control/candidate CPU is 14.90/15.37s and 15.38/15.75s; all four PCO files
- No.1 Retain one narrowed section reference during typed label publication [CONFIRMED locally; full fragment acceptance open].

## [Investigation: IR class set in the integer global bridge fails under pcc1](../investigations/native-ir-class-set-in-global-bridge.md)

- Test [CONFIRMED] — The reduced program failed under `build/gateway-stage1-20260906-v4/pcc1`.

## [Investigation: native `json.load(file)` falls into a no-libpython function stub](../investigations/native-json-load-file-no-libpython.md)

- Test [CONFIRMED] — The focused run failed deterministically on 2026-08-31:

## [Investigation: native package installation truncates hyphenated project identities](../investigations/native-package-hyphenated-source-identity.md)

- Test [CONFIRMED] — The reduced test fails: native basename returns checkout while pyproject

## [Investigation: native sys and platform version queries use CPython fallback](../investigations/native-python-version-introspection-fallback.md)

- Test [CONFIRMED] — An uncached, temporary in-process capture retained the full pre-stub module:
- No.1 Give both observed version queries explicit native owners [CONFIRMED]
- CONFIRMED — The exact canary passed in 4.93s; the final three-case execution packet passed

## [Investigation: native re.compile pattern OBJECT (replace literal-alias rewriting; numpy `.cpy.attr.compile`~71)](../investigations/native-re-compiled-pattern-object.md)

- Test [CONFIRMED] — The off-mode compile above fails with `PCC-PY-COMPILE-001` on the current

## [Investigation: native subprocess failures lose CalledProcessError fields](../investigations/native-subprocess-called-process-error-returncode.md)

- Test [CONFIRMED] — The focused regression fails against current source after a successful strict
- Stacked Failure: raw POSIX wait status [CONFIRMED] — Fixing the exception object's shape exposed a distinct lower-level failure:

## [Investigation: native subprocess provider omitted from shallow multi-file closure](../investigations/native-subprocess-provider-omitted-from-shallow-multi-file-closure.md)

- Test [CONFIRMED] — The existing bootstrap-pair E2E is the public-interface regression. It passes
- stdlib expansion [CONFIRMED]
- CONFIRMED — The multi-file closure now scans admitted sources, including deferred function

## [Investigation: non-integration heavy xdist lanes oversubscribe compiler workloads](../investigations/nonintegration-heavy-xdist-lane-oversubscription.md)

- Test [CONFIRMED] — The scheduler contract in
- CONFIRMED — The focused scheduler contract is green. The affected files also pass together

## [Investigation: NumPy exposes SetType coercion and tuple-unpack lowering failures](../investigations/numpy-2-4-6-set-codegen.md)

- Test [CONFIRMED] — Package-neutral regressions now cover `list(set)`, set tuple-unpack and arity

## [Investigation: NumPy 2.5 core exposes five missing generic C-API surfaces](../investigations/numpy-2-5-capi-surface.md)

- Test [CONFIRMED] — `tests/python/test_pcc_native_extension_loader.py::test_pcc_native_extension_numpy_25_capi_batch_under_self_backend_no_libpython`
- No.1 Implement the five public C-API semantics in the generic shim [CONFIRMED]
- CONFIRMED — The focused generic extension test passes, and exact Meson target replay for

## [Investigation: numpy closure widened by dyn-flow references runtime symbols the pcc1 self-backend link cannot resolve](../investigations/numpy-dyn-reachability-selfbackend-link-gap.md)

- Test [CONFIRMED] — Observed 2026-08-07 on the freshly rebuilt stage1 pcc1
- Update 2026-08-07 (ROOT CAUSE CONFIRMED — not a reachability/selection gap) — Both discriminators failed identically (README np_demo.py itself fails with

## [Investigation: Real NumPy first import still emits libpython fallback](../investigations/numpy-first-import-libpython-fallback.md)

- Test [CONFIRMED] — Before this investigation file was opened, the opt-in gate failed because it still asserted the old `Layer 1 cannot marshal NoneType to CPython yet` marker. The captured compile output instead showed the current no-libpython fallback-scan marker above.
- CONFIRMED — The resynced opt-in boundary gate now passes while preserving the failure as the active blocker:
- CONFIRMED — Focused host-source IR regression:
- CONFIRMED 2026-05-27 textwrap literal-dedent shrinkage — Added literal-only native `textwrap.dedent(...)` constant folding for both
- CONFIRMED — Focused host-source regressions:

## [NumPy head-truth loader probe fails on cext re-import ("load once per process")](../investigations/numpy-loader-probe-cext-reimport-load-once.md)

- Test [CONFIRMED] — Step-2 failure and its real reason:
- Verification [CONFIRMED] — Loader-probe replica (`PCC_RUNTIME_CC=cc PCC_RUNTIME_HIGH=c`, head-truth

## [Investigation: acquired package install loses index provenance](../investigations/package-acquisition-install-provenance.md)

- Test [CONFIRMED] — The current-pcc1 failure was observed on 2026-07-22 after the native-open and
- No.1 Carry acquisition provenance alongside the immutable artifact path [CONFIRMED]
- CONFIRMED — The host acquisition/install regression and existing-Meson selector pass:

## [Investigation: package acquisition ignores the pcc target Python language version](../investigations/package-acquisition-target-python.md)

- Test [CONFIRMED] — The failure was reproduced with the installed pcc-native NumPy 2.5.1 site.

## [Investigation: package campaigns branch on a package-named profile](../investigations/package-campaign-generic-capability-profile.md)

- Test [CONFIRMED] — The no-special-case gate now rejects equality/inequality branches on the
- No.1 Move campaign behavior into the shared capability registry [CONFIRMED]
- CONFIRMED — Host/source campaign gates, adjacent package gates, and a strict current-source

## [Investigation: host acquisition stops at a PATH-shadowing Python without pip](../investigations/package-host-pip-discovery.md)

- Test [CONFIRMED] — The command above reproduced deterministically in 0.13 seconds. A focused

## [Investigation: host and pcc1 package manifests duplicate schema and wheel tags](../investigations/package-manifest-schema-wheel-tag-source-of-truth.md)

- Test [CONFIRMED] — `test_host_and_pcc1_share_wheel_tag_and_capability_contract` compares host and
- No.1 Add a self-host-safe package schema module [CONFIRMED]
- CONFIRMED — Host tests pass 8/8. A strict self/no-libpython pcc1 rebuilt from current source

## [Investigation: metadata helper import reintroduces standalone CLI bridges](../investigations/package-metadata-static-exports-missing.md)

- Test [CONFIRMED] — The baseline stopped after 17 passing tests in 63.93 seconds. This existing

## [Investigation: package dependency discovery consumes development environments](../investigations/package-requirements-scan-development-environments.md)

- Test [CONFIRMED] — `tests/python/test_package_metadata_scope.py` supplies one legitimate egg-info
- Repro [CONFIRMED] — The bounded scan still adopted foreign requirements when an artifact contained
- No.2 Require a marked source wrapper and preserve root ownership [CONFIRMED] — The filesystem and archive-member policies now require a direct `PKG-INFO`,

## [Investigation: optional package C source data is inferred as an extension](../investigations/package-source-data-inferred-as-c-extension.md)

- Test [CONFIRMED] — The reduced native installer test fails (1 failed in0.20s) because it calls

## [Investigation: pcc1 stage2 times out inside `codegen[pcc.py_frontend.codegen.layer1]`](../investigations/pcc-bootstrap-stage2-layer1-codegen-timeout.md)

- Test [CONFIRMED] — Confirmation criteria:
- `## Test [CONFIRMED]` against the 17:02 stage1 build
- on the post-commit binary; the `## Test [CONFIRMED]` claim
- last-log-line / exit code. Mark No.1 `[CONFIRMED]` only if

## [Investigation: `str +=` drops the old value and `len(call)` drops an owned list result](../investigations/pcc-codegen-ownership-leaks-str-iadd-and-call-result.md)

- active — CONFIRMED by micro-benchmarks; fix pending (test-first).
- Test [CONFIRMED] — main(): s = "abcdefghij"*2000; cur = ""; for ch in s: cur = cur + ch 3 MB
- No.1 release the old binding in str `+=` `[CONFIRMED]` — `assignment_statement_lowering._emit_augassign` now routes a Name target
- No.2 release owned exact-list call results consumed by `len` `[CONFIRMED]` — `builtin_type_attr_lowering._emit_len_call` now calls `_gc_release_if_owned`

## [Investigation: compiler-design references for faster, exact, maintainable PCC](../investigations/pcc-compiler-design-reference-audit.md)

- Test [CONFIRMED] — The 1800-second exact integration timeout and surviving work boundary above
- isolated A/B CONFIRMED]
- No.7 Separate replayable GPU schedules from semantic Kernel IR [CONFIRMED]
- CONFIRMED — The focused schedule and existing owner gates pass `14 passed`; the real pinned

## [Investigation: malloc flamegraph weighted allocation counts as bytes](../investigations/pcc-flamegraph-malloc-byte-weights.md)

- Test [CONFIRMED] — `tests/python/test_pcc_flamegraph_tool.py::test_malloc_call_tree_weights_reported_bytes_not_allocation_count`
- No.1 Parse the parenthesized allocation amount for heap/peak [CONFIRMED]
- CONFIRMED — gtimeout 120s env -u LC_ALL uv run pytest -q -x -n0 \

## [Investigation: transfer mold's packed and frozen-pass linker design into pcc's owned Mach-O path](../investigations/pcc-macho-packed-linker-mold-design-transfer.md)

- Test [CONFIRMED] — The performance failure is confirmed by the completed No.75 Stage2 profile and
- No.1 Consume validated indexed `NativeObject` directly [CONFIRMED]
- No.2 Fully validated read-only `.pco` codec view [CONFIRMED]
- No.6 Reuse packed validation summaries and avoid ordinary special-table materialization [CONFIRMED]
- CONFIRMED — Focused native-object, relocatable, executable, incremental, archive,
- CONFIRMED — The raw decoder retains symbol/section descriptors and fixed relocation spans.
- CONFIRMED — After explicit authorization, the remaining `/tmp/pcc-no71-profile` probe5

## [Investigation: `float(<dyn>)` and `_to_double` re-introduce libpython linkage in closed-world build](../investigations/pcc-py-codegen-float-dyn-closed-world.md)

- Test [CONFIRMED] — `tests/python/test_pcc_self_host_getattr_default.py` and friends already
- Code Change [CONFIRMED] — def _to_double(self, v: ir.Value, ty: Type) -> ir.Value:
- Code Change [CONFIRMED] — if isinstance(ty, DynType):

## [Investigation: nested closure comprehension target leak in `compute_free_names`](../investigations/pcc-py-codegen-nested-closure-genexpr-scope.md)

- Test [CONFIRMED] — `tests/python/test_pcc_self_host_getattr_default.py` — three e2e probes
- CONFIRMED 2026-05-11 — The unsound branch was located in

## [Investigation: `@property` return type does not propagate in pcc's type inference](../investigations/pcc-py-type-infer-property-return-type.md)

- Test [CONFIRMED] — `tests/python/test_py_cross_module_class_inference.py` (2 failing

## [Investigation: connect pcc-Python Thread objects to the owned pthread kernel](../investigations/pcc-python-thread-object-pthread-bridge.md)

- Test [CONFIRMED] — The raw kernel is confirmed green. The high-level fail-first gate compiled
- CONFIRMED at the runtime boundary; compiler performance transfer pending — The pcc-Python mirror now follows the C oracle's conditional start-handoff,

## [Investigation: pcc1 loses the C-ABI variadic decorator bit](../investigations/pcc1-c-abi-variadic-decorator-bit-lost.md)

- Test [CONFIRMED] — The command above fails deterministically. With
- No.1 Replace the generator-expression `any` with an explicit decorator loop [CONFIRMED, necessary but insufficient]
- No.2 Box native bools crossing the scaffold object-handle ABI [CONFIRMED]
- CONFIRMED, necessary but insufficient — The explicit scan makes the source-level variadic decision bootstrap-safe and
- CONFIRMED — Before the change, the compiler closure IR contained:

## [Investigation: constructor-initialized class fields lose cross-module types](../investigations/pcc1-cross-module-constructor-field-type-export.md)

- Test [CONFIRMED] — The regression must print the Store path, contain pointer-returning native method definitions, and avoid `py_dict_get_default` for the user-defined `Context.get` call. The rebuilt Harness durable Session integration remains the product-level gate.

## [Investigation: pcc1 direct `.pco` assembly rejects a tagged-int slow-path label](../investigations/pcc1-direct-pco-unknown-tagged-int-label.md)

- Test [CONFIRMED] — The failure was observed under the frozen v24 Stage1 strong canary. Durable

## [Investigation: pcc1 exact compiler-arena field projection loses the append4 method ABI](../investigations/pcc1-exact-compiler-arena-field-method-abi.md)

- Test [CONFIRMED] — The v70 and v71 source-frozen pcc1 binaries both reproduced the failure after
- No.3 Localize the first wrong field/method projection, then fix its generic frontend owner [CONFIRMED]
- CONFIRMED — Focused current-source evidence is green:

## [Investigation: pcc1 exact-string concat chains allocate one object per edge](../investigations/pcc1-exact-str-concat-chain-object-tax.md)

- Test [CONFIRMED] — The allocation diagnostic was observed under the command above: watchdog

## [Investigation: existing pcc1 Meson output incorrectly requires host Python](../investigations/pcc1-existing-meson-output-requires-host.md)

- Test [CONFIRMED] — The compiled-pcc1 failure above was observed on 2026-07-22 after a current
- No.1 Treat a matching existing Meson payload as a completed build [CONFIRMED]
- CONFIRMED — The focused selector regression passes:

## [Investigation: pcc1 traps while scaling finite float literals through raw integers](../investigations/pcc1-float-literal-bignum-scale-raw-int-trap.md)

- Test [CONFIRMED] — `tests/python/test_native_float_literal_precision.py::test_current_pcc1_parses_large_and_tiny_finite_float_literals` failed deterministically with subprocess return code `-5` before the parser changed.

## [Investigation: pcc1 rejects an admitted read-only GC query while rebuilding its runtime](../investigations/pcc1-freestanding-readonly-gc-registry-static-export.md)

- Test [CONFIRMED] — The realistic Harness build failure was observed on 2026-08-14. Host inspection returned `True` for `pcc_gc_relocation_set_size` in both `freestanding_readonly_gc_runtime_imports(source)` and `freestanding_allowed_external_symbols(source)`, proving the registry contents and source parser are correct outside the self-hosted cross-module path.

## [Investigation: pcc1 frontend rebuilds text that the self backend parses back into the same indexed kernel](../investigations/pcc1-frontend-direct-indexed-kernel-plane.md)

- Test [CONFIRMED] — The performance failure is confirmed by the two source-frozen profile JSONs:
- Exact call/GEP wrapper sub-proposal [CONFIRMED locally; whole-stage pending] — Two module-level exact wrappers make the receiver type explicit at the call

## [Investigation: generated type-tag alias loads crash current pcc1 during frontend module initialization](../investigations/pcc1-generated-type-tag-alias-module-init-pin.md)

- Test [CONFIRMED] — `tests/python/test_port_abi_constants.py::test_generated_compiler_type_tag_aliases_are_static_integer_constants` requires every generated compiler `PY_TYPE_*` alias to be a literal integer equal to `freestanding_abi_spec.ABI_SPEC`. Before the fix it fails with an empty actual assignment map because all 39 aliases are dynamic `ABI_CONSTANTS[...]` lookups.

## [Investigation: pcc1 dispatches a service registration through Effect.dispose](../investigations/pcc1-harness-effect-registration-inheritance-null-disposer.md)

- Test [CONFIRMED] — The realistic native self-check failure was observed on 2026-08-14. `projects/harness/tests/test_plugin_runtime.py` and `tests/python/test_harness_plugin_kernel.py` pass under CPython, so the failing dispatch is native-only. The final acceptance is the rebuilt Harness passing its reactive Provider withdrawal self-check and focused current-pcc1 lifecycle integration.

## [Investigation: L1CodeGen mixin state outside the host contract aliases host slots under pcc1](../investigations/pcc1-host-mixin-state-outside-contract-aliases-slots.md)

- Test [CONFIRMED] — Ground truth from a host multi-module text IR of the stage1 closure
- No.1 Complete the contract and ratchet it `[CONFIRMED]`
- `[CONFIRMED]` root cause and fix on the host side. pcc1 confirmation: the

## [Investigation: migrate pcc1's compiler hot path to an Indexed Function Kernel](../investigations/pcc1-indexed-function-kernel-native-data-plane.md)

- Test [CONFIRMED] — The performance failure and representation mechanism are confirmed by the
- second clean control gives 1.338x wall. Proposal No.1 is `[CONFIRMED]` at
- This is `[CONFIRMED]` as an end-to-end structural projection deletion with no
- This batch is `[CONFIRMED]` at the structural/representative-worker boundary.
- This proposal is `[CONFIRMED]` for the worker and structural claim. Whole
- `[CONFIRMED]` as performance-neutral structural deletion, not a speedup.
- The lazy block-entry candidate is `[CONFIRMED]`. It creates an active root
- at 305.40s/306.756B versus v20's 302.85s/307.113B. `[CONFIRMED]` and retained;
- footprint reduction with no claimed wall/CPU speedup. `[CONFIRMED]` as a
- projection deletion justify `[CONFIRMED]` as a structural/memory migration,
- GB footprint and `ff943e10...` output. `[CONFIRMED]` as a stable 0.65%
- GB and 29.50s. All output is exact `ff943e10...`. `[CONFIRMED]` as a
- `[CONFIRMED]` as a small measured win and the phase boundary required for the
- Stage1 is 310.152B/1.657GB. `[CONFIRMED]` as a small representation and
- GB; Stage1 is 310.085B/1.660GB. `[CONFIRMED]` as a 0.1% simplification.
- v73. `[CONFIRMED]` as a zero-extra-work lifetime/structure improvement: the
- /26.94s CPU and 3.105GB footprint. `[CONFIRMED]` at the hoist-correctness,
- The pending V79 transfer is now `[CONFIRMED]`. A cache-off Stage2 completes in
- V106 focused result `[CONFIRMED; whole Stage2 pending]` — The canonical path handoff now crosses the real pcc1 boundary. V106 pcc1
- Standalone pcc1 effect-summary codec `[CONFIRMED]` — The line codec now lives independently at
- `[CONFIRMED]` for measured light-lane admission. Unknown and 8 GiB budgets

## [Investigation: inline post-call error edges instead of one CFG triplet per call](../investigations/pcc1-inline-error-edge-data-plane.md)

- Test [CONFIRMED] — The representation failure above is confirmed on the exact retained AST and
- Test [CONFIRMED] — Evidence:
- `[CONFIRMED]` for the opt-in cleanup-plan-zero representation and its direct
- `[CONFIRMED]` for cleanup/try inline edges and the four fixes. Active: the
- `[CONFIRMED]` for the shared landing representation. Active: alternating
- Code Change `[CONFIRMED]` — `active_position` (value ID -> index in the active lists, swap-remove) makes

## [Investigation: pcc1 passes aggregate index zero as a NULL scaffold handle](../investigations/pcc1-ir-scaffold-extract-value-zero-index-null-handle.md)

- Test [CONFIRMED] — `tests/python/test_typed_c_abi_aggregate.py::test_current_pcc1_compiles_structural_f64_pair_export` is the minimized current-pcc1 regression. It failed deterministically before a code change.

## [pcc1-run linkage scanner fabricates a false "libpython]" edge on a clean artifact](../investigations/pcc1-linkage-scanner-fabricates-libpython-edge.md)

- Test [CONFIRMED] — `PCC_HOST_PYTHON=/usr/bin/false build/bootstrap/pcc1 -m pcc.package build-exec
- Verification [CONFIRMED] — Required gate:

## [pcc1: module-level `set` variable degrades to `dyn`, breaks `set` operators](../investigations/pcc1-module-level-set-var-degrades-to-dyn.md)

- CONFIRMED and FIXED (2026-07-16). Full self-host bootstrap (stage1→stage2→stage3,
- Test [CONFIRMED] — `tests/python/test_pcc1_python_smoke.py::test_pcc1_unannotated_set_binding_keeps_union_type`

## [pcc1 cannot compile a module-level `try` / `except`](../investigations/pcc1-module-level-try-except-unsupported.md)

- Test [CONFIRMED] — Nine lines. `pcc/py_frontend/pipeline.py` carries the same shape at lines

## [Investigation: native arena projection lost after field discovery expansion](../investigations/pcc1-native-arena-projection-loss-after-field-discovery.md)

- Test [CONFIRMED] — The same-input native compiler regression is confirmed, independently of input
- No.1 Preserve declared fields when constructor RHS inference is unknown [CONFIRMED at native worker boundary; full stages pending].

## [Investigation: native file objects do not implement the iterator protocol](../investigations/pcc1-native-file-iterator-protocol.md)

- Test [CONFIRMED] — Compile and run text and binary file iteration without libpython, compare output with Python 3, and assert `iter(file) is file`. Exhaustion must end the loop by raising and consuming `StopIteration`; iteration of a closed file must retain the existing `ValueError` from `readline()`.

## [Investigation: json ensure_ascii false falls back to a no-libpython stub](../investigations/pcc1-native-json-ensure-ascii-false-strict-stub.md)

- Test [CONFIRMED] — The new native JSON regression writes Chinese text and a supplementary-plane emoji with literal `ensure_ascii=False`, also combined with `sort_keys=True`, and compares output with CPython. Strict no-libpython compilation of `session_persistence.py` and the durable Session integration are the product gates.

## [Investigation: pcc1 native JSON string escapes](../investigations/pcc1-native-json-string-escapes.md)

- Test [CONFIRMED] — The failing test added for this investigation is:
- No.1 decode JSON string escapes in `py_json.c` [CONFIRMED]
- No.2 escape control characters and round-trip JSON floats [CONFIRMED]
- CONFIRMED — The focused no-libpython regression now passes:
- CONFIRMED — Focused native JSON regressions pass:

## [Investigation: pcc1 native json.dumps serializes tuples as null](../investigations/pcc1-native-json-tuple-serializes-null.md)

- Test [CONFIRMED] — The minimized source is `/tmp/pcc_json_tuple_repro.py`. Its guarded compile

## [Investigation: os.makedirs mode falls back to a no-libpython stub](../investigations/pcc1-native-os-makedirs-mode-strict-stub.md)

- Test [CONFIRMED] — `tests/python/test_native_os_makedirs.py` already covers recursive creation and `exist_ok`. The revised test also supplies `mode=0o700` and checks the leaf directory permission. The durable Session integration remains the current-pcc1 product gate.

## [Where pcc1 native code beats CPython and where it loses, measured per operation](../investigations/pcc1-native-vs-cpython-per-operation-cost.md)

- Diagnosis [CONFIRMED] and quantified per operation. One component fix landed

## [Investigation: pcc1 loses an owned object across an IfExpr local assignment](../investigations/pcc1-owned-ifexpr-local-transfer.md)

- Test [CONFIRMED] — The frozen pcc1 IR failure, host/pcc1 differential and ownership sequence are
- CONFIRMED — `control_flow_lowering._emit_if_expr` now gives every object branch exactly one

## [Investigation: `pcc1 -m pip install numpy` owned acquisition fails with PCC-PKG-ACQUIRE-DOWNLOAD-FAILED (hard 60s libcurl timeout)](../investigations/pcc1-pip-numpy-owned-acquire-60s-timeout.md)

- Test [CONFIRMED] — Probe matrix through the runtime intrinsic `os._pcc_http_download_to_file`
- No.1 Replace the fixed total-transfer timeout with libcurl stall abort [CONFIRMED]
- CONFIRMED — After `rm pcc/py_runtime/libpy_runtime*.a` + `scripts/bootstrap.sh --stage 1`:

## [Investigation: pcc1 pip-numpy chain regressed at HEAD — default acquire mode fails closed, and the compiled numpy app breaks at import on C-API shim holes](../investigations/pcc1-pip-numpy-runtime-import-capi-regressions.md)

- Test [CONFIRMED] — Both defects observed 2026-08-08 as above; the gate test fails at its
- Update 2026-08-08 — Defect 2 CLOSED: 12 C-API shim holes found and fixed in pcc-Python [CONFIRMED] — `import numpy` now runs end to end under strict no-libpython on **all five GC

## [Investigation: pcc1 pproxy GC4 live proxy readuntil returns empty](../investigations/pcc1-pproxy-gc4-live-proxy-readuntil-empty.md)

- Test [CONFIRMED] — The live proxy/curl repro above has been observed on 2026-06-19. Later
- No.4 DynType `pop` dispatch routes `list.pop(0)` to `dict.pop` under pcc1 [CONFIRMED]
- No.5 hoisted nested-name prefix match resolves builtin `filter` to `__nested_filter_cond` [CONFIRMED]
- No.6 `next(filter(predicate, iterable), default)` has no no-libpython lowering [CONFIRMED]
- No.7 nested async `HTTP.connected` formats captured request strings as first characters [CONFIRMED]
- No.8 native `urllib.parse.ParseResult` lacks `hostname` [CONFIRMED]
- No.9 native asyncio accepted sockets inherit nonblocking state [CONFIRMED]
- No.10 blocking asyncio channels deadlock full-duplex SOCKS5 forwarding [CONFIRMED]
- No.11 lambda free-var analysis treats a forwarded function parameter as the hoisted nested helper under pcc1 [CONFIRMED]
- CONFIRMED — Before the fix, host pcc emitted `py_list_pop(xs, 0)` for `xs.pop(0)`, while
- CONFIRMED — The minimized scheduler shape emitted IR where `filter(filter_cond, rserver)`
- CONFIRMED — After No.5, the focused regression exposed the real missing builtin surface as
- CONFIRMED — The temporary-copy live probe proves the malformed data appears before
- CONFIRMED — After No.7, the exact live proxy no longer sent the truncated HTTP request.
- CONFIRMED — The SOCKS5 greeting path failed when the client waited for the server's
- CONFIRMED — After No.9, SOCKS5 handshake and connect succeeded, but payload forwarding
- CONFIRMED — Host pcc now emits the correct IR for the minimized shape:

## [Investigation: pcc1 qualification misses effective cache and discovery selection](../investigations/pcc1-qualification-cache-and-project-selection.md)

- Test [CONFIRMED] — Before the fix, the real environment-addopts regression observed only the cached
- No.1 Bind effective cache modes and discovery configuration [CONFIRMED]
- CONFIRMED — `gtimeout 60s env -u LC_ALL uv run pytest tests/python/test_install_pcc1_toolchain.py -q -x -n0 --tb=short`

## [Investigation: pcc1 qualification trusts the sampler parent environment](../investigations/pcc1-qualification-child-environment-binding.md)

- Test [CONFIRMED] — Before the fix, the receipt regression failed with `DID NOT RAISE ValueError`.
- No.1 Bind qualification to the actual pytest environment [CONFIRMED]
- CONFIRMED — The real child override exits pytest successfully but fails qualification.

## [Investigation: pcc1 qualification accepts a filtered full-suite collection](../investigations/pcc1-qualification-effective-pytest-selection.md)

- Test [CONFIRMED] — Before the fix, that regression failed with `DID NOT RAISE ValueError`.
- No.1 Validate effective pytest selection instead of argv spelling [CONFIRMED]
- CONFIRMED — Joined flags, ini/override addopts, ignore/deselect filters and missing fields

## [Investigation: qualification source validation rejects a real pytest checkout](../investigations/pcc1-qualification-real-pytest-checkout.md)

- Test [CONFIRMED] — It failed with `qualification source is not the complete build closure` before
- No.1 Compare the compiler build closure while admitting the test checkout [CONFIRMED]
- CONFIRMED — `test_real_pytest_receipts_flow_through_record_gate_and_qualify_cli` runs actual

## [Investigation: rollback rejects a broken promoted compiler](../investigations/pcc1-rollback-damaged-active-toolchain.md)

- Test [CONFIRMED] — The corrupt-binary case failed before the fix with an installed identity
- No.1 Verify the restored predecessor and identify the damaged current version by journal/link [CONFIRMED]
- CONFIRMED — Corrupt and absent new binaries recover. Changed predecessors and an external

## [Investigation: pcc1 startup fails after raw GC globals enter runtime ABI type registry](../investigations/pcc1-runtime-abi-gc-global-type-registry-startup.md)

- Test [CONFIRMED] — The fresh stage1 command above is the compiled-stage reproducer. A focused
- No.4 Defer pipeline's runtime ABI import until strict validation executes [CONFIRMED]
- CONFIRMED — The focused fresh-process import regression passes, and the current-source v3

## [Investigation: pcc1 self-host loses `_generator_ctx` after first assignment](../investigations/pcc1-self-host-generator-ctx-slot.md)

- Test [CONFIRMED] — /tmp/pcc1_stack /tmp/gen_tiny.py -o /tmp/gen_tiny.out --backend self \
- No.3 use list-as-stack for `_generator_ctx` [CONFIRMED]
- CONFIRMED — $ stage0 → /tmp/pcc1_stack: build OK
- Test [CONFIRMED] — $ /tmp/pcc1_v2 --backend self --python-libpython=off /tmp/gen_tiny.py \

## [Investigation: self-hosted pcc1 SIGBUSes at startup in the compiled-module init registry](../investigations/pcc1-self-host-module-init-startup-sigbus.md)

- Test [CONFIRMED] — `gtimeout 30s ./build/bootstrap/pcc1 --help` reproduces rc 138 on every
- operand is SP [CONFIRMED]
- Update 2026-08-15 — root cause CONFIRMED
- Root cause [CONFIRMED] — `pcc`'s own A64 assembler mis-encodes `add`/`sub` when an operand is `SP`.

## [Investigation: the Stage2 small lane's in-process native-object path is quadratic under pcc1](../investigations/pcc1-small-lane-native-object-worker-memory.md)

- Test [CONFIRMED] — pcc1 v4 NATIVE_OBJECT=0 10.7s 1.29 GiB (.s)
- No.1 chunks + one join instead of bytearray growth `[CONFIRMED]`
- No.2 `bytes.join` runtime primitive `[CONFIRMED]`
- No.3 struct.unpack_from zero-copy for bytes `[CONFIRMED]`
- CONFIRMED — Host: 31 assembler/object tests green; module 151 `.pco` byte-identical.
- CONFIRMED — `tests/python/test_native_bytes_join.py` port + cc + GC0..4 equal CPython;
- CONFIRMED — struct tests green; pcc1 (Stage1 v7): 28.6 s -> 19.5 s, peak unchanged at
- Update 2026-09-04 — emitter line transport `[CONFIRMED structural; speed weak]` — The first vertical transport step removes the complete-module assembly string
- Update 2026-09-04 — packed stackmap section `[CONFIRMED]` — The v19 ASM-only replay bounds assembly plus NativeObject publication at
- `[CONFIRMED]`; the remaining roughly 23.7s above ASM-only is still active and
- Update 2026-09-04 — direct Section-to-packed PCO `[CONFIRMED]` — A full v20 profile assigned 19.00% inclusive to native-object validation,
- instructions 548.264B->389.073B and RSS 4.086->3.977GB. `[CONFIRMED]`.
- Update 2026-09-04 — AArch64 text-entry scalar arena `[CONFIRMED structural]` — `assemble_text_lines` no longer builds one `(kind, payload)` Python tuple per
- Update 2026-09-04 — structured unscaled load/store `[CONFIRMED]` — The structured transport now carries final scalar words for all emitter-owned
- instructions 389.051B->375.069B and RSS 3.977->3.441GB. `[CONFIRMED]` for the
- Update 2026-09-04 — structured move family `[CONFIRMED]` — `mov/movz/movk` add 38,319 scalar-word records to the 72,970 unscaled
- B->369.166B and RSS 3.441->3.242GB. `[CONFIRMED]` for deterministic
- Update 2026-09-04 — structured calls and current-source Stage2 `[CONFIRMED; task open]` — The relocation-bearing transport now carries `(line_index, word,
- Update 2026-09-05 — indexed native exports `[CONFIRMED; wall still memory-bound]` — One indexed export file now carries per-module payloads, exact AST-derived
- Update 2026-09-05 — packed process boundary `[CONFIRMED structural; task open]` — A versioned direct-module sidecar now carries the pre-stackprep indexed kernel
- Update 2026-09-05 — unified indexed phases `[CONFIRMED; task open]` — The v49 runner removes the five sequential lane-pair barriers and executes one
- Update 2026-09-05 — exact admission and link width `[CONFIRMED; task open]` — Full-population sidecar floors plus deterministic first-fit reduce source-
- CONFIRMED — bounded native prerequisite, not full buffer closure — focused tests and the separately logged 227-module contextual gate pass.
- CONFIRMED — incremental driver prerequisite — focused tests and the 227-module contextual gate pass. v72 is
- CONFIRMED — module-level native producer — v73 completes Stage1 at 165.83s / 674.49 tree CPU seconds, libSystem-only;

## [Investigation: `pcc1 -> pcc2` emit throughput and native-worker memory](../investigations/pcc1-stage2-emit-throughput-and-memory.md)

- Findings [CONFIRMED]
- Why it is slow: generated code quality, not the runtime [CONFIRMED] — The decisive measurement. This function:
- The binary is 90% stack maps, and they are 97% redundant [CONFIRMED] — Section breakdown of a current `pcc1` (904 MB total):
- No.1 Replace the three hot token-classifier regexes [CONFIRMED, landed]
- No.4 Word-at-a-time `calloc`/`memset` [CONFIRMED, landed]
- No.6 Call-free tagged fast path in `py_int_to_i64` [CONFIRMED, landed]
- fields [CONFIRMED, landed — removed the then-#1 hotspot]
- No.14 Elide barriers for compile-time tagged constants [CONFIRMED, landed]
- Stack-map interning landed: 904 MB -> 196 MB [CONFIRMED] — Format version 2. Records keep their 32-byte shape and reuse the trailing
- Allocator size classes raised 2048 -> 16384 [CONFIRMED] — `__mmap` was the **#2 leaf (536 of ~10000 samples)** in a stage2 *frontend
- Where stage2 actually spends its time [CONFIRMED] — Sampling stage2 while it ran, rather than reasoning from the emit worker:
- Update — No.10 CONFIRMED: the frontend, not the runtime, owned the compile — The four earlier fixes targeted runtime *leaf* symbols and did not move the
- Update — No.11 CONFIRMED: every GC barrier ran a linear type-object scan — Continuing the caller-attribution work from No.10 on the stage2 cost (pcc1
- Update — No.12 CONFIRMED: scaffold constants were boxed and immediately unboxed — Found by reading emitted IR, not by profiling — the leaf profile only says
- Update — No.14 ROOT CAUSE CONFIRMED: every dynamic method call leaked two objects — The `py_func_call_kwargs` attribution in No.13 was a dead end (an ancestor
- Hypothesis 3 — CONFIRMED: the packed state left the tagged-int lane — `_stable_id_feed` returned the two 32-bit limbs packed as
- Update — No.17 CONFIRMED: `sort(key=...)` lowered to insertion sort — The oversized shard that had never once finished now emits in **200 seconds**.
- Update No.18 -- root cause found: pcc1 could not compile ANY `def` [CONFIRMED] — The `ssa-dominance` verifier errors reported in the previous update were
- Update No.19 -- the stage2 emit bottleneck: a quadratic in `collect_block_local_last_uses` [CONFIRMED] — With the `def`-compiling defect of No.18 fixed, stage2 reached the self-backend
- Update No.20 -- two more O(n^2) scans in the emit path, found by profiling instead of guessing [CONFIRMED] — `sample` on a live worker pointed at `_dot_numeric_text_key_id`, and a
- Update No.21 -- pcc2 could not print any integer; root cause chain and where it still stops [CONFIRMED to a specific edge] — `stage2 exit=0` is not "pcc2 is correct". On an **idle** machine (an earlier
- Update No.22 -- the big-literal root cause, one layer fixed and one layer named [CONFIRMED] — Continuing No.21. The loss is **not** below the frontend IR after all -- the port
- Update No.24 -- big integer literals FIXED; four sites, one principle [CONFIRMED] — All six shapes of the ladder from No.23 are now correct, verified on
- Update No.25 -- the real gate is `boxed_world=False`, not any single emit site [CONFIRMED] — No.24's fix works for **user programs compiled by the host** (all six shapes), and
- Update No.27 — bottleneck relocated with a trustworthy profiler [CONFIRMED] — Every earlier profile in this file resolved addresses by hand and at least two
- Update No.28 — Python-frontend line information [CONFIRMED] — The C frontend has emitted `DICompileUnit`/`DISubprogram`/`DILocation` for a
- Update No.35 — the callee signature cache is [CONFIRMED] under pcc1 — The change this file recorded as **[DENIED]** now measures a win, because the
- [CONFIRMED] 1.13x on pcc1, byte-identical output, gates green. The first
- Update No.38 — the host-side bottleneck was a character loop, 14.7x off it [CONFIRMED] — The flame graph of a whole stage1 build (428 self-profiling processes, 9632
- [CONFIRMED] 14.7x off the top host-side consumer, byte-identical, gates green.
- Two [CONFIRMED] CPU wins with direct attribution; memory unchanged and still the
- `[CONFIRMED]` The cost was call depth, not the data structure — One provenance question ran five pcc-compiled calls:
- Update No.55 — the relocation read barrier was paying for provenance under GC0 `[CONFIRMED]` — Update No.54 fused the objecthood predicate and named the span lookup as the
- `[CONFIRMED]` and kept. Tracked as
- Update No.56 — compiled mem2reg was quadratic in alloca candidates `[CONFIRMED]`, 1.71x pcc1 frontend — Update No.55 gated the read barrier. With that in, a current-source pcc1 was
- `[CONFIRMED]`, kept, tracked as `PERF-P1-MEM2REG-CANDIDATE-SCAN-QUADRATIC`.
- `[CONFIRMED]` — one pass instead of three — `_strs_eq` compares raw C names, so there is no cached length to read. It ran
- Single pass `[CONFIRMED]` and kept as
- Update No.60 — current cold/hot boundary and canonical-error layout quadratic `[CONFIRMED]` — Current frozen source `eab5407f64bce65451e9a2dc2216d8d48636f886`, pcc1
- Proposal No.60 `[CONFIRMED]` — Build a stable integer bucket from block text to original block index, maintain
- Update No.61 — Proposal No.60 result and full fixed point `[CONFIRMED]` — The deterministic 240-block reference test passed with exact layout and edge
- Proposal No.60 is `[CONFIRMED]` and retained. It removes 246.274 s (18.16%)
- Update No.62 — reuse block-local last-use analysis `[CONFIRMED]` — After Proposal No.60, the largest sub-2,000,000-byte safe shard is
- Proposal No.62 `[CONFIRMED]` — Add one optional `ParsedFunction.block_local_last_uses` analysis field.
- Update No.63 — Proposal No.62 result and attribution boundary `[CONFIRMED]` — The host check kept assembly byte-identical, reduced cProfile calls from
- deselected, fallback baseline 32 passed. Proposal No.62 is `[CONFIRMED]` and
- Update No.66 — decouple runtime IR passes from bounded bootstrap passes `[CONFIRMED]` — The invalid 793.029 s Stage2 exposed a real configuration owner even though it
- Proposal No.66 `[CONFIRMED]` — Add `PCC_RUNTIME_PYTHON_IR_PASSES`, default `default`, to the pcc-Python
- `[CONFIRMED]` and retained. This is the valid replacement for the retracted
- The final wire-format stack-map validator is independently `[CONFIRMED]` at
- Update No.71 — the emit worker re-profiled, and pcc1 was skipping a whole phase `[CONFIRMED]` — Resuming after the No.70 pause, the goal was raised: Stage2 should become at
- Update No.75 — the location fingerprint costs more than the merge it avoids `[CONFIRMED]` — With the reload red herring cleared (No.74), the oversized worker's real
- Proposal No.75 `[CONFIRMED on the pre-registered line]` — Replace the id-tuple fingerprint with an **XOR of the group ids**. XOR is
- Update No.79 (2026-08-27): oversized admission waves LANDED — lane 177.6 s -> 119.4 s [CONFIRMED] — No.78's schedule is implemented as the default: `pack_admission_waves`
- Update No.82 — No.72 ASCII name fast path `[CONFIRMED]` — The first prefilter driver was invalid and is excluded: pcc1 compiled it with
- No.89 result `[CONFIRMED]` — The final host differential covers all 416 call-bearing frozen items and all
- Update No.99 — direct `.pco` publication inherited the wrong concurrency owner `[CONFIRMED root cause; fix under gate]` — The 2026-08-31 kernel panic is now tied to one exact retained Stage2 tree, not
- Update 2026-09-05 — process-split scheduler and packed instruction plane `[CONFIRMED; investigation remains open]` — Later task-board slices supersede the paused v10 execution shape without
- Packed stack-map record arena and v58 Stage2 `[CONFIRMED; target open]` — `py_ast` carries 46,625 final 32-byte stack-map rows. Replacing one
- Update 2026-09-06 — tuple guard transfer, complete-path owners, struct native decode `[CONFIRMED; investigation remains open]` — Two accepted runtime/provider slices moved the source-frozen Stage2 from
- Architectural owner: raw pointers are `TYPE_DYN`, so provenance is probed on every refcount `[CONFIRMED; vertical slice named]` — Complete-path attribution on v81/v82 pcc1 workers puts the provenance family

## [Investigation: pcc1 stage-2 stack-map CFG lookup misses an equal block label](../investigations/pcc1-stage2-stackmap-block-label-hash-miss.md)

- Test [CONFIRMED] — The full failure and the single-function reduction were observed on
- CONFIRMED — The first block lookup fix exposed three more instances of the same native

## [Investigation: with reloads restored, pcc1 rejects its own IR — `self` outlives its active root in 19 functions](../investigations/pcc1-stage2-stale-managed-self-outlives-root.md)

- Test [CONFIRMED] — Observed under the command above. Not yet reduced to a small reproducer — the
- No.1 diff one failing module's IR, host pcc versus pcc1, before changing anything `[CONFIRMED]`
- No.2 localize the wrong `_root_group` locations under pcc1 `[CONFIRMED]`
- No.1 result — localized to the analysis, not the frontend `[CONFIRMED]`
- No.2 result — narrowed to ONE group with empty locations `[CONFIRMED]` — The reload planner knew the value and the offsets but not where in the CFG it
- No.3 result — ROOT CAUSE: `set` silently drops elements whose probe sequence cycles `[CONFIRMED]`

## [`str.find` was O(n) in its own result conversion — stage2 2.49x](../investigations/pcc1-str-find-quadratic-codepoint-conversion.md)

- Root cause [CONFIRMED] — A flame graph of a `--pcc-self-backend-split-worker` during stage2 put **95.6%
- [CONFIRMED] stage2 2.49x, byte-offset semantics unchanged. Memory is unaffected
- Five attempts at the per-operation tax now: signature cache 1.13x [CONFIRMED],

## [Investigation: backend 0 double-free / refcount underflow under high-scale threaded explicit gc.collect](../investigations/pcc1-threaded-explicit-gc-backend0-double-free-highscale.md)

- Test [CONFIRMED] — `test_pcc1_c_runtime_threads_and_explicit_gc_collect_all_backends[0]` observed
- ROOT CAUSE (CONFIRMED) — The backend-0 cycle collector collects an object whose **raw refcount is already
- No.2 free-trace: is the collector freeing in_root=1 / gc_refs>0 objects? [CONFIRMED — frees rc=0/gc_refs=0/in_root=0 objects]
- CONFIRMED (double-free) — After this fix: standalone repro 20 runs -> ok=13, timeout=7, **abort=0**
- CONFIRMED (backend 0) — After the corrected hang fix: standalone host-pcc repro **25/25 ok, 0 timeout,
- CONFIRMED (backend 0 + hang, official gates) — `tests/python/test_pcc1_threading_gc_runtime.py::test_pcc1_c_runtime_threads_and_explicit_gc_collect_all_backends[0]`

## [pcc1 self-host miscompile: tuple-unpack corrupts codegen `_str_counter`](../investigations/pcc1-tuple-unpack-self-host-str-counter-corruption.md)

- Fix applied [CONFIRMED] — `py_class_new` now marks user class objects `PY_FLAG_IMMORTAL` (offset 12),
- Test [CONFIRMED] — Minimal repro (`f` contains a tuple-unpack):

## [Investigation: unannotated cross-module returns use mismatched ABIs](../investigations/pcc1-unannotated-cross-module-return-abi.md)

- Test [CONFIRMED] — Add strict no-libpython multi-file regressions for an unannotated method and an unannotated top-level function. Both must return their object value across the module boundary, and emitted definitions must use pointer return types. Also exercise implicit fallthrough from an unannotated method and require the Python `None` singleton rather than the null error sentinel.

## [Investigation: pcc1 varargs postprocess calls unsupported regex replacement](../investigations/pcc1-varargs-postprocess-callable-replacement.md)

- Test [CONFIRMED] — The command above fails deterministically on the fresh current-source pcc1.
- No.1 Replace callable-regex rewriting with a deterministic LLVM-line scanner [CONFIRMED]
- CONFIRMED — `pcc.codegen.c_varargs.postprocess_varargs_ir` now uses a deterministic line

## [Investigation: wheel-installed pcc1 loses its runtime resource root](../investigations/pcc1-wheel-installed-runtime-resource-location.md)

- Test [CONFIRMED] — The integration test reproduced the failure with a real wheel, uv-created
- No.2 Resolve installed resources from the environment/install prefix [CONFIRMED]
- CONFIRMED — The exact uv wheel integration gate passed after rebuilding stage1:

## [pcc1 deferred-worker object-protocol tax (nested_walk 78.7%)](../investigations/pcc1-worker-object-protocol-tax.md)

- Reproducer / canary `[CONFIRMED]` — Fast loop (~24s, no Stage2 needed): replay one deferred worker item from the
- Measured attribution `[CONFIRMED]` — `scripts/pcc_flamegraph.py cpu <pid> 15` on a live replay (12,570 samples):
- Exception-free attribute probe** `[CONFIRMED, small]` — implemented
- Update 2026-08-31 — metadata-traversal skip `[CONFIRMED, −48.9%]` — The escalation to a representation-level owner found a surgical form first:
- hasattr leak [CONFIRMED, FIXED]**: every `py_obj_getattr(_maybe)` edge
- getattr builtin ownership [CONFIRMED, FILED]**: the reviewer flagged the
- deferred worker deadline [CONFIRMED, FIXED]**: the window lanes had no
- unset vars) [CONFIRMED, FILED]**: another session's in-flight area →
- Site-roots resolution cache `[CONFIRMED, small]`**: every
- CONFIRMED (mechanism) — Stage2 proof pending authorization — The mechanism is confirmed from the receipt and the source; the fix is
- Update 2026-09-04 — Step 10 pcc1 transfer `[CONFIRMED, sub-1% on representative workers]` — The detailed steps 1-10 and per-operation tables live in evidence 001. Fresh

## [Investigation: pproxy HTTP listener resets Claude Code gateway requests](../investigations/pproxy-claude-code-http-proxy-compat.md)

- Test [CONFIRMED] — The raw controls and failing Claude CLI command were observed on 2026-09-02.
- No.2 HTTP protocol detection rejects a valid fragmented proxy preamble [CONFIRMED]
- No.4 Crewmate 2.1.170 is compatible; host Claude 2.1.258 differs [CONFIRMED]
- CONFIRMED — Before the fix, the fragmented CONNECT test returned `False`; after the fix,
- CONFIRMED — `crewmate-runner:latest` contains Claude Code 2.1.170. Its one-turn

## [Investigation: process-tree guard swap-pressure false positive on high-RAM / small-swap hosts](../investigations/process-tree-guard-swap-false-positive-highram.md)

- Fix [CONFIRMED] — Waive the swap-pressure refusal when reclaimable physical memory clears

## [Investigation: process-tree sampler aborts a healthy Stage2 on transient `ps` timeout](../investigations/process-tree-sampler-ps-timeout.md)

- Test [CONFIRMED] — The failure was observed directly in the source-frozen v34 Stage2 run on
- No.1 Bounded process-table retry with receipt accounting [CONFIRMED]
- No.1 is CONFIRMED at focused-tool scope. The sampler now retries 5s→20s once,

## [Investigation: production archive external-resource host stdio symbol collision](../investigations/production-archive-external-resource-host-stdio-symbol-collision.md)

- Test [CONFIRMED] — The failure was observed after the archive member and `nm -A` ownership checks
- No.1 Remove host stdio from the complete-archive harness [CONFIRMED]
- CONFIRMED — The production archive intentionally supplies freestanding `stdin`, `stdout`,

## [Investigation: call.ret.root alloca inside loop bodies leaks stack; hot loops SIGSEGV after ~500K call iterations](../investigations/py-frontend-call-ret-root-alloca-loop-stack-overflow.md)

- Test [CONFIRMED] — LLDB, 2026-08-07:
- No.1 Hoist the call.ret.root slot to the entry block via `_alloca_in_entry` [CONFIRMED, not landed]
- CONFIRMED — Applied experimentally in-session: mini_d went from SIGSEGV to

## [Investigation: container key equality never dispatches user-instance __eq__](../investigations/py-instance-eq-ignored-in-container-keys.md)

- Test [CONFIRMED] — The repro above was run personally on 2026-08-26 and printed
- No.1 CONFIRMED and landed. Both mirrors route the instance fallthrough

## [Investigation: integration empty parameters and latent LLVM opt skips](../investigations/pytest-empty-parameter-and-opt-skip-mechanisms.md)

- Repro [CONFIRMED] — A collection hook inspected selected integration items for pytest skip markers
- Test [CONFIRMED] — The selected integration collection now reports

## [Investigation: object consumers narrow the result of int(value)](../investigations/python-boxed-int-object-consumer-projection.md)

- Test [CONFIRMED] — On 2026-09-07 the minimal native test failed in 3.84 seconds. Artifacts are

## [Investigation: boxed NaN operators inherit container identity semantics](../investigations/python-boxed-nan-value-comparison.md)

- Test [CONFIRMED] — The capability program prints True for boxed self-NaN equality; CPython

## [Investigation: `test_pcc_cross_module_class_schema_matches_local_layout` drifted past `L1CodeGen.__init__` mixin refactor](../investigations/python-class-export-schema-test-mixin-init-drift.md)

- Test [CONFIRMED] — Same pytest case. Pre-fix fails, post-fix passes (along with the
- No.2 Replace AST-side derivation with cross-emit consistency [CONFIRMED]
- CONFIRMED — The test now passes and still guards the actual invariant it was

## [Investigation: Class(args) for a class without an explicit `__init__` synthesises a phantom user_<module>_<class>___init__ symbol → link error](../investigations/python-class-init-phantom-symbol-link-fail.md)

- Test [CONFIRMED] — `tests/python/test_py_exceptions.py
- No.1 Skip the init call at emission sites when no init was found [CONFIRMED]
- CONFIRMED — Root cause: in `class_gen.py:5263-5306`, the constructor lowering tries in

## [Investigation: class instantiation lowering rejects `*args` / `**kwargs` in `__init__`](../investigations/python-class-init-vararg-not-supported.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails (NotImplementedError, then after a partial
- No.2 Pass empty tuple / dict at the call site for unfilled `*args` / `**kwargs` [CONFIRMED]
- CONFIRMED — `phase4/pathlib_parts` corpus case now passes.

## [Investigation: class method body's `from .sibling import name` leaks self.functions binding → top-level def name body attaches to sibling's symbol; local symbol undefined at link time](../investigations/python-class-method-body-from-import-leaks-functions-binding.md)

- Test [CONFIRMED] — Existing focused suites cover the regression surface (50 passed; no new
- No.1 Snapshot and restore `self.functions` around class-method body emission [CONFIRMED]
- CONFIRMED — Root cause: when emitting a class method's body, statement-level lowering

## [Investigation: conditional / indented imports in the entry module fall back to libpython (no-libpython)](../investigations/python-conditional-indented-import-no-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_package_conditional_import.py` — 4 passed (3.99s):
- No.1 Scan the entry module's imports including indented ones [CONFIRMED]
- CONFIRMED — Present-dependency indented imports now compile native and match CPython under

## [Investigation: `__exit__` bound-method dispatch in `call_exit_method` / `pcc_instance_bound_method_entry` returns NULL](../investigations/python-context-manager-exit-bound-method-3arg-dispatch-gap.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails, post-fix passes.
- self) [CONFIRMED]
- CONFIRMED — Repro probe with the patched archive now prints `context-ok` and exits 0.

## [Investigation: py_cpy_call_kw NULL kwarg value crashes inside PyDict_SetItemString — defensive diagnostic + upstream bug located](../investigations/python-cpy-call-kw-null-kwarg-segfault-diagnostic.md)

- Test [CONFIRMED] — Defensive guard (this fix): no dedicated unit test added; the focused
- No.1 Defensive NULL-kw-value guard in py_cpy_call_kw [CONFIRMED]
- CONFIRMED — Root cause: `py_cpy_call_kw` did not validate `kw_vals[i]` before passing it

## [Investigation: cpython-compat `import numpy` fails at `_multiarray_umath` C-extension init](../investigations/python-cpython-compat-import-numpy-multiarray-init-fails.md)

- Update — ROOT CAUSE CONFIRMED (code-level), 2026-05-29 — Read pcc's shim impls in `py_capi_shim.c`:
- Root cause CONFIRMED: the no-libpython C-API shim (`py_capi_shim.o`, ~286 `Py*` symbols,

## [Investigation: deep dotted package attribute access `a.b.c.X` raises AttributeError (no-libpython)](../investigations/python-deep-dotted-package-attr-no-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_package_deep_dotted_attr.py` — 4 passed (3.93s):
- No.1 Flatten the dotted module chain and resolve the spelled name in the native export table [CONFIRMED]
- CONFIRMED — Repro + all bisect cases now match CPython under

## [Investigation: class accessed via dotted module path `import p.sub; p.sub.Thing(...)` fails (no-libpython)](../investigations/python-dotted-module-class-attr-no-libpython.md)

- active — root cause CONFIRMED + bisected 2026-05-31; a speculative fix was
- Test [CONFIRMED failing] — The repro above (`AttributeError: sub`) and the LOAD variant (`TypeError`) are

## [Investigation: `enum` recursive stdlib pull blocks native Enum support](../investigations/python-enum-stdlib-skip-uses-native-enum-members.md)

- Test [CONFIRMED] — Both the pytest target and the minimal probe pass.
- stdlib walker skips compiling `pcc/py_stdlib/enum.py` [CONFIRMED]
- CONFIRMED — `test_t1_metaclass_type_enum_abcmeta_compiled` 1 passed in 0.85s

## [Investigation: exact-int branch rebind loses the scalar fallthrough value](../investigations/python-exact-int-branch-representation-join.md)

- Test [CONFIRMED] — The checked strict-xfail test above reproduced the defect on 2026-08-08 in
- This update is deliberately not a CONFIRMED verdict: the user requested an

## [Investigation: extern c_ptr arg for a Name leaks py_cpy_wrap_pcc_<N>arg into the no-libpython runtime archive](../investigations/python-extern-c_ptr-funcname-libpython-wrap-leak.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails, post-fix passes.
- No.1 Special-case extern call arg `Name → c_ptr` to emit the raw fn-ptr [CONFIRMED]
- CONFIRMED — `py_re.o` no longer references `py_cpy_wrap_pcc_2arg` (`nm` returns no

## [Investigation: fake `dlfcn.h` omits `RTLD_LAZY` used by `py_http.c`](../investigations/python-fake-libc-dlfcn-rtld-lazy-missing.md)

- Test [CONFIRMED] — Both repros were observed on 2026-07-22. The single-source gate fails in
- CONFIRMED — focused fake-libc/C tests: 19 passed in 0.65s;

## [Investigation: `fake_libc_include/netdb.h` and `sys/socket.h` lack `addrinfo` / `sockaddr` so pcc can't compile `py_http.c`](../investigations/python-fake-libc-netdb-socket-addrinfo-missing.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails, post-fix passes. Full
- headers [CONFIRMED]
- CONFIRMED — `tests/python/test_py_runtime_pcc_emit.py` 67 passed (was 1 failure

## [Investigation: for v in [int list] stores i64 into a boxed (ptr) slot — link error / empty loop](../investigations/python-for-list-int-elem-boxed-slot-i64-store.md)

- Test [CONFIRMED] — Already-present tests gate it (they were failing before the fix, pass after):
- No.1 Gate the native-i64 list fast path on a native-i64 target slot [CONFIRMED]
- CONFIRMED — Root cause: `_emit_for_list_index` allocates the loop-variable slot via

## [Investigation: `<object tag=N>` rendering of CPython PyObjects in pcc containers](../investigations/python-format-cpy-object-render-hook.md)

- Test [CONFIRMED] — Direct hook-exercising test wasn't added: with the prior native-format
- `py_cpy_ensure_init`. [CONFIRMED]
- CONFIRMED — Mandatory bootstrap gate

## [Investigation: `name = "<literal>"; name.format(...)` falls to libpython fallback even though the format string is statically a literal](../investigations/python-format-name-bound-literal-falls-to-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_str_format_index.py::test_str_format_name_bound_literal_takes_native_path` — 1 passed in 0.84s. Differential vs CPython `subprocess.run([sys.executable, src])`; asserts identical stdout for both the for-loop and the dict-comprehension shapes.
- No.1 Extend `_maybe_emit_literal_str_format` to accept `Name` references when the function body contains exactly one `name = <StrLit>` Assign and no other rebind. [CONFIRMED]
- CONFIRMED — New regression `test_str_format_name_bound_literal_takes_native_path` -> 1 passed (differential vs CPython; covers both for-loop and dict-comprehension forms of c8/c21).

## [Investigation: `__format__(self, spec)` receives self as `spec` when method is a bound PyFunc](../investigations/python-format-spec-user-format-bound-method-self-prepend.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails, post-fix passes.
- No.1 Drop the redundant self from args for the PyFunc bound path [CONFIRMED]
- CONFIRMED — Targeted test now passes.

## [Investigation: `from package import submodule` falls back to libpython (no-libpython)](../investigations/python-from-package-import-submodule-no-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_package_from_import_submodule.py` — 4 passed (3.89s):
- No.1 Add each `from pkg import name` imported name as a submodule discovery candidate [CONFIRMED]
- CONFIRMED — Repro + all bisect cases now compile native and match CPython under

## [Investigation: `f"{x}"` lowered to `str(x)` skips user `__format__` override](../investigations/python-fstring-bare-expr-skips-user-format-override.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails, post-fix passes.
- No.2 Patch `_e_FString` to wrap parts in `format(inner, "")` [CONFIRMED]
- CONFIRMED — `test_format_protocol.py` 7 / 7 (was 6 passed / 1 failed).

## [Investigation: Builtin f-string format specs still fail](../investigations/python-fstring-format-spec-builtins.md)

- Test [CONFIRMED] — The repro above was run on 2026-05-08 and failed because
- No.1 Add builtin decimal format helper for comma and zero-padded d [CONFIRMED]
- CONFIRMED — Implemented:

## [Investigation: function-local class hoisting publishes the class after its first use](../investigations/python-function-local-class-hoist-order.md)

- Test [CONFIRMED] — Confirmed on 2026-08-14 with fail-fast serial execution. The accidental

## [Investigation: enumerate(...) index inside a generator resets to <null> after the first yield](../investigations/python-generator-enumerate-counter-not-persisted.md)

- Test [CONFIRMED] — `tests/python/test_python_generator_parity.py::test_generator_enumerate_resumes`
- No.1 Reserve a deterministic frame slot for the enumerate counter [CONFIRMED]
- CONFIRMED — Root cause: `_normalise_for_enumerate` desugars `for idx, v in enumerate(xs)` to

## [Investigation: owned-local flag cache leaks across sibling generators — self-backend rejects undefined pointer value (closes the long-standing numpy `.owned.N` cap at 149 IR modules)](../investigations/python-generator-owned-flag-cache-leaks-across-sibling-generators.md)

- Test [CONFIRMED] — `tests/python/test_python_generator_parity.py::test_generator_sibling_owned_flag_isolation`
- No.1 Reset `_owned_local_flag_slots` / `_gc_rooted_local_names` to fresh in the user-function-lowering generator branch [CONFIRMED]
- CONFIRMED — Root cause: `_emit_user_function` saves the caches at line 957-958

## [Investigation: range(...) for-loop inside a generator stops after the first yield](../investigations/python-generator-range-loop-counter-not-persisted.md)

- Test [CONFIRMED] — `tests/python/test_python_generator_parity.py::test_generator_range_loop_resumes`
- No.1 Route range(...) through the resumable object-iterator inside generators [CONFIRMED]
- CONFIRMED — Root cause: `for ... in range(...)` takes the inline "L1 fast path" whose loop

## [Investigation: hasattr() on a native-builtin module statically folds to False for any attr not in pcc's hardcoded attr table](../investigations/python-hasattr-static-false-on-builtin-modules.md)

- Test [CONFIRMED] — Focused regression: 60 passed across
- No.1 Remove the static-False fall-through for unknown native-table attrs [CONFIRMED]
- CONFIRMED — Root cause: `_maybe_emit_native_module_hasattr` historically returned

## [Investigation: Python mixed integer/float comparisons lose integer precision](../investigations/python-int-float-lossy-comparison.md)

- Test [CONFIRMED] — The retained program exercises typed and boxed paths with a CPython control.

## [Investigation: `test_all_ir_pass_modules_emit_llvm_in_strict_frontend_mode` — `alias_analysis.py` Enum + `parity.py` harness deps](../investigations/python-ir-passes-self-compile-enum-and-parity-harness.md)

- Test [CONFIRMED] — Same pytest case; pre-fix 1 fail, post-fix 1 pass.
- carrier [CONFIRMED]
- No.2 Skip `parity.py` (harness module, not a core pass) [CONFIRMED]
- CONFIRMED — `import alias_analysis; AliasResult.NoAlias / MayAlias / ...` all
- CONFIRMED — Test passes after the skip.

## [Investigation: Native module attribute writes still require libpython](../investigations/python-module-attr-write-native.md)

- Test [CONFIRMED] — The repro above was run on 2026-05-08 and failed with the expected
- No.1 Store native builtin module attrs in compiler-owned PyObject* globals [CONFIRMED]
- CONFIRMED — Implemented in `pcc/py_frontend/codegen/layer1.py`:

## [Investigation: Python NaN truth and inequality use ordered predicates](../investigations/python-nan-ordered-predicates.md)

- Test [CONFIRMED] — The current-source capability program executes the failure. The durable

## [Investigation: `mod_alias.module_global_attr` (e.g. `_mat.__all__`) failed with AttributeError because (a) Attr-RHS exports were dropped and (b) Attr lowering had no `module_global` branch](../investigations/python-native-module-alias-module-global-attr-attribute-error.md)

- Test [CONFIRMED] — End-to-end: the numpy auto-mode compile now runs past the original line
- No.1 Recognise Attr-expression RHS in `_export_static_literal_type` + add `module_global` branch to Attr lowering [CONFIRMED]
- CONFIRMED — Root cause was a compounded bug across the multi-file export pipeline:

## [Investigation: `from .sibling import name` nested inside top-level `if/try` skips native predeclare](../investigations/python-nested-if-import-from-skips-native-predeclare.md)

- Test [CONFIRMED] — `tests/python/test_py_multi_file_compile.py::MultiFileCompileTests::test_import_from_inside_top_level_if_predeclares_native_export -> 1 passed in 1.18s` (default LLVM backend, frontend-only test — the predeclare is backend-independent).
- No.1 Add native cross-module predeclare branch to `_prescan_nested_imports`. [CONFIRMED]
- CONFIRMED — Frontend test `test_import_from_inside_top_level_if_predeclares_native_export` passes; IR contains no `py_cpy_*` helper calls and no `name 'ones' is not defined` literal.

## [Investigation: wire the pcc-native C-API header surface into the real numpy build (pcc-native include redirect)](../investigations/python-no-libpython-numpy-build-pcc-capi-include-redirect.md)

- No.1 Gated generic pcc-native include redirect in `build_exec` + curated C-API include dir [CONFIRMED: redirect mechanism unit-gated + validated vs real numpy (38/40); full execute=True build + runtime core pending]
- CONFIRMED at unit level (2026-05-29) — Implemented in `pcc/package/build_exec.py`, resolving the curated-dir design

## [Investigation: `os.path.join(a, *parts)` native lowering smuggled CPython parts into `py_list_extend`](../investigations/python-os-path-join-splat-dyn-arg-safety-bound.md)

- Test [CONFIRMED] — Same pytest case. Pre-fix fails, post-fix passes; the typed
- No.1 Require typed `list` / `tuple` for the starred inner expression [CONFIRMED]
- CONFIRMED — `tests/python/test_native_os_path_join_splat.py` — 5 passed (was 1

## [Investigation: package __init__ computed module-level assignments don't bind as package attrs (no-libpython)](../investigations/python-package-init-computed-module-attr-no-libpython.md)

- Gap originally CONFIRMED + precisely bisected 2026-05-31 by probing B-P0-PKG
- No.1 Register computed module-top assignments as DynType module_global exports [CONFIRMED]
- CONFIRMED — Repro cases now match CPython under `--backend self --python-libpython=off`:

## [Investigation: submodule module-top runs BEFORE its package __init__ (module-init order, no-libpython)](../investigations/python-package-init-submodule-exec-order-no-libpython.md)

- active — root cause CONFIRMED + definitively bisected 2026-05-31; the fix is a
- Test [CONFIRMED] — The order probe above is the deterministic gate (pcc vs CPython stdout order).

## [Investigation: `libpy_runtime_pcc.a` duplicate-symbol `pcc_gc_thread_unregister_buffers`](../investigations/python-pcc-built-archive-weak-symbol-duplicates.md)

- Test [CONFIRMED] — `test_runtime_oracle_diff.py` 21 / 21 passing (was 13 failures + 8
- No.2 Drop the weak fallback; declare extern only [CONFIRMED]
- CONFIRMED — `tests/python/test_runtime_oracle_diff.py` 21 passed, 6 skipped

## [Investigation: `pcc/__main__.py` 4 → 0 fallback via `cli_bootstrap` static export](../investigations/python-pcc-main-static-export-cli-bootstrap.md)

- Test [CONFIRMED] — `tests/python/test_fallback_baseline.py` +
- `pcc.__main__` in the consumer allowlist [CONFIRMED]
- CONFIRMED — Standalone compile of `pcc/__main__.py` emits 0 `py_cpy_*` calls.

## [Investigation: pcc1 list-of-functions value-position lowering via syntactic FuncDef fallback](../investigations/python-pcc1-list-of-functions-syntactic-fallback.md)

- Test [CONFIRMED] — Same pytest case; pre-fix fails, post-fix passes (1 passed in 1.12s).
- No.2 Syntactic fallback on element shape (Name in self.functions) [CONFIRMED]
- CONFIRMED — `tests/python/test_pcc1_pytest_capable.py::test_pcc1_runs_test_list_via_indirect_calls`

## [Investigation: `pcc.parse.py_parse` 10→0 off-mode fallback via `py_lex` static export](../investigations/python-py_parse-static-export-py_lex-ratchet-shrink.md)

- Test [CONFIRMED] — `tests/python/test_fallback_baseline.py` + `_ir_py_fallback_baseline.py`
- No.1 Add static export entries for `pcc.parse.py_lex` [CONFIRMED]
- CONFIRMED — `pcc/parse/py_parse.py` standalone compile: 10 → 0 `py_cpy_*` calls.

## [Investigation: scaffold tests assert OFF-mode `py_cpy_*` behavior that pcc closed](../investigations/python-scaffold-off-mode-historical-py_cpy-assertions-stale.md)

- Test [CONFIRMED] — Same pytest run; pre-fix 5 fail / 6 pass, post-fix 11 / 11 pass.
- No.1 Flip the polarity to "OFF must not regress past ON" [CONFIRMED]
- No.2 Update stale ScaffoldUnsupportedError import path [CONFIRMED]
- CONFIRMED — `test_ir_scaffold_load.py` 4 / 4 (was 2 failures);
- CONFIRMED — `test_load_arity_check` now passes.

## [Investigation: 5 self-backend asm tests assert idioms that codegen optimized past](../investigations/python-self-backend-asm-assertions-stale.md)

- Test [CONFIRMED] — Same pytest run; pre-fix 5 failures / 509 passes, post-fix 264 /
- No.1 Replace exact-asm assertions with semantic-shape assertions [CONFIRMED]
- CONFIRMED — `tests/c/test_self_backend.py` 264 / 264.

## [Investigation: `from package import *` star import falls back to libpython (no-libpython)](../investigations/python-star-import-no-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_package_star_import.py` — 3 passed (3.10s):
- No.1 Record the module for a `*`-only from-import in discovery [CONFIRMED]
- CONFIRMED — Star imports now compile native and match CPython under

## [Investigation: `test_runtime_substrate_spike.py` two stale-shape failures](../investigations/python-substrate-spike-test-sweep.md)

- Test [CONFIRMED] — Same pytest run; pre-fix 2 failures / 32 passes, post-fix 34 / 34 pass.
- No.1 Update `_lookup_slot` regex to accept the 5-param shape [CONFIRMED]
- No.2 Inline strcmp in `py_obj_ops_compare.py` [CONFIRMED]
- No.3 Add `py_str_accessors` to the `py_mem_` allowlist [CONFIRMED]
- CONFIRMED — re-passes.
- CONFIRMED — py_obj_ops_compare.py no longer matches `py_subs_` substring.
- CONFIRMED — `test_active_python_runtime_modules_do_not_call_substrate_helpers`

## [Investigation: shared Python target omitted from native provider closures](../investigations/python-target-provider-closure.md)

- Test [CONFIRMED] — `test_python_target_contract.py::test_host_pcc_emits_native_python_target_contract_with_c_runtime`
- No.1 Admit registered shared components through both provider closures [CONFIRMED]
- CONFIRMED — Focused closure checks cover recursive and shallow admission. After the

## [Investigation: User iterator __next__ advances state twice](../investigations/python-user-iter-double-advance.md)

- Test [CONFIRMED] — The repro above was run on 2026-05-08 and failed with output `['1', '3', '5']`
- No.1 Preserve user __next__ return value before state update [CONFIRMED]
- CONFIRMED — Generated IR showed only one `Range3.__next__` call per loop iteration, but the

## [Investigation: `yield a, b` misparses as `(yield a), b` and leaks the `_yield` sentinel](../investigations/python-yield-tuple-misparse-leaks-yield-sentinel.md)

- Test [CONFIRMED] — `tests/python/test_python_generator_parity.py::test_generator_yields_implicit_tuple`
- No.1 Make `_parse_yield_expr` consume the testlist into an implicit tuple [CONFIRMED]
- CONFIRMED — Minimal repros (`yield 1, 2`; tuple-unpack-for + `yield a, b`) now print the

## [Investigation: raise UserExceptionSubclass(args) skips __init__, loses instance attributes (no-libpython)](../investigations/raise-user-exception-subclass-skips-init-loses-attrs.md)

- Test [CONFIRMED] — Probe /tmp/gap_probe/exc_attr.py: `e.code`, `str(e)`, direct `MyError(500,"boom").code`,
- Root cause (CONFIRMED by IR + runtime read) — `pcc/py_frontend/codegen/exception_lowering.py` `_emit_exception_value`
- No.1 Raise a properly-constructed instance + teach exc machinery about instances [CONFIRMED #40]
- test_native_user_exception_attrs.py 1 passed. FULL bootstrap PASSED (b12d08mom, 18 passed/4 skipped, 614s/10:14). CONFIRMED #40.
- pending — CONFIRMED multi-part (deferred to a focused session) — Root cause confirmed. The str(e)/args sub-item (3) is CONFIRMED to need real
- Root cause confirmed. The str(e)/args sub-item (3) is CONFIRMED to need real

## [Investigation: reflected arithmetic operators __radd__/__rmul__/... (no-libpython)](../investigations/reflected-arithmetic-operators-no-libpython.md)

- active — gap CONFIRMED; a FRONTEND static-dispatch fix was ATTEMPTED and

## [Investigation: resurrection test class attributes lower through libpython](../investigations/resurrection-class-attr-libpython-fallback.md)

- Test [CONFIRMED] — The command above was run on 2026-05-08 and produced:
- No.1 Add native dyn-list `clear()` dispatch [CONFIRMED]
- CONFIRMED — IR-only reductions showed the fallback source:

## [Investigation: rebuilt pcc runtime archive leaves `__atomic_thread_fence` unresolved](../investigations/runtime-oracle-pcc-archive-atomic-thread-fence.md)

- Test [CONFIRMED] — `test_gcc_atomic_fetch_and_lock_builtins_lower_to_llvm_atomics` was extended
- No.1 Lower GCC thread-fence orders to LLVM fence instructions [CONFIRMED]
- CONFIRMED — The focused GCC atomic regression passed, a freshly emitted `pcc_threads.o`

## [Investigation: runtime-oracle pcc archive rejects host `stdatomic.h` expansion](../investigations/runtime-oracle-pcc-archive-stdatomic-preprocess.md)

- Test [CONFIRMED] — The direct make command above deterministically reproduces the failure at the
- No.1 Add a fake-libc C11 `stdatomic.h` mapped to pcc-supported atomic builtins [CONFIRMED]
- CONFIRMED — The focused C11 regression passed, and the archive advanced past preprocessing

## [Investigation: native self-backend emission from pcc1 without host Python](../investigations/s-p0-native-self-emitter-no-host.md)

- Test [CONFIRMED] — A minimal pcc-native helper entry importing the full backend closure first
- Test [CONFIRMED] — With both host process escape hatches made executable failures,
- Runtime ownership failure [CONFIRMED] — The same crash reproduced in a newly linked LLVM-backed oracle, ruling out the
- Strict application result [CONFIRMED] — After rebuilding the normal precompiled pcc-Python runtime archive, the exact
- Bootstrap failure split [CONFIRMED] — The first final-state GC0 run reached `pcc1 -> pcc2` and exposed two distinct
- Bootstrap harness boundary [CONFIRMED] — The full gate also proved two harness assumptions that needed to be explicit:
- Final gates [CONFIRMED] — Strict real M1 canary behavior, with the test setting both host escape hatches

## [Investigation: HEAD ad60403d drops the entry module's trailing `main()` call — every self-backend program ending in `main()` silently does nothing (exit 0, no output)](../investigations/self-backend-entry-main-call-dropped-exitcode-regression.md)

- Test [CONFIRMED] — Observed under the pytest command above (14 failures, e.g.
- checks all CONFIRMED before touching the fix:
- mapping) instead of reverting it [CONFIRMED]
- CONFIRMED — Minimized repros: `-> None` main prints and exits 0; `-> int` main prints

## [Investigation: self-backend freestanding external-resource harness stalls](../investigations/self-backend-freestanding-external-resource-harness-stalls.md)

- Test [CONFIRMED] — `tests/python/test_freestanding_gc_external_resource.py` compiles the exact
- No.1 Split the combined harness and identify the first self-only boundary [CONFIRMED]
- No.2 Repair the smallest proven self-backend lowering/runtime defect [CONFIRMED]
- CONFIRMED — The harness now accepts `single`, `threaded`, and `metal` phases and disables
- CONFIRMED — The unoptimized assembly correctly loaded zero into `w10` before

## [Investigation: self-backend nested valueclass payload aggregate returns](../investigations/self-backend-nested-valueclass-payload-aggregate-return.md)

- Test [CONFIRMED] — The focused valueclass regression first failed on IR shape before the frontend
- No.1 Enable non-recursive nested valueclass direct payload ABI [CONFIRMED]
- No.2 Parse nested literal aggregate returns/extracts/calls structurally [CONFIRMED]
- CONFIRMED — The focused IR-shape assertions in
- CONFIRMED — Focused gates after the fix:

## [Investigation: self-backend nested valueclass payload equality](../investigations/self-backend-nested-valueclass-payload-equality.md)

- Test [CONFIRMED] — The focused regression was added and failed for the expected IR-shape reason:
- No.1 Recurse valueclass payload equality over nested valueclass fields [CONFIRMED]
- CONFIRMED — Focused gate after the fix:

## [Investigation: self-backend sparse SSA cache memory explosion](../investigations/self-backend-sparse-ssa-cache-memory-explosion.md)

- Repro [CONFIRMED] — An RSS-capped run of
- Root Cause [CONFIRMED] — `decode_ssa_name()` cached numeric LLVM SSA names in Python lists indexed by
- Fix [CONFIRMED] — Replace both numeric-name lists with dictionaries keyed by the parsed integer.

## [Investigation: stack-map plan per-call label scans make huge-module emit quadratic](../investigations/self-backend-stackmap-label-scan-quadratic-emit.md)

- Repro [CONFIRMED] — Same RSS-capped single-worker run as the OOM investigation. A macOS `sample`
- Root Cause [CONFIRMED] — `FunctionStackMapPlan.instruction_suffix_lines(block, index)` linearly scans
- Test [CONFIRMED] — + CPU-minutes with no completion before the fix (SIGINT'd, traceback

## [self-backend torture cluster: phi parallel-copy swap + smin/smax-against-zero peephole](../investigations/self-backend-torture-phi-swap-and-minmax-zero-fold.md)

- Test [CONFIRMED] — Before the fixes: `fp-cmp-1` self=250, `pr78559` self=-6, llvm/native=0.
- CONFIRMED — `emit_self_asm` shows the `ifend.1->ifend.2` edge now stages both sources into
- CONFIRMED — `smin(x,0)` now keeps `movz w10,#0`; `smin(x,7)` is unchanged. `pr78559` 2

## [Investigation: self-backend aggregate call signatures for valueclass payloads](../investigations/self-backend-valueclass-aggregate-call-signature.md)

- every comma. CONFIRMED. The failing aggregate argument text

## [Investigation: self-backend verifier dense dominator sets OOM (50 GiB stage1 kill)](../investigations/self-backend-verifier-dense-dominator-sets-oom.md)

- Repro [CONFIRMED] — Surviving temp dirs from failed runs hold the manifests and IR inputs
- Root Cause [CONFIRMED] — `self_backend_verify.py` (new in `a2031b76`; stage1 never ran it before) uses
- Test [CONFIRMED] — Baseline before fix: `tests/c/test_self_backend_verifier.py` — 9 passed.
- (Cooper–Harvey–Kennedy) [CONFIRMED]

## [Investigation: Windows-GNU is classified as a Linux self-backend target](../investigations/self-backend-windows-gnu-target-classification.md)

- Test [CONFIRMED] — Before the fix:
- No.1 Match explicit architecture/vendor/OS components [CONFIRMED — host boundary]
- CONFIRMED — The performance lock was inactive and a nonblocking flock probe succeeded

## [Investigation: self bootstrap closure and IR growth make one GC gate take tens of minutes](../investigations/self-bootstrap-146-module-ir-emission-regression.md)

- Test [CONFIRMED] — The regression is confirmed by checked profile JSON and direct process
- No.1 Attribute and remove unintended closure/IR expansion [CONFIRMED]
- No.2 Improve huge-module self-emitter scheduling after IR shape is understood [CONFIRMED]
- No.3 Remove self-backend linear scans from known-value hot paths [CONFIRMED]
- No.4 Compile native regular-expression programs once [CONFIRMED]
- CONFIRMED — The retained 2026-06-02 pcc2 and the current pcc2 expose exactly 112 and 146
- CONFIRMED — Large compiled-stage modules are now split by a short-lived pcc1 splitter
- CONFIRMED — The worst source-mode module fell from 106.30 seconds to 3.44 seconds. The
- CONFIRMED — The differential regression proves one compile across 100 repeated matches.

## [`self.<class_attr>` inside an inherited method ignores subclass override](../investigations/self-class-attr-ignores-subclass-override.md)

- Fix (CONFIRMED) — `pcc/py_frontend/codegen/class_gen.py`:
- Repro (`docs`-local; CONFIRMED 2026-06-26 under `--python-libpython=off`) — class Base:

## [Investigation: self-host oracle cold warmup cascades across xdist fixtures](../investigations/self-host-oracle-cold-warmup-xdist-fixture-cascade.md)

- Test [CONFIRMED] — The test-infrastructure contract now requires the self-host oracle, runtime
- CONFIRMED — The structural scheduling contract passed, and a genuinely cold source key

## [Investigation: pcc1→pcc2 stage 2 fails with LiftError AttributeError obj across unrelated modules](../investigations/self-host-stage2-lift-attributeerror-obj.md)

- Test [CONFIRMED] — Failure observed personally 2026-08-26, twice:

## [Investigation: list()/sum()/tuple()/set() consume DynType via len+getitem, silently yielding empty/wrong results for iterator-only objects (generators)](../investigations/sequence-builtins-len-getitem-not-iterator-protocol.md)

- Test [CONFIRMED] — Run 2026-05-30: the direct for-loop is diff-IDENTICAL; `list(gen())` and

## [Investigation: `set(d)` / `frozenset(d)` on a mapping lower to an EMPTY set in pcc-compiled code](../investigations/set-and-frozenset-of-dict-lower-to-empty.md)

- Test [CONFIRMED] — `tests/python/test_native_set_from_dict_keys.py` (4 cases). Confirmed red on
- No.1 route a statically known `DictType` argument through `py_dict_keys` `[CONFIRMED]`
- No.3 sweep the rest of the builtin-over-mapping family `[CONFIRMED]`
- CONFIRMED — gtimeout 400s env -u LC_ALL uv run pytest -q -x -n0 \
- Stage1 verification `[CONFIRMED]` — A Stage1 built from the fixed source (`scripts/bootstrap.sh --backend self
- No.3 sweep the rest of the builtin-over-mapping family `[CONFIRMED]` — The defect is a *pattern*, not one call site: "get the length, then index
- CONFIRMED — Both probe sweeps are byte-identical to CPython's output, and:
- Update (2026-08-27): the family's error paths — three silent runtime contracts and the fail-closed fix [CONFIRMED, fixed at the frontend] — The external reviewer's P1 (exception-path owned-ref leaks in the swept
- Update (2026-08-27, same slice): zip(strict=True) now enforces ValueError [CONFIRMED fixed] — The reviewer's P2 — both zip paths silently truncated under ``strict=True`` —

## [Investigation: shared-object refcount contention destroys thread scaling; gc.immortalize restores it on backend 0](../investigations/shared-refcount-contention-thread-scaling.md)

- Test [CONFIRMED] — Measured 2026-08-07 on macOS arm64 (M-series), speedup = serial / parallel:
- No.1 Expose the immortal bit as `gc.immortalize(obj)` [CONFIRMED]
- CONFIRMED — Scaling: the matrix above (0.33x -> 4.20x on backend 0).

## [Investigation: sorted()/min()/max() ignore a custom __lt__ (no-libpython)](../investigations/sorted-min-max-custom-lt-not-used-no-libpython.md)

- Test [CONFIRMED] — /tmp/gap_probe/sorted_lt.py (sorted + .sort + int regression) and lt_direct.py
- Root cause (CONFIRMED) — `py_obj_sorted` (py_obj_ops_compare.c) sorts with `py_obj_cmp_threeway`. For a
- No.4 FRONTEND: route sorted(list-of-custom-__lt__) to the static __lt__ insertion sort [CONFIRMED #54]
- No.5 FRONTEND: route min/max(list-of-custom-__lt__) to a static __lt__ object fold [CONFIRMED #55]
- Root cause of the ineffectiveness — CONFIRMED 2026-05-30 — `py_instance_getattr_default` (py_class.c:71) returns a **bound method**:
- No.4 FRONTEND re-route — sorted() to the static __lt__ insertion sort [CONFIRMED #54]
- CONFIRMED — `/tmp/gap_probe/sorted_lt.py` (list literal + named var + non-mutation check +
- No.5 FRONTEND object fold — min/max(list-of-custom-__lt__) [CONFIRMED #55]
- CONFIRMED — `/tmp/gap_probe/minmax_lt.py` (`min(xs).v`/`max(xs).v`, list-literal + named var)

## [Investigation: *iterable splat in a builtin call / *args forwarding (no-libpython)](../investigations/splat-unpack-in-builtin-call-no-libpython.md)

- No.1 print(*items): build args via _emit_pcc_args_list, convert list→tuple, py_print_many [CONFIRMED #62]
- No.2 zip(*matrix): runtime transpose via a py_zip_star helper [CONFIRMED #64] (max/min(*) still pending — rare)
- No.3 g(*xs) into def g(*args): fix the *args-binding to receive the forwarded list [CONFIRMED #63]
- No.2 zip(*matrix) [CONFIRMED #64]
- CONFIRMED — `/tmp/gap_probe/zipstar.py` IDENTICAL (transpose comprehension + list(zip(*m)),
- No.3 g(*xs) into def g(*args) [CONFIRMED #63]
- CONFIRMED — `/tmp/gap_probe/argsfwd.py` IDENTICAL (g(*xs), list-literal splat, h(1,*xs),
- No.1 print(*items) [CONFIRMED #62]
- CONFIRMED — `/tmp/gap_probe/printsplat.py` IDENTICAL (print(*items), print(*[...]), mixed

## [Investigation: cold self-host stage1 build >25 min / never completes — link + frontend speedup to ~5.1 min](../investigations/stage1-cold-build-speedup-2026-08-15.md)

- Test [CONFIRMED] — Before: measure2-6 (2026-08-15) each killed by the 25-30 min watchdog; the
- No.1 Restore bootstrap frontend codegen parallelism (8-10 workers) [CONFIRMED]
- No.2 Pack self-backend stack-map text emission (96.7M lines -> 3.4M) [CONFIRMED]
- No.3 Batch stack-map decode / cursor-walk address offsets / inline location validation [CONFIRMED]
- No.4 Raw-byte stack-map merge in the link (no 30M object materialization) [CONFIRMED]
- No.5 Structural stack-map scan in object validation (no 3x full decode) [CONFIRMED]
- No.6 Disable CPython cycle collector for the batch link + assemble workers [CONFIRMED]
- No.7 Parallelize the pcc-Python runtime archive rebuild (make -j8) [CONFIRMED]
- No.8 Raise merged-closure limits (MAX_LOCATIONS 16M->128M, _MAX_COUNT 1M->8M) [CONFIRMED]
- instead of backed by file padding bytes [CONFIRMED]
- CONFIRMED — Frontend codegen phase: serial 1-2 workers for 13+ min -> 10 workers, ~38 s
- CONFIRMED — One module's `.s`: 96,783,254 lines / 1.1 GB -> 3,412,758 lines / 458 MB.
- CONFIRMED — Per-giant-file worker chain (assemble + from_sections + encode): >320 s ->
- CONFIRMED — Cache-hit min link (1 giant + 5 small): ~100 s semantic -> 38 s total.
- CONFIRMED — Per-giant worker chain 73 s -> ~24 s (assemble ~20 s + from_sections/encode
- CONFIRMED — `sample` of the semantic link showed ~84% CPU in `gc_collect_main` /
- CONFIRMED — Cold runtime rebuild ~20-30 min serial -> ~4-6 min with -j8 (one-time per
- CONFIRMED — The full link previously failed with "too many stack-map locations" / "native
- CONFIRMED — Pre-fix the pcc1's `__bss`/`__common` globals read the file's padding bytes;

## [Investigation: private Stage1 environment disables every worker bytecode cache](../investigations/stage1-private-pycache-disabled-worker-startup.md)

- Test [CONFIRMED] — The timing defect was observed on 2026-08-31 with the frozen v19 source and
- No.1 Allow writes only to each arm's private pycache [CONFIRMED]
- CONFIRMED — `_measurement_env` now removes inherited `PYTHONDONTWRITEBYTECODE` while

## [Investigation: Stage1 receipt validator rejects current producer metric labels](../investigations/stage1-receipt-metric-contract-rejected.md)

- Test [CONFIRMED] — The retained-receipt check failed exactly as above. The new
- No.1 Admit exact current measurement contracts alongside original v1 [CONFIRMED]
- CONFIRMED — Producer-shaped and original-v1 cases pass; partial/wrong/type-changed labels

## [Investigation: stage1 self-backend ir-scaffold segfault](../investigations/stage1-self-backend-ir-scaffold-segfault.md)

- Test [CONFIRMED] — The repro command above is the current gating test. It has been run locally and observed to crash.

## [Investigation: Stage2 coordinator exceeds memory cap at deferred checkpoint](../investigations/stage2-deferred-checkpoint-coordinator-memory-cap.md)

- Test [CONFIRMED] — The run terminates MEMORY_LIMIT/-15 after131.407s. Tree peak8,592,687,104B;

## [Investigation: str()/print()/format() of an instance does not fall back to __repr__ (no-libpython)](../investigations/str-print-format-no-repr-fallback-no-libpython.md)

- Test [CONFIRMED] — `tests/python/test_native_str_falls_back_to_repr.py` — only-`__repr__` class via
- No.1 py_obj_str falls back to py_obj_repr when __str__ absent [CONFIRMED]
- No.2 container element repr [CONFIRMED #39] + default repr for no-__repr__ [pending — rare]
- CONFIRMED — test_native_str_falls_back_to_repr.py 1 passed. repr_fb probe: cases print(a),

## [Investigation: strict decref ignores dynamic C-extension type tags](../investigations/strict-cext-dynamic-tag-decref-parity.md)

- Test [CONFIRMED] — The repro above deterministically observes the missing strict decref/dealloc.

## [Investigation: tests/conftest.py legacy path shim silently corrupts `Path(__file__).resolve()` repo-root arithmetic in 18 test files](../investigations/tests-conftest-legacy-path-shim-false-repo-root.md)

- Test [CONFIRMED] — The repro above was observed SKIPPED before the fix and PASSED with
- No.1 Migrate the 18 `parents[2]` victims to an AGENTS.md walk-up helper [CONFIRMED]
- No.2 Allowlist `pcc/cli_bootstrap.py` for torch *metadata mentions* [CONFIRMED]
- CONFIRMED — All 18 files: 248 passed, 1 deliberate skip
- CONFIRMED — `env -u LC_ALL uv run pytest -q -rs -n0

## [Investigation: list-indexed threading receivers miss native dispatch](../investigations/threading-list-index-native-dispatch.md)

- Test [CONFIRMED] — Observed locally on 2026-05-08:
- No.1 Confirm BOC and minimal list-indexed repros [CONFIRMED]
- No.2 Generalize native threading method dispatch beyond `Name` receivers [CONFIRMED]
- No.3 Add focused regression tests for list-indexed Lock and Thread methods [CONFIRMED]
- No.4 Re-run BOC ring/cooking and GC threading gates [CONFIRMED]
- No.5 Tune BOC ring speedup workload after correctness fix [CONFIRMED]
- No.6 Propagate native Thread element kind through list comprehension, append(name), and for-loop targets [CONFIRMED]
- CONFIRMED — The BOC ring test fails correctness, BOC cooking times out, and the
- CONFIRMED — The focused tests added under No.3 pass and demonstrate direct native dispatch
- CONFIRMED — Observed:
- CONFIRMED — Observed after No.2:

## [Investigation: list-indexed Thread.start fails under pthread runtime](../investigations/threading-list-index-start-failure.md)

- Test [CONFIRMED] — Manual reduction observed the failure for `n=1`, `n=2`, and `n=4` list sizes.

## [Investigation: threading.Lock loses mutual exclusion under pcc-compiled threads](../investigations/threading-lock-lost-update.md)

- Test [CONFIRMED] — Regression test added:
- No.1 Confirm the pcc-compiled Python lost-update path [CONFIRMED]
- No.2 Abort or raise on failed Lock/RLock/Condition/Semaphore acquire [CONFIRMED]
- No.3 Audit `py_instance_get_field(self, 0)` and direct native-threading method dispatch [CONFIRMED]
- CONFIRMED — The test xfails in the current tree and the compiled binary repeatedly prints
- CONFIRMED — The regression no longer relies on ignoring the `False` result. If a native
- CONFIRMED — Generated IR for the reproducer now calls:

## [Investigation: threads-on isolated runtime misses pcc-Python archive members](../investigations/threads-isolated-runtime-missing-python-port-members.md)

- Test [CONFIRMED] — The focused integration test deterministically reached the isolated runtime
- No.1 Reproduce with a retained isolated runtime and inspect the archive/link input [CONFIRMED]
- No.2 Repair archive selection or construction at the demonstrated boundary [CONFIRMED]
- CONFIRMED — The retained run selected the intended private
- CONFIRMED — The new regression first failed with an out-of-closure

## [Investigation: threads-on pcc1 segfaults at the publication barrier](../investigations/threads-pcc1-startup-segfault-after-freestanding-build.md)

- Test [CONFIRMED] — The retained invalid-shape executable is
- No.1 Capture the first native crash stack and owning runtime function [CONFIRMED]
- CONFIRMED — LLDB stopped at `pcc_gc_note_frame_enter + 20`; the next 79 frames alternated

## [Investigation: pow(b, e, mod) (3-arg modular exponentiation) -> NameError under no-libpython](../investigations/three-arg-pow-modexp-no-libpython.md)

- Root cause (CONFIRMED) — `call_expression_lowering.py:789` handles `pow` only for `len(expr.args) == 2`

## [Investigation: tuple completion scans run for no-op publication backends](../investigations/tuple-publication-noop-quadratic-scan.md)

- Test [CONFIRMED] — Native scaling is confirmed; deterministic scan-count regression is pending. Source proof
- No.1 Gate completion scanning by the publication backend [CONFIRMED 2026-09-06: linear native scaling, exact output, 1.25x py_ast PCO worker; staged Stage2/Stage3 qualification still owed].
- Update — same-compiler control, linear scaling and emitter replay `[CONFIRMED]` — HEAD `2203dc3d` equals the frozen v80 snapshot in every build

## [Investigation: tuple()/set()/frozenset() over a custom __iter__/__next__ iterator falls back to libpython](../investigations/tuple-set-frozenset-over-custom-iterator-libpython-fallback.md)

- Root cause (CONFIRMED by bisect) — `_maybe_emit_tuple_builtin` (tuple_zip_lowering.py): handles only literal
- No.1 Route ClassType args through the iterator protocol [CONFIRMED #42 for tuple/set/frozenset]

## [Investigation: a tuple-unpack loop variable shadowing the enclosing method name infers as 'callable'](../investigations/type-infer-loop-var-shadowing-method-name.md)

- Test [CONFIRMED] — Observed 2026-08-27 under the command above, host pcc, current worktree and

## [Investigation: typed-int unboxed arithmetic silently wraps on i64 overflow (violates Python bignum semantics)](../investigations/typed-int-unboxed-overflow-silent-wraparound.md)

- Test [CONFIRMED] — The two repros above were run 2026-05-30: the large-literal/accumulator case is

## [Investigation: typed list mutators return False instead of None](../investigations/typed-list-mutators-return-false-instead-of-none.md)

- Test [CONFIRMED] — The focused test deterministically proves both halves: list contents are
- CONFIRMED — All six typed success returns now use `_emit_none_literal()`. The focused

## [Investigation: unary `-`/`~` on class instances crashed codegen (ClassType -> int coercion)](../investigations/unary-neg-classtype-coercion-crash.md)

- Test [CONFIRMED] — The minimal repro crash was observed; the regression
- No.1 Dispatch `__neg__`/`__invert__` for ClassType operands before numeric coercion [CONFIRMED]
- CONFIRMED — Observed (2026-06-10): minimal repro compiles and prints 42 (auto-mode

## [Investigation: `KnownClass.method(self)` with a CPython-backed base hit the any-class name-scan fallback (bogus arg-count error)](../investigations/unbound-class-method-call-wrong-fallback.md)

- Test [CONFIRMED] — The minimal repro failure was observed byte-identical to the gnu.py
- No.1 Route unresolvable `KnownClass.method(...)` to dynamic getattr-on-class + call [CONFIRMED]
- CONFIRMED — Observed (2026-06-10): minimal repro compiles, IR contains

## [Investigation: uv-locked pcc1 restart loses CalledProcessError returncode](../investigations/uv-locked-pcc1-restart-called-process-error-returncode.md)

- Test [CONFIRMED] — The fixed `build/bootstrap/pcc1` available after the report correctly forwarded
- CONFIRMED — The deterministic infrastructure contract turned GREEN, the complete

## [Investigation: nested ValueBox dynamic equality and hash](../investigations/valuebox-nested-valueclass-dynamic-equality-hash.md)

- Test [CONFIRMED] — The test was added and failed under strict self-backend mode:
- No.1 Normalize valueclass constructor returns to ValueBox at Dyn boundaries [CONFIRMED]
- CONFIRMED — Focused gate after the fix:

## [Investigation: valueclass constructors in attribute stores should box as ValueBox](../investigations/valuebox-valueclass-attribute-store-projection.md)

- Test [CONFIRMED] — Both focused tests above were run and observed failing on 2026-06-04 before the

## [Investigation: valueclass constructor augmented-subscript keys bypass object projection](../investigations/valuebox-valueclass-aug-subscript-key-projection.md)

- Test [CONFIRMED] — `tests/python/test_py_value_class_unboxed.py::test_valueclass_constructor_aug_subscript_key_projection_boxes_valuebox`
- No.1 Emit the aug-assign subscript key via `_emit_subscript_key_object(...)` [CONFIRMED]
- CONFIRMED — Observed gate results after the one-site change (2026-06-10, this host):

## [Investigation: valueclass constructors in builtin object-boundary calls should box as ValueBox](../investigations/valuebox-valueclass-builtin-object-boundary-projection.md)

- Test [CONFIRMED] — The focused pair was run together:
- constructor projection [CONFIRMED]
- CONFIRMED — The first half of the patch routed `repr`, `ascii`, `hash`,

## [Investigation: valueclass constructor compare operands bypass object projection](../investigations/valuebox-valueclass-compare-operand-projection.md)

- Test [CONFIRMED] — Both focused regressions above observed red under the listed command.
- No.1 Route the object-vs-object compare operands through `_emit_expr_as_pcc_object(...)` [CONFIRMED]
- CONFIRMED — Recorded after the gates below ran green (same day):

## [Investigation: valueclass constructors in comprehensions should box as ValueBox](../investigations/valuebox-valueclass-comprehension-projection.md)

- Test [CONFIRMED] — Both focused tests were added and failed:
- No.1 Normalize valueclass constructor expressions before comprehension stores [CONFIRMED]
- CONFIRMED — Focused gates after the fix:

## [Investigation: valueclass constructors in conditional expressions should box as ValueBox](../investigations/valuebox-valueclass-conditional-expression-projection.md)

- Test [CONFIRMED] — Both focused tests above were run and observed failing on 2026-06-04 before the

## [Investigation: valueclass constructor projection through dataclasses.replace keyword overrides](../investigations/valuebox-valueclass-dataclasses-replace-keyword-projection.md)

- Test [CONFIRMED] — `tests/python/test_py_value_class_unboxed.py::test_valueclass_constructor_dataclasses_replace_keyword_boxes_valuebox`
- `_emit_expr_as_pcc_object(...)` [CONFIRMED]
- CONFIRMED — The direct keyword override path now calls `_emit_expr_as_pcc_object(kw_expr)`.

## [Investigation: valueclass constructors in dict builtin keyword values should box as ValueBox](../investigations/valuebox-valueclass-dict-builtin-keyword-projection.md)

- Test [CONFIRMED] — Both focused tests above were run and observed failing on 2026-06-04 before
- No.1 Route builtin dict keyword values through object-boundary projection [CONFIRMED]
- CONFIRMED — `literal_lowering._emit_dict_builtin(...)` now emits keyword values through

## [Investigation: valueclass constructors in dict update keyword values should box as ValueBox](../investigations/valuebox-valueclass-dict-update-keyword-projection.md)

- Test [CONFIRMED] — Both focused tests above were run and observed failing on 2026-06-04 before
- No.1 Route dict method object arguments through object-boundary projection [CONFIRMED]
- CONFIRMED — `dict_lowering._dict_method_box(...)` now emits method argument values through

## [Investigation: valueclass constructors in dynamic callable arguments should box as ValueBox](../investigations/valuebox-valueclass-dynamic-callable-argument-projection.md)

- Test [CONFIRMED] — Both focused tests above were run and observed failing on 2026-06-04 before the

## [Investigation: valueclass constructor exception-message arguments bypass object projection](../investigations/valuebox-valueclass-exception-arg-projection.md)

- Test [CONFIRMED] — IR guard observed red under the command above (the runtime probe was
- No.1 Route the exception message argument through `_emit_expr_as_pcc_object(...)` [CONFIRMED]
- CONFIRMED — Observed (2026-06-10): focused pair -> 2 passed; full unboxed guard -> 39

## [Investigation: valueclass constructors in Any locals and containers should box as ValueBox](../investigations/valuebox-valueclass-local-container-projection.md)

- Test [CONFIRMED] — Both focused tests were added and failed:
- No.1 Normalize valueclass constructor expressions before object-shaped assignment and container stores [CONFIRMED]
- CONFIRMED — Focused gates after the fix:

## [Investigation: valueclass constructors in membership needles should box as ValueBox](../investigations/valuebox-valueclass-membership-needle-projection.md)

- Test [CONFIRMED] — The focused IR guard fails because generated `main` still contains a call to
- No.1 Route membership needles through object-boundary projection [CONFIRMED]
- No.2 Route dict subscript lookup keys through object-boundary projection [CONFIRMED]
- CONFIRMED — After the code change, the focused IR guard passes:
- CONFIRMED — After the code change, the strict self-backend runtime regression passes:

## [Investigation: valueclass constructors in mutation stores should box as ValueBox](../investigations/valuebox-valueclass-mutation-store-projection.md)

- Test [CONFIRMED] — Both focused tests were added and failed:
- No.1 Normalize valueclass constructor expressions before list/dict mutation stores [CONFIRMED]
- CONFIRMED — Focused gates after the fix:

## [Investigation: valueclass constructors in sequence builtin literal sources should box as ValueBox](../investigations/valuebox-valueclass-sequence-builtin-literal-source-projection.md)

- Test [CONFIRMED] — Both focused regressions were run on 2026-06-04 and failed as expected:

## [Investigation: valueclass constructors in set builtin literal sources should box as ValueBox](../investigations/valuebox-valueclass-set-builtin-literal-source-projection.md)

- Test [CONFIRMED] — The focused IR regression above was run on 2026-06-04 and failed with:
- No.1 Use the generic PyObject projection helper for set builtin literal-source elements [CONFIRMED]
- CONFIRMED — The literal-source branch now calls `_emit_expr_as_pcc_object(...)` for each

## [Investigation: valueclass constructors in set method element arguments should box as ValueBox](../investigations/valuebox-valueclass-set-method-element-projection.md)

- Test [CONFIRMED] — Both focused regressions were run on 2026-06-04 and failed as expected:
- No.1 Use the generic PyObject projection helper for set method element arguments [CONFIRMED]
- CONFIRMED — The shared item path used by `set.add`, `set.remove`, and `set.discard` now

## [Investigation: valueclass constructors in short-circuit expressions should box as ValueBox](../investigations/valuebox-valueclass-short-circuit-projection.md)

- Test [CONFIRMED] — Both focused tests above were run and observed failing on 2026-06-04 before
- No.1 Route object-boundary short-circuit expressions through boxed arm phis [CONFIRMED]
- CONFIRMED — `compare_membership_lowering.py` now has an object-boundary short-circuit

## [Investigation: valueclass constructor subscript-store keys bypass object projection (identity instance + unhashable TypeError)](../investigations/valuebox-valueclass-subscript-store-key-projection.md)

- Test [CONFIRMED] — `tests/python/test_py_value_class_unboxed.py::test_valueclass_constructor_subscript_store_key_projection_boxes_valuebox`
- No.1 Route subscript-store dict/object keys through `_emit_subscript_key_object(...)` [CONFIRMED]
- CONFIRMED — Observed gate results after the two-site change (2026-06-10, this host):

## [Investigation: valueclass constructors in super method arguments should box as ValueBox](../investigations/valuebox-valueclass-super-method-argument-projection.md)

- Test [CONFIRMED] — Both focused regressions were run on 2026-06-05 and failed as expected:
- CONFIRMED — Confirmed. The direct method-call branches had already been changed to treat

## [Investigation: valueclass constructor unpack-target subscript-store keys bypass object projection](../investigations/valuebox-valueclass-unpack-subscript-store-key-projection.md)

- Test [CONFIRMED] — `tests/python/test_py_value_class_unboxed.py::test_valueclass_constructor_unpack_subscript_store_key_projection_boxes_valuebox`
- No.1 Route `_store_value_at_subscript(...)` keys through `_emit_subscript_key_object(...)` [CONFIRMED]
- CONFIRMED — Observed gate results after the one-site change (2026-06-10, this host):

## [Investigation: valueclass constructors in user method arguments should box as ValueBox](../investigations/valuebox-valueclass-user-method-argument-projection.md)

- Test [CONFIRMED] — Both focused regressions were run on 2026-06-04 and failed as expected:
- CONFIRMED — Confirmed. Missing annotations are resolved to `DynType` by type inference, so

## [Investigation: `if Segment(...):` crashed codegen — ClassType truthiness unsupported](../investigations/valueclass-condition-truthiness-crash.md)

- Test [CONFIRMED] — The probe sweep observed the codegen crash; the focused regression above
- No.1 Add a ClassType branch to `_truthy` (box payloads, dispatch `py_obj_truthy`) [CONFIRMED]
- CONFIRMED — Observed (2026-06-10): focused regression -> 1 passed (prints

## [Investigation: prove a valueclass hot loop without mistaking tagged-int calls for heap allocation](../investigations/valueclass-hot-loop-zero-allocation-proof.md)

- Test [CONFIRMED] — The focused test above failed deterministically on the over-strong `no call`
- No.2 Combine aggregate IR shape with differential runtime allocation counts [CONFIRMED]
- CONFIRMED — The combined test generates LLVM IR for the 1000-iteration form, verifies that

## [Investigation: valueclass raw payload reaches instance identity surfaces](../investigations/valueclass-identity-surface-diagnostics.md)

- Test [CONFIRMED] — The parameterized source-shape regression above deterministically exposes the
- mutation builtins during type inference. [CONFIRMED]
- CONFIRMED — Attr inference now rejects direct `__dict__` and `__weakref__` access on a

## [Investigation: renamed valueclass exports retain the alias as class identity](../investigations/valueclass-renamed-export-canonical-identity.md)

- Test [CONFIRMED] — The alias cases were observed red after the call_sig gap was isolated. The
- Focused CONFIRMED — The final metadata/IR/export packet is 11 passed. The real slots and stackmap

## [Investigation: weakref on a valueclass payload — identity-escape gap closed at compile time](../investigations/valueclass-weakref-identity-escape.md)

- Test [CONFIRMED] — Pre-fix success observed; post-fix the same source fails to compile
- No.1 Compile-time diagnostic beside the `is` identity-escape check [CONFIRMED]
- CONFIRMED — Observed (2026-06-10 late night): probe now rejected at compile time;

## [Investigation: valueclass payload ABI falls back after four fields](../investigations/valueclass-wide-payload-aggregate-abi.md)

- Test [CONFIRMED] — The focused test above failed deterministically: `1 failed in 0.37s`.
- through seven in both payload type mirrors. [CONFIRMED partial]
- box/unbox helpers in native function adapters. [CONFIRMED]
- CONFIRMED partial — The two narrow branches moved the minimized repro from a `ptr` signature to a
- CONFIRMED — The first adapter patch exposed a control-flow ownership bug: the adapter's

## [Investigation: virtual-thread resume unboxes args/return for a boxed worker (i64 vs ptr LLVM type error)](../investigations/virtual-thread-resume-unboxes-args-for-boxed-worker.md)

- Test [CONFIRMED] — `tests/python/test_virtual_thread_frontend.py` 6/6 pass; the repro runs and prints
- CONFIRMED — /6 vthread frontend tests pass; end-to-end output `1/4/42`. The else-branches

## [Investigation: virtual-thread handler throughput trails asyncio by 9.7x](../investigations/vthread-asyncio-throughput-gap.md)

- Test [CONFIRMED] — The full three-arm baseline completed 90 runs. Both compiler entries passed the
- No.1 bypass zero-timeout IO polling when no fd waiters exist [CONFIRMED]
- No.2 elide repeated retaining stores of the same reference under GC0 [CONFIRMED]
- CONFIRMED — The interposed-kevent test failed with 101 calls before the patch, then passed
- CONFIRMED — Both runtime variants passed the ownership and empty-IO regressions (four

## [Investigation: unrelated parking methods reject a concrete nonparking call](../investigations/vthread-concrete-nonparking-method-collision.md)

- Test [CONFIRMED] — The reduced Resource.close returns 42 without parking. A separate Parking.close

## [Investigation: handled exception context survives only in an SSA value](../investigations/vthread-handler-context-across-park.md)

- Test [CONFIRMED] — A handler catches ValueError, yields, and raises RuntimeError. Before the fix

## [Investigation: generator return consumes a local owner twice](../investigations/vthread-owned-local-return-double-release.md)

- Test [CONFIRMED] — A yielding child builds values=[1,2] and returns values. Before the fix three

## [Investigation: return root does not dominate a parking finally continuation](../investigations/vthread-return-root-after-parking-finally.md)

- Test [CONFIRMED] — The reduced worker returns 42 inside try, then calls a yielding cleanup in

## [Investigation: a single carrier exits with sleeping children pending](../investigations/vthread-single-carrier-pending-timer-exit.md)

- Test [CONFIRMED] — The child sleeps for 20 ms and returns 42. Before the fix, a self/no-libpython

## [Investigation: native threading receiver temporary crosses a park in SSA](../investigations/vthread-threading-receiver-across-park.md)

- Test [CONFIRMED] — A Counter owns a typed Lock field and its increment method yields before

## [Investigation: wheel runtime archive loses target and freshness evidence](../investigations/wheel-pcc-runtime-archive-freshness.md)

- Test [CONFIRMED] — The integration test reproduced both the missing-stamp rebuild and the stale
- No.2 Ship target evidence and force rebuild only when inputs are newer [CONFIRMED]
- CONFIRMED — The exact uv wheel integration gate passed: `1 passed in 6.86s`. The installed

## [Investigation: division/modulo by zero silently yields 0/inf/NULL instead of ZeroDivisionError (no-libpython, six lowering paths)](../investigations/zero-division-silent-no-libpython-six-paths.md)

- Test [CONFIRMED] — `tests/python/test_native_zero_division.py` — inline-dyn, typed+cross-function,
- No.1 Guard all six division lowering paths [CONFIRMED]
- CONFIRMED — `tests/python/test_native_zero_division.py` 3 passed. The four `divzero*` probes
