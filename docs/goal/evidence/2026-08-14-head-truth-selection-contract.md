# Head-truth five-GC selection contract — 2026-08-14

Mode: manifest/workflow parsing and artifact-observation contracts only.

Head-truth manifest cases passed inside the 131-case archive artifact group;
the separate workflow file completed with 3 passed. The registered command
selects integration tests, emits per-node status, and cannot accept missing or
nonpassing GC0..4 observations.

No five-GC bootstrap matrix was launched; that one final run remains open.
