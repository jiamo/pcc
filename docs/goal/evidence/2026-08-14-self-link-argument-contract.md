# Self-link argument honesty focused evidence — 2026-08-14

Mode: host-side route, argument and publication contracts; no compiler build.

`tests/python/test_self_link_argument_contract.py` completed with 39 passed in
fail-fast serial mode. It covers both link call sites, selector validation,
unsupported/map/implied-surface rejection before emission, object/native input
separation, output validation and atomic publication behavior.

This does not include the frozen-source fallback baseline, real executable
link evidence, or sequential pcc1 fixed point.
