/* pcc fake CPython <abstract.h>: the real Python.h is self-contained, so this just
 * pulls it in (numpy includes the sub-header directly in some files). */
#ifndef Py_abstract_H
#define Py_abstract_H
#include <Python.h>
#endif
